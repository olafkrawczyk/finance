---
phase: 10-frontend-cache-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - frontend/src/pages/MonthlyPage.tsx
  - frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-03
must_haves:
  truths:
    - "MonthlyPage uses useTransactionsList, useOpeningBalance, useCategories hooks instead of useState/useEffect"
    - "DashboardPage uses useMonthlySummary, useAssets, useInsightsForecast hooks instead of useState/useEffect"
    - "Both pages display skeleton layouts (not spinner) when isPending is true"
    - "Both pages keep existing data visible during background refetch (isFetching only)"
    - "Mutations in both pages use D-15 broad invalidation pattern"
    - "Derived state (sidebar data, forecast, filters) uses useMemo instead of useEffect + useState"
    - "No manual setTransactions/setData/setError calls remain"
  artifacts:
    - path: "frontend/src/pages/MonthlyPage.tsx"
      provides: "React Query conversion — 3 parallel queries + mutation + skeleton"
      min_lines: 420
      contains: "useTransactionsList"
    - path: "frontend/src/pages/DashboardPage.tsx"
      provides: "React Query conversion — 3 parallel queries + derived state + skeleton"
      min_lines: 290
      contains: "useMonthlySummary"
  key_links:
    - from: "frontend/src/pages/MonthlyPage.tsx"
      to: "frontend/src/lib/query/hooks.ts"
      via: "import { useTransactionsList, ... }"
    - from: "frontend/src/pages/MonthlyPage.tsx"
      to: "frontend/src/components/Skeleton.tsx"
      via: "import Skeleton from '../components/Skeleton'"
    - from: "frontend/src/pages/DashboardPage.tsx"
      to: "frontend/src/lib/query/hooks.ts"
      via: "import { useMonthlySummary, useAssets, useInsightsForecast }"

## Artifacts This Phase Produces

### Modified files:
- `frontend/src/pages/MonthlyPage.tsx` — replaces useState/useEffect/Promise.all with useQuery hooks; replaces spinner loading with skeleton layout; replaces manual delete mutation with useDeleteMutation
- `frontend/src/pages/DashboardPage.tsx` — replaces 3 separate useEffects with parallel useQuery hooks; moves derived state (aiForecast, predictionData, normalizedData) to useMemo; replaces spinner with skeleton layout

### New symbols none — both files are modifications of existing pages, no new exports
---

<objective>
Convert MonthlyPage and DashboardPage from useState/useEffect data fetching to React Query hooks with skeleton loading states. MonthlyPage is the template pattern (D-14) — most complex 3-way Promise.all + mutations. DashboardPage follows the same pattern for 3 parallel queries with derived state.

Purpose: Eliminate manual state management for server data in the two most critical pages; demonstrate the conversion template for all other pages.
Output: MonthlyPage.tsx and DashboardPage.tsx converted to React Query.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-frontend-cache-isolation/10-CONTEXT.md
@.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md
@.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md
@.planning/phases/10-frontend-cache-isolation/10-UI-SPEC.md
@/home/olafk/finance/frontend/src/pages/MonthlyPage.tsx
@/home/olafk/finance/frontend/src/pages/DashboardPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert MonthlyPage to React Query hooks + skeleton layout</name>
  <files>
    frontend/src/pages/MonthlyPage.tsx
  </files>
  <read_first>
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 436-547 for MonthlyPage conversion target code)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 544-569 for MonthlyPage skeleton layout)
    @/home/olafk/finance/frontend/src/pages/MonthlyPage.tsx
    @/home/olafk/finance/frontend/src/lib/query/hooks.ts
    @/home/olafk/finance/frontend/src/components/Skeleton.tsx
  </read_first>
  <action>
    Convert MonthlyPage by applying the following transformations in order. The exact target code is in PATTERNS.md lines 484-547. Follow that structure precisely.

    1. **Imports:**
       - Remove: `import { getTransactions, getOpeningBalance, getCategories, deleteTransaction } from '../api'`
       - Remove: `import React, { useState, useEffect, useMemo } from 'react'` → change to `import React, { useMemo } from 'react'`
       - Add: `import { useTransactionsList, useOpeningBalance, useCategories, useDeleteTransaction } from '../lib/query/hooks'`
       - Add: `import Skeleton from '../components/Skeleton'`

    2. **Replace state declarations:**
       - Delete: `useState<NormalizedTransaction[] | null>` for transactions
       - Delete: `useState` for sidebarData, error, loading, isTruncated
       - Delete: `useState<FilterState>` pattern variables (keep `searchTerm`, `selectedType`, `selectedCategory`, `sortBy`)
       - Keep: `deleteConfirmId`, `deleteError` (these are UI-only state, not server data)
       - Keep: `searchTerm`, `selectedType`, `selectedCategory`, `sortBy` (local UI filter state)
       - Keep: `uniqueCategories` and `filteredAndSortedTransactions` useMemo

    3. **Replace useEffect with query hooks:**
       - Delete the entire `useEffect(() => { ... Promise.all([...]) ... }, [yearMonth])` block
       - Add three parallel hooks at the top of the component function:
         ```
         const { data: txResult, isPending: txLoading, error: txError } = useTransactionsList({ date_from: dateFrom, date_to: dateTo, per_page: 500 });
         const { data: openingBalances, isPending: obLoading } = useOpeningBalance(year, month);
         const { data: categories, isPending: catLoading } = useCategories();
         const deleteMutation = useDeleteTransaction();
         ```
       - Compute `isPending = txLoading || obLoading || catLoading`

    4. **Move derived state to useMemo:**
       - Extract the normalization + sidebar computation from the useEffect into a single `useMemo` with `[txResult, categories]` as dependencies
       - Inside the useMemo: normalize transactions, compute sidebarData, check isTruncated — same logic as before but without `setState` calls; return `{ transactions, sidebarData, isTruncated }`
       - Derive `transactions`, `sidebarData`, `isTruncated` from the useMemo result
       - For error state: use `txError` from the query hook

    5. **Replace handleDelete with mutation:**
       - Delete the entire `handleDelete` async function
       - Replace with: `const handleDelete = (id: string) => deleteMutation.mutate(id)`
       - Remove manual refetch after delete — D-15 broad invalidation in hooks.ts handles this

    6. **Replace loading state with skeleton layout:**
       - Replace the spinner loading block (`if (loading) { return ... spinner }`) with:
         ```
         if (isPending) {
           return (
             <div className="max-w-6xl mx-auto w-full px-4 space-y-6" aria-busy="true">
               <div className="space-y-2">
                 <Skeleton className="h-8 w-64" />
                 <Skeleton className="h-4 w-48" />
               </div>
               <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
                 {[1, 2, 3, 4].map((i) => (
                   <Skeleton key={i} className="h-10 w-full rounded-lg" />
                 ))}
               </div>
               <Skeleton className="h-10 w-full rounded-lg" />
               {[1, 2, 3, 4, 5, 6].map((i) => (
                 <Skeleton key={i} className="h-12 w-full rounded-lg" />
               ))}
             </div>
           );
         }
         ```
       - Follow D-18 skeleton layout: filter bar placeholder + table header + 6 row skeletons
       - Use `aria-busy="true"` on container per UI-SPEC.md

    7. **Error state:**
       - Replace `if (error)` with `if (txError)` — same error display, read from query hook

    8. **Everything else stays the same:**
       - filterAndSortTransactions, extractUniqueCategories (pure functions — unchanged)
       - Filter UI, TransactionTable rendering, MonthSidebar, delete confirmation dialog (unchanged)
       - getMonthName, date parsing (unchanged)

    IMPORTANT: Do NOT change the filter panel, TransactionTable, MonthSidebar, or delete dialog rendering at all. Only the data fetching layer changes.
  </action>
  <verify>
    <automated>grep -c "useTransactionsList\|useOpeningBalance\|useCategories\|useDeleteTransaction" frontend/src/pages/MonthlyPage.tsx && grep -c "import Skeleton from" frontend/src/pages/MonthlyPage.tsx && grep -v "^import\|^//" frontend/src/pages/MonthlyPage.tsx | grep -c "isPending"</automated>
  </verify>
  <acceptance_criteria>
    - No `useState` calls for data/loading/error remain — only UI state (searchTerm, selectedType, etc.) and deleteConfirmId/deleteError
    - No `useEffect` for data fetching remains
    - No `import { getTransactions, ... } from '../api'` — replaced with hooks imports
    - `import { useTransactionsList, useOpeningBalance, useCategories, useDeleteTransaction } from '../lib/query/hooks'` present
    - `import Skeleton from '../components/Skeleton'` present
    - Three query hooks + one mutation hook at top of component
    - Derived state (transactions, sidebarData, isTruncated) computed via useMemo
    - Loading state uses skeleton layout, not spinner
    - Delete mutation uses `deleteMutation.mutate(id)` not manual fetch
    - `isPending` used for skeleton, data exists during re-fetch
  </acceptance_criteria>
  <done>MonthlyPage converted: no useState/useEffect for data, uses useTransactionsList/useOpeningBalance/useCategories/useDeleteMutation, skeleton layout on isPending</done>
</task>

<task type="auto">
  <name>Task 2: Convert DashboardPage to React Query hooks + skeleton layout</name>
  <files>
    frontend/src/pages/DashboardPage.tsx
  </files>
  <read_first>
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 559-641 for DashboardPage conversion target code)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 572-597 for DashboardPage skeleton layout)
    @/home/olafk/finance/frontend/src/pages/DashboardPage.tsx
    @/home/olafk/finance/frontend/src/lib/query/hooks.ts
    @/home/olafk/finance/frontend/src/components/Skeleton.tsx
  </read_first>
  <action>
    Convert DashboardPage by applying the following transformations. The exact target code is in PATTERNS.md lines 592-641.

    1. **Imports:**
       - Remove: `import { getMonthlySummary, getInsightsForecast, getAssets } from '../api'`
       - Remove: `import React, { useState, useEffect, useMemo } from 'react'` → change to `import React, { useMemo } from 'react'`
       - Add: `import { useMonthlySummary, useAssets, useInsightsForecast } from '../lib/query/hooks'`
       - Add: `import Skeleton from '../components/Skeleton'`

    2. **Replace state declarations:**
       - Delete: `useState<NormalizedSummaryRow[] | null>` for data
       - Delete: `useState` for error, loading, aiForecast, assets
       - Keep: no UI-only state to keep — Dashboard is pure data display

    3. **Replace three useEffects with query hooks:**
       - Delete useEffect #1 (getMonthlySummary) 
       - Delete useEffect #2 (getAssets)
       - Delete useEffect #3 (getInsightsForecast with [data] dependency)
       - Add three parallel hooks at component top:
         ```
         const { data: summaryData, isPending, error } = useMonthlySummary();
         const { data: assetsData } = useAssets();
         const { data: forecastData } = useInsightsForecast();
         ```
       - The `isPending` from `useMonthlySummary` gates the skeleton display
       - Assets and insightsForecast can load in background (they'll show when ready)

    4. **Move all derived state to useMemo:**
       - Data normalization (replaces `setData(normalized)`): `useMemo(() => { if (!summaryData) return null; return summaryData.map(...).reverse(); }, [summaryData])`
       - assets parsing (replaces `setAssets(parsed)`): `useMemo(() => (assetsData ?? []).map(...), [assetsData])`
       - aiForecast computation (replaces `useEffect` #3): `useMemo(() => { if (!normalizedData || !forecastData) return null; ... }, [normalizedData, forecastData])`
       - The existing `totalBankBalance`, `currentMonthData`, `predictionData` useMemo blocks can stay — they already derive from state that's now from hooks

    5. **Replace loading state with skeleton layout:**
       - Replace the spinner loading block with:
         ```
         if (isPending) {
           return (
             <div className="space-y-6 max-w-6xl mx-auto w-full px-4" aria-busy="true">
               <div className="space-y-2">
                 <Skeleton className="h-8 w-48" />
                 <Skeleton className="h-4 w-64" />
               </div>
               <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                 <Skeleton className="h-40 rounded-2xl" />
                 <Skeleton className="h-40 rounded-2xl" />
               </div>
               <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                 <Skeleton className="h-80 rounded-2xl" />
                 <Skeleton className="h-80 rounded-2xl" />
                 <Skeleton className="h-80 rounded-2xl" />
                 <Skeleton className="h-80 rounded-2xl" />
               </div>
             </div>
           );
         }
         ```
       - D-18 skeleton: 2 summary tile cards + 4 chart card placeholders

    6. **Error state:**
       - Keep the existing error card display (same pattern, reads from query hook)

    7. **Everything else stays the same:**
       - formatPln, formatMonthLabel, totalNetValue calculation
       - Summary tiles, chart components (BalanceChart, ComboChart, SavingsChart, SavingsLogChart)
       - InsightsWidget rendering
       - Empty state (Brak danych do wykresu)
       - onMonthClick, onAssetsClick handlers

    IMPORTANT: The InsightsWidget component is NOT being converted in this task — it still uses its own useState/useEffect. It will be converted in Plan 03. Do not change InsightsWidget rendering.
  </action>
   <verify>
    <automated>grep -c "useMonthlySummary\|useAssets\|useInsightsForecast" frontend/src/pages/DashboardPage.tsx && grep -c "import Skeleton from" frontend/src/pages/DashboardPage.tsx && grep -v "^import\|^//" frontend/src/pages/DashboardPage.tsx | grep -c "useMemo"</automated>
  </verify>
  <acceptance_criteria>
    - No `useState` calls for data/loading/error/aiForecast/assets remain
    - No `useEffect` for data fetching remains (only popstate listener still present)
    - No `import { getMonthlySummary, ... } from '../api'` — replaced with hooks imports
    - `import { useMonthlySummary, useAssets, useInsightsForecast } from '../lib/query/hooks'` present
    - Three parallel useQuery hooks at component top
    - Derived state (normalizedData, assets, aiForecast) computed via `useMemo`
    - Loading state uses skeleton layout matching page geometry (D-18)
    - `isPending` used for skeleton, data exists during re-fetch
    - InsightsWidget rendering is NOT changed (Plan 03 handles it)
  </acceptance_criteria>
  <done>DashboardPage converted: no useState/useEffect for data, uses useMonthlySummary/useAssets/useInsightsForecast, derived state via useMemo, skeleton layout on isPending</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Existing 401 redirect in api.ts preserved — both pages use hooks wrapping api.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | MonthlyPage skeleton | mitigate | skeleton uses isPending (no data yet), never isFetching (has data) — prevents stale data display per D-17 |
| T-10-04 | Information Disclosure | DashboardPage derived state race | mitigate | useMemo dependency arrays exactly match query data inputs — no stale partial renders |
</threat_model>

<verification>
1. MonthlyPage.tsx converted: no useState/useEffect for data, uses hooks, skeleton layout
2. DashboardPage.tsx converted: no useState/useEffect for data, uses hooks, skeleton layout
3. Both pages compile without errors: `npx tsc --noEmit` passes
4. Git commit: `feat(phase-10): convert MonthlyPage + DashboardPage to React Query hooks`
</verification>

<success_criteria>
- [ ] MonthlyPage uses useTransactionsList, useOpeningBalance, useCategories, useDeleteTransaction
- [ ] DashboardPage uses useMonthlySummary, useAssets, useInsightsForecast
- [ ] Both pages display skeleton layouts on isPending (not spinners)
- [ ] Both pages have no manual data useState/useEffect patterns
- [ ] Derived state migrated to useMemo
- [ ] Mutations use the hook's mutate() method, not manual api.ts calls
- [ ] `npx tsc --noEmit` passes with no type errors
- [ ] app serves without console errors: `npm run dev:web` starts successfully (verify by running for 5s then killing)
</success_criteria>

<output>
Create `.planning/phases/10-frontend-cache-isolation/10-02-SUMMARY.md` when done
</output>