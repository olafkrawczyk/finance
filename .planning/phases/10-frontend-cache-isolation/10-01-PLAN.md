---
phase: 10-frontend-cache-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - frontend/src/main.tsx
  - frontend/src/lib/query/client.ts
  - frontend/src/lib/query/queryKeys.ts
  - frontend/src/lib/query/provider.tsx
  - frontend/src/lib/query/hooks.ts
  - frontend/src/components/Skeleton.tsx
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-02
must_haves:
  truths:
    - "Every React Query key is prefixed with user.id (FRONTEND-01)"
    - "queryClient.clear() fires on both login and logout (FRONTEND-02)"
    - "Skeleton component renders pulsing gray bars with animate-pulse"
    - "QueryClient wraps the entire app in main.tsx"
    - "All existing api.ts functions are wrapped by React Query hooks"
    - "Every hook uses enabled: !!userId to prevent unauthenticated fetches"
    - "Mutation hooks invalidate queries on success via ['user', userId] broad invalidation"
  artifacts:
    - path: "frontend/src/lib/query/client.ts"
      provides: "QueryClient singleton with staleTime: 30_000, refetchOnWindowFocus: true"
      contains: "export const queryClient"
    - path: "frontend/src/lib/query/queryKeys.ts"
      provides: "Type-safe query key factory with user ID prefix"
      contains: "export const queryKeys"
    - path: "frontend/src/lib/query/provider.tsx"
      provides: "QueryProvider, CacheManager, useUserId hook"
      contains: "export function CacheManager"
    - path: "frontend/src/lib/query/hooks.ts"
      provides: "All useQuery and useMutation hooks wrapping api.ts"
      contains: "useTransactionsList"
    - path: "frontend/src/components/Skeleton.tsx"
      provides: "Reusable pulsing gray bar component"
      contains: "animate-pulse bg-slate-800/50"
    - path: "frontend/src/main.tsx"
      provides: "App wrapped with QueryProvider"
      contains: "QueryProvider"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/lib/query/provider.tsx"
      via: "import { QueryProvider } from './lib/query/provider'"
    - from: "frontend/src/lib/query/provider.tsx"
      to: "frontend/src/lib/auth-client.ts"
      via: "useSession() for session detection"
    - from: "frontend/src/lib/query/hooks.ts"
      to: "frontend/src/api.ts"
      via: "wraps all api.ts functions with useQuery/useMutation"
    - from: "frontend/src/lib/query/hooks.ts"
      to: "frontend/src/lib/query/queryKeys.ts"
      via: "queryKeys.factoryMethod(userId, ...params)"

## Artifacts This Phase Produces

### New files:
- `frontend/src/lib/query/client.ts` — `queryClient` (QueryClient singleton with staleTime=30_000, refetchOnWindowFocus=true, retry=3)
- `frontend/src/lib/query/queryKeys.ts` — `queryKeys` object with nested resource key factories:
  - `queryKeys.user(userId)` → `['user', userId]`
  - `queryKeys.transactions.{all,list,detail}(userId, ...)` 
  - `queryKeys.categories.all(userId)`
  - `queryKeys.accounts.all(userId)`
  - `queryKeys.assets.all(userId)`
  - `queryKeys.summary.{all,monthly}(userId, ...)`
  - `queryKeys.openingBalance.byMonth(userId, ...)`
  - `queryKeys.insights.{all,list,forecast}(userId, ...)`
  - `queryKeys.importStatus(userId, jobId)`
  - `queryKeys.migration.status(userId, jobId)`
- `frontend/src/lib/query/provider.tsx` — `QueryProvider` (component wrapper), `CacheManager` (auth-change clear component), `useUserId` (hook returning session.user.id)
- `frontend/src/lib/query/hooks.ts` — Hook functions:
  - **Query hooks:** `useTransactionsList`, `useMonthlySummary`, `useOpeningBalance`, `useCategories`, `useAccounts`, `useAssets`, `useInsightsList`, `useInsightsForecast`, `useImportStatus`, `useMigrationStatus`, `useTransactionDetail`
  - **Mutation hooks:** `useDeleteTransaction`, `useCreateTransaction`, `useUpdateTransaction`, `useAssignCategory`, `useCreateAsset`, `useUpdateAsset`, `useDeleteAsset`, `useDismissInsight`, `useGenerateInsights`
- `frontend/src/components/Skeleton.tsx` — `Skeleton` (default export component with className prop, animate-pulse, bg-slate-800/50, rounded-lg, aria-hidden)

### Modified files:
- `package.json` — add `@tanstack/react-query@^5.101.0` to dependencies
- `frontend/src/main.tsx` — wrap `<App />` with `<QueryProvider>`
---

<objective>
Create the React Query infrastructure layer: install @tanstack/react-query, build the query key factory, provider with CacheManager, all query/mutation hooks, and the Skeleton component. Wire everything into main.tsx.

Purpose: Establish the per-user cache isolation foundation that all pages will use.
Output: All files in frontend/src/lib/query/, the Skeleton component, and modified main.tsx.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-frontend-cache-isolation/10-CONTEXT.md
@.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md
@.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md
@.planning/phases/10-frontend-cache-isolation/10-UI-SPEC.md
@/home/olafk/finance/frontend/src/main.tsx
@/home/olafk/finance/frontend/src/lib/auth-client.ts
@/home/olafk/finance/frontend/src/api.ts
@/home/olafk/finance/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tanstack/react-query and create client.ts + queryKeys.ts</name>
  <files>
    package.json
    frontend/src/lib/query/client.ts
    frontend/src/lib/query/queryKeys.ts
  </files>
  <read_first>
    @/home/olafk/finance/package.json
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 364-381 for client.ts pattern)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 176-241 for queryKeys.ts pattern)
    @/home/olafk/finance/frontend/src/lib/auth-client.ts
  </read_first>
  <action>
    1. Install @tanstack/react-query v5.101.0 via `npm install @tanstack/react-query@^5.101.0` from project root.
    2. Create `frontend/src/lib/query/` directory.
    3. Create `frontend/src/lib/query/client.ts`:
       - Import `{ QueryClient }` from `@tanstack/react-query`
       - Export `const queryClient = new QueryClient({...})` singleton
       - Set `staleTime: 30_000` (D-03), `refetchOnWindowFocus: true` (D-04), `retry: 3`, `retryDelay` with exponential backoff (Math.min(1000 * 2 ** attemptIndex, 10000))
       - Follow same singleton export convention as `auth-client.ts` (export the instance directly)
    4. Create `frontend/src/lib/query/queryKeys.ts`:
       - Export `const queryKeys` object with nested resource factories
       - Every key starts with `['user', userId]` (D-05)
       - Include all resource groups from RESEARCH.md lines 183-241:
         - user, transactions (all/list/detail), categories (all), accounts (all), assets (all), summary (all/monthly), openingBalance (byMonth), insights (all/list/forecast), importStatus, migration (status)
       - Use `as const` on tuples for type safety
       - params objects use spread `{ ...filters }` to prevent mutation
    5. Verify lint/no syntax errors by running `npx tsc --noEmit` in project root (if tsconfig exists)
  </action>
  <verify>
    <automated>ls frontend/src/lib/query/client.ts frontend/src/lib/query/queryKeys.ts && node -e "const p = require('./package.json'); console.log(p.dependencies['@tanstack/react-query'] ? 'OK' : 'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json has `@tanstack/react-query` in dependencies (not devDependencies)
    - `npm ls @tanstack/react-query` exits 0
    - frontend/src/lib/query/client.ts exists and exports `queryClient`
    - frontend/src/lib/query/queryKeys.ts exists and exports `queryKeys`
    - `queryKeys.user('abc')` returns `['user', 'abc']`
    - `queryKeys.transactions.list('abc', {page: 1})` returns `['user', 'abc', 'transactions', {page: 1}]`
    - client.ts uses same singleton export pattern as auth-client.ts
  </acceptance_criteria>
  <done>Package installed, client.ts exports queryClient with staleTime=30_000, queryKeys.ts exports factory with all resource groups and ['user', userId] prefix</done>
</task>

<task type="auto">
  <name>Task 2: Create provider.tsx with QueryProvider, CacheManager, useUserId</name>
  <files>
    frontend/src/lib/query/provider.tsx
  </files>
  <read_first>
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 244-287 for CacheManager + QueryProvider pattern)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 383-392 for useUserId pattern)
    @/home/olafk/finance/frontend/src/lib/auth-client.ts
    @/home/olafk/finance/frontend/src/components/DismissConfirmDialog.tsx (for component export conventions)
  </read_first>
  <action>
    Create `frontend/src/lib/query/provider.tsx` with three exports:

    1. `useUserId()` custom hook (D-07):
       - Reads `authClient.useSession()` 
       - Returns `session?.user?.id` (can be undefined when not logged in)
       - Must handle the case where session is loading or absent

    2. `CacheManager` component (D-10, D-09):
       - Uses `authClient.useSession()` to detect session transitions
       - Uses `useRef` to track previous session value — prevents effect loop (Pitfall 3 from RESEARCH.md)
       - In `useEffect`: compare `prevSessionRef.current` (previous) with current session
       - On null→session (login) OR session→null (logout): calls `queryClient.clear()`
       - Updates `prevSessionRef.current` to the current session after comparison
       - Returns `null` — no visual output

    3. `QueryProvider` component:
       - Wraps children in `<QueryClientProvider client={queryClient}>`
       - Includes `<CacheManager />` inside the provider
       - Exports as named export

    Follow the exact code from RESEARCH.md lines 255-287 for CacheManager and QueryProvider.
    Follow RESEARCH.md lines 388-392 for useUserId.
    Use relative import for auth-client: `'../auth-client'` (matching project convention — PATTERNS.md line 1053)
    Use relative import for client: `'./client'`
  </action>
  <verify>
    <automated>grep -c "export function CacheManager" frontend/src/lib/query/provider.tsx && grep -c "export function useUserId" frontend/src/lib/query/provider.tsx && grep -c "export function QueryProvider" frontend/src/lib/query/provider.tsx</automated>
  </verify>
  <acceptance_criteria>
    - provider.tsx exports `QueryProvider`, `CacheManager`, and `useUserId`
    - `CacheManager` uses `useRef` for prevSession comparison (not direct object comparison)
    - `CacheManager` calls `queryClient.clear()` on both login AND logout transitions
    - `QueryProvider` renders `QueryClientProvider` > `CacheManager` > children
    - `useUserId` returns `session?.user?.id`
    - Uses relative imports (`'../auth-client'`) not `@/` path alias
  </acceptance_criteria>
  <done>provider.tsx exports QueryProvider, CacheManager (with useRef prevSession pattern), and useUserId; CacheManager calls queryClient.clear() on login/logout; uses relative imports</done>
</task>

<task type="auto">
  <name>Task 3: Create hooks.ts with all query + mutation hooks + Skeleton.tsx + wire main.tsx</name>
  <files>
    frontend/src/lib/query/hooks.ts
    frontend/src/components/Skeleton.tsx
    frontend/src/main.tsx
  </files>
  <read_first>
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 394-523 for hooks.ts patterns)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md (lines 526-541 for Skeleton component)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-UI-SPEC.md (lines 127-141 for Skeleton spec)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 199-331 for hooks.ts target code)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 349-364 for Skeleton target code)
    @/home/olafk/finance/frontend/src/main.tsx
    @/home/olafk/finance/frontend/src/api.ts
    @/home/olafk/finance/frontend/src/components/ProgressBar.tsx (for component export conventions)
  </read_first>
  <action>
    Create `frontend/src/lib/query/hooks.ts` — ALL query and mutation hooks wrapping api.ts functions:

    **Query hooks (useQuery):**
    For each: get userId via useUserId(), build queryKey via queryKeys factory, set queryFn wrapping the api.ts function, set enabled: !!userId.
    - `useTransactionsList(filters?)` → `api.getTransactions(filters)`, key: `queryKeys.transactions.list(userId!, filters)`
    - `useMonthlySummary()` → `api.getMonthlySummary()`, key: `queryKeys.summary.all(userId!)`
    - `useOpeningBalance(year, month)` → `api.getOpeningBalance(year, month)`, key: `queryKeys.openingBalance.byMonth(userId!, year, month)`
    - `useCategories()` → `api.getCategories()`, key: `queryKeys.categories.all(userId!)`
    - `useAccounts()` → `api.getAccounts()`, key: `queryKeys.accounts.all(userId!)`
    - `useAssets()` → `api.getAssets()`, key: `queryKeys.assets.all(userId!)`
    - `useInsightsList(filters?)` → `api.getInsights(filters)`, key: `queryKeys.insights.list(userId!, filters)`
    - `useInsightsForecast()` → `api.getInsightsForecast()`, key: `queryKeys.insights.forecast(userId!)`
    - `useImportStatus(jobId)` → `api.getImportStatus(jobId)`, key: `queryKeys.importStatus(userId!, jobId)`
    - `useMigrationStatus(jobId)` → `api.getMigrationStatus(jobId)`, key: `queryKeys.migration.status(userId!, jobId)`
    - `useTransactionDetail(id)` → `api.getTransaction(id)`, key: `queryKeys.transactions.detail(userId!, id)`

    **Mutation hooks (useMutation with D-15 broad invalidation):**
    For each: get userId, mutationFn wrapping api.ts, onSuccess: `queryClient.invalidateQueries({ queryKey: ['user', userId] })`
    - `useDeleteTransaction()` → `api.deleteTransaction(id)`
    - `useCreateTransaction()` → `api.createTransaction(data)`
    - `useUpdateTransaction()` → `api.updateTransaction(id, data)` — params as `{ id, data }` object
    - `useAssignCategory()` → `api.assignCategory(transactionId, categoryId)` — params as `{ transactionId, categoryId }` object
    - `useCreateAsset()` → `api.createAsset(data)`
    - `useUpdateAsset()` → `api.updateAsset(id, data)` — params as `{ id, data }` object
    - `useDeleteAsset()` → `api.deleteAsset(id)`
    - `useDismissInsight()` → `api.dismissInsight(insightId)` — param as `{ insightId }` object
    - `useGenerateInsights()` → `api.generateInsights()`

    Use these imports: `import { useQuery, useMutation } from '@tanstack/react-query'`, `import * as api from '../../api'`, `import { queryKeys } from './queryKeys'`, `import { useUserId } from './provider'`, `import { queryClient } from './client'`.

    Each mutation hook must follow the pattern from RESEARCH.md lines 491-523.

    Create `frontend/src/components/Skeleton.tsx`:
    - Single `SkeletonProps` interface with optional `className?: string` (default `'h-4 w-full'`)
    - Export default function Skeleton
    - Render `<div className="animate-pulse bg-slate-800/50 rounded-lg ${className}" aria-hidden="true" />`
    - Follow same export conventions as ProgressBar.tsx (see PATTERNS.md lines 339-350)

    Modify `frontend/src/main.tsx`:
    - Add import: `import { QueryProvider } from './lib/query/provider'`
    - Wrap `<App />` with `<QueryProvider>` inside StrictMode (see PATTERNS.md lines 392-407)
    - Keep all existing imports and structure unchanged
  </action>
  <verify>
    <automated>grep -c "export function useTransactionsList" frontend/src/lib/query/hooks.ts && grep -c "export function useCategories" frontend/src/lib/query/hooks.ts && grep -c "export function useDeleteTransaction" frontend/src/lib/query/hooks.ts && grep -c "export default function Skeleton" frontend/src/components/Skeleton.tsx && grep -c "QueryProvider" frontend/src/main.tsx</automated>
  </verify>
  <acceptance_criteria>
    - hooks.ts exports all 11 query hooks and all 9 mutation hooks
    - Every query hook uses `enabled: !!userId` to prevent unauthenticated fetches
    - Every mutation hook uses `invalidateQueries({ queryKey: ['user', userId] })` on success (D-15)
    - Skeleton.tsx has `animate-pulse`, `bg-slate-800/50`, `rounded-lg`, and `aria-hidden="true"`
    - main.tsx wraps `<App />` with `<QueryProvider>`
    - All imports use relative paths (project convention)
    - `npx tsc --noEmit` passes with no errors related to new files
  </acceptance_criteria>
  <done>hooks.ts exports 11 query hooks and 9 mutation hooks, all with enabled:!!userId and D-15 broad invalidation; Skeleton.tsx has animate-pulse/rounded-lg/aria-hidden; main.tsx wraps App with QueryProvider</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Existing 401 redirect in api.ts preserved — React Query wraps, doesn't replace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | CacheManager effect loop | mitigate | useRef prevSession comparison prevents infinite clear() cycle (RESEARCH.md Pitfall 3) |
| T-10-02 | Information Disclosure | Stale cache across sessions | mitigate | queryClient.clear() on both login/logout (D-09) — nuclear clear prevents cross-user data display |
| T-10-SC | Tampering | npm install @tanstack/react-query | mitigate | Package verified by RESEARCH.md Package Legitimacy Audit — [OK] disposition, established package (~4yr, 15M/week downloads) |
</threat_model>

<verification>
1. npm install completed without errors
2. All 4 lib/query/ files created with correct exports
3. Skeleton.tsx created with animate-pulse
4. main.tsx wraps App with QueryProvider
5. tsc --noEmit passes
6. Git commit: `docs(phase-10): add React Query infrastructure — client, provider, hooks, skeleton`
</verification>

<success_criteria>
- [ ] @tanstack/react-query installed in package.json
- [ ] All query keys include ['user', userId] prefix (FRONTEND-01)
- [ ] queryClient.clear() called on login AND logout transitions (FRONTONT-02)
- [ ] All hooks wrap api.ts without modification to api.ts (D-13)
- [ ] All hooks use enabled: !!userId guard
- [ ] All mutations invalidate queries on success via D-15 broad invalidation
- [ ] QueryProvider wraps full app in main.tsx
- [ ] Skeleton component with animate-pulse exported from frontend/src/components/Skeleton.tsx
</success_criteria>

<output>
Create `.planning/phases/10-frontend-cache-isolation/10-01-SUMMARY.md` when done
</output>
