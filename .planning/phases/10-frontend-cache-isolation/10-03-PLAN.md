---
phase: 10-frontend-cache-isolation
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - frontend/src/pages/SummaryPage.tsx
  - frontend/src/pages/InsightsPage.tsx
  - frontend/src/pages/AssetsPage.tsx
  - frontend/src/pages/CategorizePage.tsx
  - frontend/src/components/InsightsWidget.tsx
  - frontend/src/pages/AddTransactionPage.tsx
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-03
must_haves:
  truths:
    - "SummaryPage uses useMonthlySummary hook instead of useState/useEffect"
    - "InsightsPage uses useInsightsList, useDismissInsight, useGenerateInsights hooks"
    - "AssetsPage uses useAssets, useCreateAsset, useUpdateAsset, useDeleteAsset hooks"
    - "CategorizePage uses useTransactionsList, useCategories, useAssignCategory hooks"
    - "InsightsWidget uses useInsightsList with refetchInterval: 60000 instead of manual polling"
    - "AddTransactionPage uses useCategories, useAccounts, useTransactionDetail, useCreateTransaction, useUpdateTransaction hooks"
    - "All pages display skeleton layouts during isPending"
    - "No page manually calls api.ts functions for data fetching or mutations"
  artifacts:
    - path: "frontend/src/pages/SummaryPage.tsx"
      provides: "React Query conversion — single query + skeleton"
      contains: "useMonthlySummary"
    - path: "frontend/src/pages/InsightsPage.tsx"
      provides: "React Query conversion — paginated query + mutations + skeleton"
      contains: "useInsightsList"
    - path: "frontend/src/pages/AssetsPage.tsx"
      provides: "React Query conversion — CRUD queries + mutations + skeleton"
      contains: "useAssets"
    - path: "frontend/src/pages/CategorizePage.tsx"
      provides: "React Query conversion — batch mutation + skeleton"
      contains: "useAssignCategory"
    - path: "frontend/src/components/InsightsWidget.tsx"
      provides: "React Query conversion — polling with refetchInterval + skeleton"
      contains: "refetchInterval"
    - path: "frontend/src/pages/AddTransactionPage.tsx"
      provides: "React Query conversion — form with query hooks for dropdowns + skeleton"
      contains: "useCategories"
  key_links:
    - from: "All pages"
      to: "frontend/src/lib/query/hooks.ts"
      via: "import { useXxx } from '../lib/query/hooks'"
    - from: "All pages"
      to: "frontend/src/components/Skeleton.tsx"
      via: "import Skeleton from '../components/Skeleton'"

## Artifacts This Phase Produces

### Modified files:
- `frontend/src/pages/SummaryPage.tsx` — replaces useState/useEffect with useMonthlySummary, skeleton layout
- `frontend/src/pages/InsightsPage.tsx` — replaces manual fetchInsightsList/fetchCounts with useInsightsList, useDismissInsight, useGenerateInsights
- `frontend/src/pages/AssetsPage.tsx` — replaces manual fetchAssets/create/update/delete with useAssets, useCreateAsset, useUpdateAsset, useDeleteAsset
- `frontend/src/pages/CategorizePage.tsx` — replaces manual fetchData/assignCategory with useTransactionsList, useCategories, useAssignCategory
- `frontend/src/components/InsightsWidget.tsx` — replaces manual fetchLatestInsights + setInterval with useInsightsList + refetchInterval
- `frontend/src/pages/AddTransactionPage.tsx` — replaces manual Promise.all for categories/accounts with useCategories, useAccounts, useTransactionDetail

### New symbols none — all files are modifications of existing pages/components, no new exports
---

<objective>
Convert all remaining pages from useState/useEffect data fetching to React Query hooks: SummaryPage, InsightsPage, AssetsPage, CategorizePage, InsightsWidget, and AddTransactionPage.

Purpose: Complete the frontend cache isolation migration — every page uses per-user scoped React Query hooks, shows skeleton layouts on initial load, and has no manual data state management.
Output: All 6 page/component files converted to React Query.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-frontend-cache-isolation/10-CONTEXT.md
@.planning/phases/10-frontend-cache-isolation/10-RESEARCH.md
@.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md
@.planning/phases/10-frontend-cache-isolation/10-UI-SPEC.md
@/home/olafk/finance/frontend/src/pages/SummaryPage.tsx
@/home/olafk/finance/frontend/src/pages/InsightsPage.tsx
@/home/olafk/finance/frontend/src/pages/AssetsPage.tsx
@/home/olafk/finance/frontend/src/pages/CategorizePage.tsx
@/home/olafk/finance/frontend/src/components/InsightsWidget.tsx
@/home/olafk/finance/frontend/src/pages/AddTransactionPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert SummaryPage + CategorizePage + InsightsWidget to React Query</name>
  <files>
    frontend/src/pages/SummaryPage.tsx
    frontend/src/pages/CategorizePage.tsx
    frontend/src/components/InsightsWidget.tsx
  </files>
  <read_first>
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 647-707 for SummaryPage pattern)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 888-941 for InsightsWidget pattern)
    @/home/olafk/finance/frontend/src/pages/SummaryPage.tsx
    @/home/olafk/finance/frontend/src/pages/CategorizePage.tsx
    @/home/olafk/finance/frontend/src/components/InsightsWidget.tsx
    @/home/olafk/finance/frontend/src/lib/query/hooks.ts
    @/home/olafk/finance/frontend/src/components/Skeleton.tsx
  </read_first>
  <action>
    Convert three files following the exact patterns in PATTERNS.md:

    **SummaryPage.tsx:** Simplest conversion — single query pattern.
    1. Imports: Remove `useState, useEffect` from React import and `getMonthlySummary` from '../api'. Add `import { useMonthlySummary } from '../lib/query/hooks'` and `import Skeleton from '../components/Skeleton'`.
    2. Replace: `const [data, setData] = useState(...)` + `const [error, setError] = useState(...)` + `const [loading, setLoading] = useState(true)` + `useEffect(...)` with:
       `const { data, isPending, error } = useMonthlySummary();`
    3. Move normalization to useMemo: `const normalizedData = useMemo(() => { if (!data) return null; return data.map(...) }, [data])`
    4. Replace spinner loading block with skeleton:
       ```
       if (isPending) {
         return (
           <div className="space-y-6 max-w-6xl mx-auto w-full px-4" aria-busy="true">
             <div className="space-y-2">
               <Skeleton className="h-8 w-48" />
               <Skeleton className="h-4 w-64" />
             </div>
             <Skeleton className="h-96 w-full rounded-2xl" />
           </div>
         );
       }
       ```
    5. Error state reads `error.message` from query hook (same card display)
    6. Pass `normalizedData` to SummaryTable instead of `data`

    **CategorizePage.tsx:** Medium complexity — Promise.all + batch mutation.
    1. Imports: Remove `useState, useEffect` from React import, remove `getTransactions, getCategories, assignCategory` from api import. Add `import { useTransactionsList, useCategories, useAssignCategory } from '../lib/query/hooks'` and `import Skeleton from '../components/Skeleton'`.
    2. Replace state: delete `transactions`, `categories`, `loading`, `error` useState. Keep `selectedIds` (Set), `targetCategory`, `assigning`, `success` (UI state).
    3. Replace `useEffect` + `fetchData`:
       ```
       const { data: txResult, isPending } = useTransactionsList({ per_page: 500, uncategorized: true });
       const { data: categoryRows } = useCategories();
       const assignMutation = useAssignCategory();
       ```
    4. Derive `transactions` via useMemo (same uncategorized normalization from txResult.data)
    5. Derive `categories` via useMemo from `categoryRows`
    6. Replace `handleAssign` async function: remove manual fetch, use `assignMutation.mutate()` for each category — but since it's batch, call multiple times (each triggers invalidation). OR use `Promise.all(ids.map(id => assignMutation.mutateAsync({ transactionId: id, categoryId: targetCategory })))`.
    7. After assignment succeeds: remove categorized transactions from local state via `setTransactions(prev => prev.filter(...))` — keep optimistic removal since invalidation happens in background.
    8. Replace spinner loading block with skeleton:
       ```
       if (isPending) {
         return (
           <div className="space-y-6 max-w-6xl mx-auto w-full px-4" aria-busy="true">
             <div className="space-y-2">
               <Skeleton className="h-8 w-48" />
               <Skeleton className="h-4 w-64" />
             </div>
             <Skeleton className="h-12 w-full rounded-lg" />
             <Skeleton className="h-96 w-full rounded-2xl" />
           </div>
         );
       }
       ```

    **InsightsWidget.tsx:** Polling conversion — manual setInterval → refetchInterval.
    1. Imports: Remove `useState, useEffect` from React import, remove `getInsights` from api import. Add `import { useInsightsList } from '../lib/query/hooks'` and `import Skeleton from '../components/Skeleton'`.
    2. Replace: `const [insights, setInsights] = useState(...)` + `const [loading, setLoading] = useState(true)` + `const [error, setError] = useState(null)` + `const fetchLatestInsights = ...` + `useEffect(() => { fetchLatestInsights(); const id = setInterval(...) })` with:
       ```
       const { data, isPending, error } = useInsightsList(
         { per_page: 3, dismissed: false },
       );
       ```
       Note: The hook is already called with filters as first arg. To set `refetchInterval: 60000`, pass it as part of the options to the useInsightsList. Since the hook doesn't accept options, import `queryClient` or use the hook's returned refetch. Actually the simplest approach: the `useInsightsList` hook can accept a second `options` parameter — modify the hook call to pass `refetchInterval: 60000` via the query options. Use `useInsightsList({ per_page: 3, dismissed: false })` and add a `refetchInterval` option by spreading into the useQuery config. Reference the pattern from PATTERNS.md lines 919-941.
    3. Replace existing skeleton cards (lines 49-77) with Skeleton component usage for consistency:
       ```
       if (isPending) {
         return (
           <div className="mb-8">
             <h3 className="text-sm font-semibold text-slate-400 uppercase tracking-wider mb-3">Najnowsze analizy</h3>
             <div className="flex gap-4 overflow-x-auto pb-2">
               {[1, 2, 3].map((n) => (
                 <div key={n} className="min-w-[250px] flex-shrink-0">
                   <Skeleton className="h-[120px] w-full rounded-xl" />
                 </div>
               ))}
             </div>
           </div>
         );
       }
       ```
    4. `insights` derived from `data?.data ?? []`
    5. Everything else unchanged (rendering, navigation, error state)

    Follow relative import conventions: `'../lib/query/hooks'`, `'../components/Skeleton'` from pages; `'../../lib/query/hooks'` from components.
  </action>
  <verify>
    <automated>grep -c "useMonthlySummary" frontend/src/pages/SummaryPage.tsx && grep -c "useTransactionsList\|useCategories\|useAssignCategory" frontend/src/pages/CategorizePage.tsx && grep -c "useInsightsList" frontend/src/components/InsightsWidget.tsx && grep -c "import Skeleton from" frontend/src/pages/SummaryPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SummaryPage: no useState/useEffect for data, uses useMonthlySummary, skeleton on isPending
    - CategorizePage: no useState/useEffect for data fetching, uses query hooks + mutation hook, skeleton on isPending
    - InsightsWidget: no useState/useEffect for data, no setInterval, uses useInsightsList with refetchInterval: 60000, uses Skeleton component
    - All three files have `import { useXxx } from '../lib/query/hooks'`
    - All three files have `import Skeleton from '../components/Skeleton'`
  </acceptance_criteria>
  <done>SummaryPage, CategorizePage, InsightsWidget converted: no useState/useEffect for data, uses query hooks, skeleton on isPending; InsightsWidget uses refetchInterval instead of setInterval</done>
</task>

<task type="auto">
  <name>Task 2: Convert InsightsPage + AssetsPage + AddTransactionPage to React Query</name>
  <files>
    frontend/src/pages/InsightsPage.tsx
    frontend/src/pages/AssetsPage.tsx
    frontend/src/pages/AddTransactionPage.tsx
  </files>
  <read_first>
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 711-813 for InsightsPage pattern)
    @/home/olafk/finance/.planning/phases/10-frontend-cache-isolation/10-PATTERNS.md (lines 820-882 for AssetsPage pattern)
    @/home/olafk/finance/frontend/src/pages/InsightsPage.tsx
    @/home/olafk/finance/frontend/src/pages/AssetsPage.tsx
    @/home/olafk/finance/frontend/src/pages/AddTransactionPage.tsx
    @/home/olafk/finance/frontend/src/lib/query/hooks.ts
    @/home/olafk/finance/frontend/src/components/Skeleton.tsx
  </read_first>
  <action>
    Convert three files following the patterns in PATTERNS.md:

    **InsightsPage.tsx (lines 711-813 in PATTERNS.md):**
    1. Imports: Remove `useState, useEffect` from React import, remove `getInsights, dismissInsight, generateInsights` from api import. Add `import { useInsightsList } from '../lib/query/hooks'`, `import { queryClient } from '../lib/query/client'`, `import { useMutation } from '@tanstack/react-query'`, `import * as api from '../api'` for the mutations (since hooks.ts may not have useUserId inside the mutation). Actually simpler: use `useMutation` directly with `queryClient.invalidateQueries`.
    2. Replace: Delete `insights useState`, `loading`, `error`, `counts`, `fetchInsightsList`, `fetchCounts`, both `useEffect` blocks. Add:
       ```
       const { data, isPending, error } = useInsightsList({
         type: activeType !== 'all' ? activeType : undefined,
         dismissed: false,
         page,
         per_page: 20,
       });
       const insights = data?.data ?? [];
       const totalCount = data?.meta?.total ?? 0;
       ```
    3. Mutations: Replace `handleConfirmDismiss` with direct useMutation wrapping api.dismissInsight. Replace manual `setInsights(prev => prev.filter(...))` + `fetchCounts()` with `queryClient.invalidateQueries({ queryKey: ['user', userId] })`. Same for `handleGenerate`.
    4. Keep: `activeType`, `page`, `dismissTarget`, `generating` state, `successMsg` state, perPage constant.
    5. Pagination stays the same: `setActiveType` + `setPage(1)` in handleTypeChange — re-fetch happens automatically because query key changes.
    6. Loading: Replace spinner with skeleton layout (tabs row + 2 insight card skeletons matching UI-SPEC.md). `if (isPending) { return (... skeleton ...) }`
    7. `totalPages` derived from `Math.ceil(totalCount / 20)`

    **AssetsPage.tsx (lines 820-882 in PATTERNS.md):**
    1. Imports: Remove `useState, useEffect` from React import, remove `getAssets, createAsset, updateAsset, deleteAsset` from api import. Add `import { useAssets } from '../lib/query/hooks'`, `import { useMutation } from '@tanstack/react-query'`, `import * as api from '../api'`, `import { queryClient } from '../lib/query/client'`, `import Skeleton from '../components/Skeleton'`.
    2. Replace: Delete `assets useState`, `loading`, `error`, `success`, `fetchAssets`, `useEffect`. Add:
       ```
       const { data: assetsData, isPending } = useAssets();
       const assets = useMemo(() => (assetsData ?? []).map(a => ({...})), [assetsData]);
       ```
       NOTE: Since the page accesses `assets` as a mapped array, derive via useMemo.
    3. Mutations: Create three individual useMutations wrapping api.createAsset, api.updateAsset, api.deleteAsset — each with `onSuccess: () => queryClient.invalidateQueries({ queryKey: ['user', userId] })`. Keep `handleAddAsset`, `handleSaveEdit`, `handleDeleteAsset` functions but replace direct api calls with `mutation.mutateAsync(...)` or `mutation.mutate(...)`.
    4. Keep: All form UI state (`newName`, `newValue`, `submitting`, editing state) — these are local UI concerns.
    5. Loading: Replace spinner with skeleton:
       ```
       if (isPending) {
         return (
           <div className="space-y-6 max-w-6xl mx-auto w-full px-4" aria-busy="true">
             <div className="space-y-2">
               <Skeleton className="h-8 w-64" />
               <Skeleton className="h-4 w-48" />
             </div>
             <Skeleton className="h-96 w-full rounded-2xl" />
           </div>
         );
       }
       ```

    **AddTransactionPage.tsx:**
    1. Imports: Remove `useState, useEffect` from React import, remove `getCategories, getAccounts, createTransaction, getTransaction, updateTransaction` from api import. Add `import { useCategories, useAccounts } from '../lib/query/hooks'`, `import { useMutation } from '@tanstack/react-query'`, `import * as api from '../api'`, `import { queryClient } from '../lib/query/client'`, `import Skeleton from '../components/Skeleton'`. Keep `useState` for form fields (local UI state).
    2. Replace first useEffect (load categories + accounts): 
       ```
       const { data: categoryRows, isPending: catLoading } = useCategories();
       const { data: accountRows, isPending: accLoading } = useAccounts();
       ```
    3. For edit mode (second useEffect for getTransaction): Since the transaction detail is only needed once on mount and doesn't need caching/freshness, use the existing approach — but wrap it in a useQuery for consistency. Actually, use `useTransactionDetail(transactionId!)` with `enabled: !!transactionId`. This way the detail fetches when transactionId is available.
       ```
       const { data: editTx, isPending: txLoading } = useTransactionDetail(transactionId!);
       ```
    4. Replace `handleSubmit` to use mutations instead of direct api calls. Create:
       ```
       const createMutation = useMutation({
         mutationFn: (data) => api.createTransaction(data),
         onSuccess: () => { ... setSuccess(true) ... }
       });
       const updateMutation = useMutation({
         mutationFn: ({ id, data }) => api.updateTransaction(id, data),
         onSuccess: () => { ... }
       });
       ```
    5. Keep: All form state (`categoryId`, `amount`, `description`, `date`, `type`, `submitting`, `error`, `success`, `loading` for edit mode).
    6. Derive categories and accounts from hooks (they load in parallel). Set `categoryId` default via useEffect (when categories load and none selected yet).
    7. Loading (edit mode): `if (txLoading || catLoading) { return (... skeleton ...) }` — only in edit mode when transactionId is present.
    8. Keep the rest of the form rendering identical.

    Follow relative import conventions: `'../lib/query/hooks'`, `'../lib/query/client'`, `'../components/Skeleton'`.
  </action>
  <verify>
    <automated>grep -c "useInsightsList" frontend/src/pages/InsightsPage.tsx && grep -c "useAssets" frontend/src/pages/AssetsPage.tsx && grep -c "useCategories\|useAccounts\|useTransactionDetail" frontend/src/pages/AddTransactionPage.tsx && grep -c "import Skeleton from" frontend/src/pages/InsightsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - InsightsPage: no manual fetchInsightsList/fetchCounts, uses useInsightsList with pagination, mutations via useMutation with invalidation, skeleton on isPending
    - AssetsPage: no manual fetchAssets, uses useAssets + useMutation for CRUD, skeleton on isPending, form UI state preserved unchanged
    - AddTransactionPage: no manual getCategories/getAccounts/getTransaction calls, uses query hooks for data + mutations for submit, skeleton in edit mode
    - All three have `import { useXxx } from '../lib/query/hooks'` and `import Skeleton from '../components/Skeleton'`
    - No `import { getXxx } from '../api'` remains in any of the three files
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>InsightsPage, AssetsPage, AddTransactionPage converted: no manual data fetching, uses query hooks + mutations, skeleton on isPending, form UI state preserved in AddTransactionPage</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Existing 401 redirect in api.ts preserved — all pages use hooks wrapping api.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information Disclosure | InsightsWidget polling | mitigate | refetchInterval: 60000 in React Query replaces setInterval — cleanup handled automatically by React Query |
| T-10-06 | Information Disclosure | AssetsPage optimistic removal | mitigate | D-15 invalidation after mutation ensures cache consistency — no stale inline state |
| T-10-07 | Information Disclosure | AddTransactionPage edit mode | mitigate | useTransactionDetail uses `enabled: !!transactionId` guard — no premature fetches |
</threat_model>

<verification>
1. All 6 files converted: SummaryPage, InsightsPage, AssetsPage, CategorizePage, InsightsWidget, AddTransactionPage
2. No file imports from `'../api'` for data fetching — all use React Query hooks
3. `npx tsc --noEmit` passes with no type errors
4. `npm run dev:web` starts without errors (verify by running for 5s then killing)
5. Git commit: `feat(phase-10): convert remaining pages to React Query hooks`
</verification>

<success_criteria>
- [ ] Every page in the app uses React Query hooks for server data (no useState/useEffect data fetching)
- [ ] Every query key includes the per-user prefix via queryKeys.ts factory (FRONTEND-01)
- [ ] Every page displays skeleton layouts on isPending (not spinners) (FRONTEND-03)
- [ ] No page imports api.ts functions directly for data fetching or mutations
- [ ] All mutations use D-15 broad invalidation pattern
- [ ] InsightsWidget uses refetchInterval instead of setInterval
- [ ] All 6 files compile without errors
- [ ] App serves without console errors
</success_criteria>

<output>
Create `.planning/phases/10-frontend-cache-isolation/10-03-SUMMARY.md` when done
</output>
