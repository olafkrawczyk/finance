---
phase: 02-ingestion-auth
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
files_modified:
  - package.json
  - vite.config.ts
  - tailwind.config.js
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/index.css
  - frontend/src/App.tsx
  - frontend/src/components/ImportUpload.tsx
  - frontend/src/components/ImportStatus.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can open the import page, select an account, choose bank format, and upload a CSV file"
    - "Upload form submits multipart data to POST /import and redirects to status page with job_id"
    - "Status page polls GET /import/:jobId every 2 seconds and shows real-time progress"
    - "Status page displays status badge, progress bar, processed/total count, and error list"
    - "Polling stops when job reaches 'completed' or 'failed'"
  artifacts:
    - path: "frontend/src/components/ImportUpload.tsx"
      provides: "CSV upload form with account selector and bank format selector"
      exports: ["ImportUpload"]
    - path: "frontend/src/components/ImportStatus.tsx"
      provides: "Import job status with polling, progress, and errors"
      exports: ["ImportStatus"]
    - path: "frontend/src/App.tsx"
      provides: "React router for /import and /import/:jobId"
      exports: ["App"]
    - path: "vite.config.ts"
      provides: "Vite build config with React plugin"
      exports: ["default"]
  key_links:
    - from: "frontend/src/components/ImportUpload.tsx"
      to: "POST /import"
      via: "fetch with FormData containing csv File and account_id"
      pattern: "fetch.*POST.*import"
    - from: "frontend/src/components/ImportStatus.tsx"
      to: "GET /import/:job_id"
      via: "setInterval polling every 2000ms"
      pattern: "setInterval.*2000"
---

<objective>
Set up a minimal React + Vite + Tailwind CSS frontend and build the import-specific UI: an upload form for CSV files with account/bank-format selectors, and a real-time status page that polls the import job endpoint.

Purpose: Provides the user-facing interface for Phase 2's import pipeline. The backend API and worker are already functional; this plan connects them to a usable UI.
Output: Working dev server, import upload page, and import status page with live progress updates.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ingestion-auth/02-UI-SPEC.md
@.planning/phases/02-ingestion-auth/02-RESEARCH.md

# Prior plan outputs
@.planning/phases/02-ingestion-auth/02-01-PLAN.md (auth endpoints)
@.planning/phases/02-ingestion-auth/02-03-PLAN.md (import API)

# Existing project state
@package.json
@index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vite + React + Tailwind frontend build</name>
  <files>package.json, vite.config.ts, tailwind.config.js, frontend/index.html, frontend/src/main.tsx, frontend/src/index.css</files>
  <read_first>
    @package.json (observe existing scripts and dependencies)
    @.planning/phases/02-ingestion-auth/02-UI-SPEC.md Design System section
  </read_first>
  <action>
    Add to root `package.json` dependencies: `react`, `react-dom`, `react-router-dom`. Add devDependencies: `vite`, `@vitejs/plugin-react`, `tailwindcss`, `postcss`, `autoprefixer`, `@types/react`, `@types/react-dom`. Add scripts: `"dev:frontend": "vite"`, `"build:frontend": "tsc && vite build"`, `"preview:frontend": "vite preview"`. Run `bun install` to install new packages. Create `vite.config.ts` at root exporting a config with `@vitejs/plugin-react()` in plugins, `root: 'frontend'`, and `build.outDir: '../dist/frontend'`. Create `tailwind.config.js` exporting `{ content: ['./frontend/index.html', './frontend/src/**/*.{js,ts,jsx,tsx}'], theme: { extend: {} }, plugins: [] }`. Create `postcss.config.js` exporting `{ plugins: { tailwindcss: {}, autoprefixer: {} } }`. Create `frontend/index.html` with standard HTML5 boilerplate, `<div id="root"></div>`, and `<script type="module" src="/src/main.tsx"></script>`. Create `frontend/src/main.tsx` importing `React`, `ReactDOM`, `./App`, and `./index.css`, then mounting `<App />` into `document.getElementById('root')` with `ReactDOM.createRoot`. Create `frontend/src/index.css` with `@tailwind base; @tailwind components; @tailwind utilities;` and a body background of `#f9fafb` (gray-50 per UI-SPEC). Create `frontend/src/App.tsx` as a React component using `BrowserRouter` from `react-router-dom` with routes for `/import` (ImportUpload) and `/import/:jobId` (ImportStatus). Export `App` as default.
  </action>
  <verify>
    <automated>grep -c "vite" package.json && grep -c "react" package.json && grep -c "tailwindcss" package.json && ls frontend/index.html && ls frontend/src/main.tsx</automated>
  </verify>
  <done>`package.json` has react, vite, tailwindcss, react-router-dom; `vite.config.ts` exists with React plugin and root: 'frontend'; `frontend/index.html` and `frontend/src/main.tsx` exist; `bun run dev:frontend` starts Vite dev server without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create ImportUpload component</name>
  <files>frontend/src/components/ImportUpload.tsx</files>
  <read_first>
    @.planning/phases/02-ingestion-auth/02-UI-SPEC.md Import Upload Screen section
    @src/interface-adapters/api/import.ts (observe POST /import API contract)
    @src/interface-adapters/api/reference.ts (observe GET /accounts response)
  </read_first>
  <action>
    Create `frontend/src/components/ImportUpload.tsx` as a React functional component. On mount, fetch `GET /accounts` and populate an account `<select>` dropdown. Include a bank format `<select>` with options ING and IPKO (default ING). Include a file `<input type="file" accept=".csv">` styled as a drop zone with dashed border (`border-2 border-dashed border-gray-300 rounded-lg p-8 text-center`) per UI-SPEC. Show selected filename below the drop zone. Include a submit button (`bg-blue-500 text-white px-4 py-2 rounded`) disabled until an account is selected and a file is chosen. On submit: create a `FormData`, append the `File` as `'csv'` and `account_id` as `'account_id'`, and `bank_format` as `'bank_format'`. Call `fetch('/import', { method: 'POST', body: formData, credentials: 'include' })`. If response is 202, read `data.job_id` from JSON and navigate to `/import/${job_id}` using `useNavigate` from `react-router-dom`. If 401, show error "Please sign in". If 400, show `error.message`. Use Tailwind classes for all styling: page padding `p-6`, card padding `p-4`, gap `gap-4`, headings `text-xl font-semibold text-gray-900`, labels `text-sm font-medium text-gray-700`. Add `aria-label="Upload CSV file"` to the file input. Add `role="status"` to the submit button spinner.
  </action>
  <verify>
    <automated>grep -c "ImportUpload" frontend/src/components/ImportUpload.tsx && grep -c "fetch.*POST.*import" frontend/src/components/ImportUpload.tsx && grep -c "useNavigate" frontend/src/components/ImportUpload.tsx && grep -c "aria-label" frontend/src/components/ImportUpload.tsx</automated>
  </verify>
  <done>`frontend/src/components/ImportUpload.tsx` renders account selector, bank format selector, file drop zone, and submit button; calls `POST /import` with FormData; navigates to `/import/:jobId` on success.</done>
</task>

<task type="auto">
  <name>Task 3: Create ImportStatus component and wire routes</name>
  <files>frontend/src/components/ImportStatus.tsx, frontend/src/App.tsx</files>
  <read_first>
    @.planning/phases/02-ingestion-auth/02-UI-SPEC.md Import Status Screen section
    @src/interface-adapters/api/import.ts (observe GET /import/:job_id response shape)
    @frontend/src/components/ImportUpload.tsx
  </read_first>
  <action>
    Create `frontend/src/components/ImportStatus.tsx` as a React functional component. Use `useParams` from `react-router-dom` to read `jobId`. Use `useEffect` with `setInterval` (2000ms) to poll `GET /import/${jobId}` with `credentials: 'include'`. Stop the interval when `data.status` is `'completed'` or `'failed'`, or after 5 minutes (150 polls × 2s = 300s). Store response in `useState`. Display: job ID in mono font (`font-mono text-xs`), status badge (`role="status"`, color-coded: yellow for pending, blue for processing, green for completed, red for failed), progress bar (`role="progressbar"`, `aria-valuenow`, `aria-valuemin="0"`, `aria-valuemax` set to `total_rows || 0`, width percentage computed as `processed / total_rows * 100`), processed count (`Processed: {processed} / {total_rows}`), and an error list section (collapsible, `role="alert"`, `aria-live="assertive"`, shows each error string). Include a "Back to Imports" link using `useNavigate`. Style per UI-SPEC: page padding `p-6`, card `bg-white rounded-lg shadow p-4`, progress bar container `bg-gray-200 rounded-full h-4`, fill `bg-blue-500 h-4 rounded-full transition-all`. Update `frontend/src/App.tsx` to import `ImportUpload` and `ImportStatus` and render them at `/import` and `/import/:jobId` respectively inside `BrowserRouter`.
  </action>
  <verify>
    <automated>grep -c "ImportStatus" frontend/src/components/ImportStatus.tsx && grep -c "setInterval" frontend/src/components/ImportStatus.tsx && grep -c "progressbar" frontend/src/components/ImportStatus.tsx && grep -c "ImportUpload" frontend/src/App.tsx && grep -c "ImportStatus" frontend/src/App.tsx</automated>
  </verify>
  <done>`frontend/src/components/ImportStatus.tsx` polls `/import/:jobId` every 2 seconds, renders status badge, progress bar, processed/total, and error list; `frontend/src/App.tsx` routes `/import` to `ImportUpload` and `/import/:jobId` to `ImportStatus`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Frontend → API | Untrusted user input crosses from browser through React UI to backend API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Spoofing | Auth bypass via direct API call | accept | Backend already validates auth (Plan 01); UI is just a consumer |
| T-02-21 | Tampering | File upload via UI | accept | Same protections as API endpoint (Plan 03); UI sends same FormData |
| T-02-22 | Information Disclosure | Job errors in UI | accept | Errors contain only row-level parsing issues; no sensitive data |
| T-02-23 | Denial of Service | Polling continues after tab close | accept | `useEffect` cleanup stops interval on unmount; max 5 minute timeout |
</threat_model>

<verification>
- `bun run dev:frontend` starts Vite dev server on default port (5173)
- `bun run index.ts` starts backend on port 3000
- Navigating to `http://localhost:5173/import` shows upload form with account dropdown and file picker
- Uploading a CSV redirects to status page showing job ID and "pending" or "processing" badge
- Status page updates processed count as worker processes batches
- Polling stops when status becomes "completed"
- Status colors match UI-SPEC (yellow pending, blue processing, green completed, red failed)
</verification>

<success_criteria>
- Vite + React + Tailwind CSS build tooling is configured and runs
- `frontend/src/components/ImportUpload.tsx` renders account selector, bank format selector, file drop zone, and submit button
- Upload submits multipart FormData to `POST /import` and redirects to `/import/:jobId`
- `frontend/src/components/ImportStatus.tsx` polls `GET /import/:jobId` every 2 seconds
- Progress bar shows percentage based on `processed / total_rows`
- Error list displays row-level parsing errors
- Polling stops automatically when job is completed or failed
- All UI elements have proper accessibility attributes (aria-label, role, aria-live)
- Styling follows UI-SPEC color system and spacing guidelines
</success_criteria>

## Artifacts this phase produces

### React Components
- `frontend/src/components/ImportUpload.tsx`: account selector, bank format selector, file drop zone, submit handler
- `frontend/src/components/ImportStatus.tsx`: job status badge, progress bar, processed count, error list, polling logic
- `frontend/src/App.tsx`: BrowserRouter with /import and /import/:jobId routes

### Build Configuration
- `vite.config.ts`: Vite config with @vitejs/plugin-react, root: 'frontend'
- `tailwind.config.js`: Tailwind content paths for frontend/src
- `postcss.config.js`: PostCSS with tailwindcss and autoprefixer
- `frontend/index.html`: HTML entry point with div#root
- `frontend/src/main.tsx`: React mount point
- `frontend/src/index.css`: Tailwind directives + base gray-50 background

### Package Changes
- `package.json`: added react, react-dom, react-router-dom, vite, @vitejs/plugin-react, tailwindcss, postcss, autoprefixer, @types/react, @types/react-dom
- New scripts: dev:frontend, build:frontend, preview:frontend

## PLAN COMPLETE
