---
phase: 01-foundation-core-ledger-db
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/infrastructure/db/schema.sql, src/core/ledger/entities.ts, src/application/schemas/ledger.ts]
autonomous: true
requirements: [REQ-1.1, REQ-1.2, REQ-T1.2]

must_haves:
  truths:
    - "Database schema includes transactions and ledger_entries tables"
    - "Zero-sum constraint trigger prevents unbalanced commits"
    - "Zod schemas validate transaction structure and numeric precision"
  artifacts:
    - path: "src/infrastructure/db/schema.sql"
      provides: "DDL for ledger tables and triggers"
    - path: "src/core/ledger/entities.ts"
      provides: "Domain types for the ledger"
  key_links:
    - from: "src/application/schemas/ledger.ts"
      to: "src/core/ledger/entities.ts"
      via: "Type mapping"
---

<objective>
Implement the immutable ledger database schema, including the double-entry zero-sum constraint trigger and domain entities.

Purpose: Ensure data integrity at the database level.
Output: SQL schema and TypeScript entities for the ledger.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-1/RESEARCH.md
@.planning/phases/01-foundation-core-ledger-db/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Ledger Schema and Trigger</name>
  <files>src/infrastructure/db/schema.sql</files>
  <action>
    Create the SQL schema file with:
    1. `transactions` table: `id` (UUID), `description` (TEXT), `created_at` (TIMESTAMP).
    2. `ledger_entries` table: `id` (UUID), `transaction_id` (UUID, FK), `account_id` (TEXT), `amount` (NUMERIC(19, 4)), `currency` (TEXT).
    3. The `enforce_transaction_balance` function and `trigger_check_balance` constraint trigger as defined in RESEARCH.md.
    Apply the schema to the database using the Bun/postgres.js client (either via a script or manual execution).
  </action>
  <verify>
    <automated>psql $DB_URL -f src/infrastructure/db/schema.sql && psql $DB_URL -c "\d ledger_entries"</automated>
  </verify>
  <done>Database tables and triggers are created.</done>
</task>

<task type="auto">
  <name>Task 2: Define Ledger Entities and Zod Schemas</name>
  <files>src/core/ledger/entities.ts, src/application/schemas/ledger.ts</files>
  <action>
    Define TypeScript types for `Transaction` and `LedgerEntry` in `src/core/ledger/entities.ts`.
    Create Zod validation schemas in `src/application/schemas/ledger.ts`:
    - `TransactionSchema`: Validates the header and an array of at least two entries.
    - `EntrySchema`: Ensures `amount` is a string (to be parsed as numeric) or a number with proper precision.
    Ensure the sum of entries in the Zod schema also validates to zero as a first-line check.
  </action>
  <verify>
    <automated>bun test tests/schemas.test.ts</automated>
  </verify>
  <done>Domain types and validation schemas are ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Domain -> DB | Database triggers act as the final integrity gate. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Ledger Trigger | mitigate | The trigger is defined as `DEFERRABLE INITIALLY DEFERRED` to allow multi-row inserts within a transaction while ensuring final balance. |
</threat_model>

<verification>
Manual test: Attempt to insert a non-zero-sum transaction directly via SQL; it must fail on COMMIT.
</automated>
</verification>

<success_criteria>
- Schema applies without errors.
- Trigger prevents unbalanced transaction commits.
- Zod schemas correctly validate balanced inputs and reject unbalanced ones.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-core-ledger-db/01-02-SUMMARY.md` when done
</output>
