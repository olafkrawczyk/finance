---
phase: 01-foundation-core-ledger-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/index.ts, src/infrastructure/db/client.ts, src/infrastructure/db/health.ts]
autonomous: true
requirements: [REQ-T1.1, REQ-T1.3]

must_haves:
  truths:
    - "Bun + Hono backend is initialized and responds to health checks"
    - "Backend can connect to Postgres and verify PGMQ extension presence"
  artifacts:
    - path: "src/index.ts"
      provides: "Entry point for Hono server"
    - path: "src/infrastructure/db/client.ts"
      provides: "Postgres.js client instance"
    - path: "package.json"
      provides: "Project dependencies and scripts"
  key_links:
    - from: "src/index.ts"
      to: "src/infrastructure/db/client.ts"
      via: "dependency injection or import"
---

<objective>
Initialize the Bun + Hono backend project and establish a verified connection to the Postgres database with PGMQ.

Purpose: Create the "Walking Skeleton" for the application.
Output: Working backend skeleton with DB connectivity.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/phase-1/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Bun Project and Structure</name>
  <files>package.json, src/index.ts, tsconfig.json</files>
  <action>
    Initialize a new Bun project. Install core dependencies: `hono`, `zod`, `postgres`. 
    Setup the directory structure as defined in RESEARCH.md:
    - `src/core/`
    - `src/application/`
    - `src/infrastructure/`
    - `src/interface-adapters/`
    Create a basic Hono server in `src/index.ts` with a `/health` endpoint that returns 200 OK.
  </action>
  <verify>
    <automated>bun run src/index.ts & sleep 2 && curl -f http://localhost:3000/health && kill $!</automated>
  </verify>
  <done>Hono server is running and responds to /health.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Postgres & PGMQ Health Check</name>
  <files>src/infrastructure/db/client.ts, src/infrastructure/db/health.ts</files>
  <action>
    Configure the `postgres.js` client in `src/infrastructure/db/client.ts` using environment variables for connection (DB_URL).
    Create a health check utility in `src/infrastructure/db/health.ts` that:
    1. Executes `SELECT 1` to verify connectivity.
    2. Executes `SELECT extname FROM pg_extension WHERE extname = 'pgmq'` to verify the PGMQ extension is installed.
    Update `src/index.ts` to include a `/health/db` endpoint that calls this utility.
  </action>
  <verify>
    <automated>curl -f http://localhost:3000/health/db</automated>
  </verify>
  <done>Database connectivity and PGMQ presence are verified via API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| External -> API | Untrusted HTTP requests enter the system at Hono routes. |
| API -> Postgres | Data persists across this boundary; SQL injection is the primary threat. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | DB Client | mitigate | Use `postgres.js` tagged template literals to ensure all queries are parameterized. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify the project structure matches RESEARCH.md and the backend is reachable.
</verification>

<success_criteria>
- `bun run src/index.ts` starts without error.
- `/health` returns 200.
- `/health/db` returns 200 and confirms PGMQ.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-core-ledger-db/01-01-SUMMARY.md` when done
</output>
