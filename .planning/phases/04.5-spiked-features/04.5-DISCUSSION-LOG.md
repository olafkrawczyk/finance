# Phase 4.5: Spiked Features - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-06
**Phase:** 4.5-spiked-features
**Areas discussed:** Edit field restrictions, Delete + re-import behavior, Account ID in hash

---

## Edit Field Restrictions

| Option | Description | Selected |
|--------|-------------|----------|
| Full edit | All fields editable: type, amount, date, description, account, category | ✓ |
| Lock type + account | Lock type and account_id after creation | |
| Lock type + account + date | Only amount, description, date, category editable | |

**User's choice:** Full edit — all fields editable
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse add form | Reuse AddTransactionPage with pre-filled data | ✓ |
| Inline modal | Dedicated modal overlay on the month page | |

**User's choice:** Reuse AddTransactionPage form with pre-filled data
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Clear category | Reset category to null on edit | |
| Keep existing category | Keep existing category_id unchanged | ✓ |

**User's choice:** Keep existing category on edit
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, re-enqueue | Send transaction_id to analysis_queue | |
| No, skip | Skip re-analysis on edit | ✓ |

**User's choice:** No, skip re-analysis on edit
**Notes:** Edit is a minor correction — no need to re-run LLM analysis

---

## Delete + Re-import Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Block re-import | Keep import_hash in DB — re-import silently skips | |
| Clear hash on delete | Remove import_hash so same CSV row can re-import | ✓ |

**User's choice:** Clear hash on delete for re-importability
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| No cascade | Insights keep stale transaction IDs | |
| Clean refs | Remove from insights.linked_transaction_ids | ✓ |
| Soft delete | Mark deleted_at, hide from views | |

**User's choice:** Clean refs from insights on delete
**Notes:** None

---

## Account ID in Hash

| Option | Description | Selected |
|--------|-------------|----------|
| Add account_id | Include account_id in dedup hash | |
| Keep as-is | Current hash sufficient | ✓ |
| Add both IDs | Add account_id + reference_id | |

**User's choice:** Keep as-is — no dedup changes
**Notes:** Acceptable for single-user app

| Option | Description | Selected |
|--------|-------------|----------|
| Add column, defer hash | Add reference_id column, no hash change | |
| Skip | Skip reference_id entirely | ✓ |

**User's choice:** Skip reference_id entirely
**Notes:** None

---

## the agent's Discretion

- API endpoint design (PUT/DELETE/GET single transaction) — follow existing ledger route patterns
- Validation schema for update — reuse CreateTransactionSchema
- Trigger modification SQL — follow Approach C from spike 002
- Frontend details: navigate to edit page, confirmation dialog, toast notifications

## Deferred Ideas

- Import dedup fix (account_id in hash + reference_id column) — decided not to change
- Soft delete — rejected in favor of hard delete with hash clearing
- Inline edit modal — rejected in favor of reusing AddTransactionPage
