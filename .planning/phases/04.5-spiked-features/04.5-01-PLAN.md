---
phase: 04.5-spiked-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/db/schema.sql
  - src/core/ledger/entities.ts
  - src/application/schemas/ledger.ts
  - src/core/ledger/use-cases.ts
  - src/interface-adapters/api/ledger.ts
  - tests/ledger.test.ts
autonomous: true
requirements:
  - REQ-1.2
user_setup: []

must_haves:
  truths:
    - "Transactions can be updated — all fields (type, amount, date, description, account_id, category_id, transfer_to_account_id)"
    - "Transactions can be hard-deleted — import_hash cleared first, insights references cleaned up"
    - "Single transaction can be fetched by ID (for edit form prefill)"
    - "Edit does NOT re-enqueue to PGMQ analysis_queue"
    - "Delete runs atomically: SET import_hash=NULL → array_remove from insights → DELETE"
    - "UPDATE trigger sets updated_at automatically (BEFORE UPDATE, overrides client values)"
    - "Existing tests for immutability are rewritten — UPDATE and DELETE now succeed"

  artifacts:
    - path: "src/infrastructure/db/schema.sql"
      provides: "Modified triggers — block_immutable_change() returns NEW; block_delete() dropped; trg_transactions_updated_at added; updated_at column"
      min_lines: 215
    - path: "src/core/ledger/entities.ts"
      provides: "Transaction.updated_at field"
      contains: "updated_at: string | null"
    - path: "src/application/schemas/ledger.ts"
      provides: "UpdateTransactionSchema, UpdateTransactionInput type"
      exports: ["UpdateTransactionSchema"]
    - path: "src/core/ledger/use-cases.ts"
      provides: "getTransaction(), updateTransaction(), deleteTransaction() use-case functions"
      exports: ["getTransaction", "updateTransaction", "deleteTransaction"]
    - path: "src/interface-adapters/api/ledger.ts"
      provides: "GET /:id, PUT /:id, DELETE /:id routes on ledgerRoutes"
      exports: ["ledgerRoutes"]
    - path: "tests/ledger.test.ts"
      provides: "Updated immutability test, new CRUD tests"
      exports: []

  key_links:
    - from: "src/interface-adapters/api/ledger.ts"
      to: "src/core/ledger/use-cases.ts"
      via: "import { getTransaction, updateTransaction, deleteTransaction }"
      pattern: "import.*getTransaction.*updateTransaction.*deleteTransaction"
    - from: "src/core/ledger/use-cases.ts"
      to: "src/application/schemas/ledger.ts"
      via: "import UpdateTransactionInput"
      pattern: "import.*UpdateTransactionInput"
    - from: "src/infrastructure/db/schema.sql"
      to: "src/core/ledger/entities.ts"
      via: "Transaction.updated_at column mirrors entity field"
      pattern: "updated_at"
    - from: "src/core/ledger/use-cases.ts"
      to: "src/infrastructure/db/client"
      via: "import sql for transaction queries"
      pattern: "from.*db/client"

---

<objective>
**Backend: Schema Migration + API Routes + Use-Cases + Tests for Transaction CRUD**

**Purpose:** Allow editing all fields of a transaction and deleting transactions atomically (with import_hash clearing and insights cleanup). This removes the previous immutability enforcement per the modified REQ-1.2.

**Output:**
- `schema.sql`: modified triggers allowing UPDATE/DELETE, new `updated_at` column and auto-timestamp trigger
- `entities.ts`: `updated_at` field on Transaction interface
- `schemas/ledger.ts`: `UpdateTransactionSchema` type alias
- `use-cases.ts`: `getTransaction()`, `updateTransaction()`, `deleteTransaction()` functions
- `ledger.ts`: `GET /:id`, `PUT /:id`, `DELETE /:id` endpoints on `ledgerRoutes`
- `ledger.test.ts`: rewritten immutability test + CRUD tests
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.5-spiked-features/04.5-CONTEXT.md
@.planning/phases/04.5-spiked-features/04.5-RESEARCH.md
@.planning/phases/04.5-spiked-features/04.5-PATTERNS.md

# Source files modified in this plan
@src/infrastructure/db/schema.sql
@src/core/ledger/entities.ts
@src/application/schemas/ledger.ts
@src/core/ledger/use-cases.ts
@src/interface-adapters/api/ledger.ts
@tests/ledger.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration — triggers, updated_at column, entity field, and UpdateTransactionSchema</name>
  <read_first>src/infrastructure/db/schema.sql, src/core/ledger/entities.ts, src/application/schemas/ledger.ts</read_first>
  <files>
    src/infrastructure/db/schema.sql
    src/core/ledger/entities.ts
    src/application/schemas/ledger.ts
  </files>
  <action>
    **schema.sql** — Append migration after line 183 (before insights table, but actually after the last transaction-related block at line 83). Add the following changes:

    1. `ALTER TABLE transactions ADD COLUMN updated_at TIMESTAMPTZ;` — add the column for tracking last edit time (nullable: existing rows have NULL until first edit). Per D-01/D-07 edit tracking requirement.
    2. Replace `block_immutable_change()` function (lines 51-66): change body to `RETURN NEW;` — allow all updates. This replaces the previous logic that only allowed category_id null→non-null.
    3. Keep the trigger name `trg_transactions_no_update` attached to the modified function. The CREATE OR REPLACE updates the function body; the existing trigger automatically uses the new body.
    4. Drop the DELETE trigger and function: `DROP TRIGGER IF EXISTS trg_transactions_no_delete ON transactions;` then `DROP FUNCTION IF EXISTS block_delete();`. Per D-07 (hard delete, no soft delete).
    5. Add new `update_transaction_timestamp()` function and trigger, following the exact same pattern as `update_updated_at_column()` for `import_jobs` at lines 151-164. Use `BEFORE UPDATE ON transactions FOR EACH ROW`. The function MUST be `BEFORE UPDATE` (not AFTER) so it overrides any explicit `updated_at` SET clause (per RESEARCH.md Pitfall 3). Function name: `update_transaction_timestamp()`. Trigger name: `trg_transactions_updated_at`.

    **entities.ts** — Add `updated_at: string | null` field to the `Transaction` interface (line 4-15). Insert after `created_at: string` at line 14. Keep the existing comment block. The field is `string | null` because existing rows have NULL until first edit. Per D-01 edit tracking.

    **schemas/ledger.ts** — After line 12 (end of `CreateTransactionSchema`), add:
    ```typescript
    // PUT /transactions/:id — same fields as create, all required
    export const UpdateTransactionSchema = CreateTransactionSchema;
    export type UpdateTransactionInput = z.infer<typeof UpdateTransactionSchema>;
    ```
    The schema is identical to CreateTransactionSchema because all fields are editable and all are required (same as create). Per D-01 (all fields editable) and user discretion ("reuse CreateTransactionSchema as UpdateTransactionSchema").
    
    Also add `UpdateTransactionInput` to the type exports block at lines 45-48, after `CreateTransactionInput`.
  </action>
  <acceptance_criteria>
    - schema.sql has new `updated_at TIMESTAMPTZ` column on transactions table
    - schema.sql's `block_immutable_change()` function body returns `NEW` unconditionally
    - schema.sql no longer has `block_delete()` function or `trg_transactions_no_delete` trigger
    - schema.sql has `update_transaction_timestamp()` function with `BEFORE UPDATE ON transactions` trigger
    - entities.ts Transaction interface has `updated_at: string | null` after `created_at`
    - schemas/ledger.ts exports `UpdateTransactionSchema` and `UpdateTransactionInput` type
    - `bun test tests/ledger.test.ts -t "immutability"` shows the immutability test fails (expected — triggers changed, test not yet updated)
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ledger.test.ts 2>&1 | head -30</automated>
    <human-check>Open src/infrastructure/db/schema.sql and verify the 4 changes (column, trigger modification, delete trigger dropped, updated_at trigger added)</human-check>
  </verify>
  <done>
    - Schema migration SQL written to schema.sql
    - Transaction entity has updated_at field
    - UpdateTransactionSchema exported
    - Tests may fail (trigger behavior changed) — next task will update tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Use-case functions (getTransaction, updateTransaction, deleteTransaction) + API routes (GET/PUT/DELETE /transactions/:id)</name>
  <read_first>src/core/ledger/use-cases.ts, src/interface-adapters/api/ledger.ts, src/application/schemas/ledger.ts</read_first>
  <files>
    src/core/ledger/use-cases.ts
    src/interface-adapters/api/ledger.ts
  </files>
  <action>
    **use-cases.ts** — Add three new exported async functions after the last existing function (line 156). Follow patterns from PATTERNS.md lines 128-188.

    1. `export async function getTransaction(id: string): Promise<Transaction | undefined>`:
       - Simple `SELECT * FROM transactions WHERE id = ${id}`
       - Return `row as Transaction | undefined` (single destructured row)
       - Follow the SELECT pattern from `updateOpeningBalance` line 130-131 (single row, destructuring)

    2. `export async function updateTransaction(id: string, input: UpdateTransactionInput): Promise<Transaction>`:
       - `UPDATE transactions SET` with all 7 fields: account_id, category_id, type, amount, description, date, transfer_to_account_id
       - For nullable fields (category_id, description, transfer_to_account_id), use `?? null` pattern (same as createTransaction lines 16-17)
       - `WHERE id = ${id} RETURNING *`
       - If `!tx` throw `new Error('Transaction not found')`
       - Return `tx as Transaction`
       - **Do NOT** enqueue to PGMQ `analysis_queue` — per D-04 (explicitly no re-analysis on edit). This is explicitly different from the spike finding which includes PGMQ enqueue.
       - Use the same column listing pattern as createTransaction (lines 13-18) but with UPDATE SET syntax.

    3. `export async function deleteTransaction(id: string): Promise<void>`:
       - Use `sql.begin(async (sql) => { ... })` for atomicity per D-05/D-06/D-07
       - Step 1 (D-05): `UPDATE transactions SET import_hash = NULL WHERE id = ${id}` — clears hash so same CSV row can be re-imported
       - Step 2 (D-06): `UPDATE insights SET linked_transaction_ids = array_remove(linked_transaction_ids, ${id}) WHERE ${id} = ANY(linked_transaction_ids)` — removes the deleted transaction ID from any insight that references it
       - Step 3 (D-07): `DELETE FROM transactions WHERE id = ${id}` — hard delete

    **Import note:** Add `UpdateTransactionInput` to the type imports from `'../../application/schemas/ledger'` (line 3-8).

    **ledger.ts** — Add three new routes. Follow patterns from PATTERNS.md lines 198-333.

    **Imports to add** (after line 7):
    - `import { UpdateTransactionSchema } from '../../application/schemas/ledger';`
    - `import { getTransaction, updateTransaction, deleteTransaction } from '../../core/ledger/use-cases';`

    **GET /:id route** (after POST route at line 36):
    - `ledgerRoutes.get('/:id', requireAuth, async (c) => { ... })`
    - UUID validation: `uuidSchema.safeParse(rawId)` — same pattern as PATCH route lines 97-104
    - Call `const tx = await getTransaction(id);`
    - If `!tx` return 404 with `{ data: null, error: { message: 'Transaction not found' }, meta: null }`
    - Return `{ data: tx, error: null, meta: null }` with 200
    - Try/catch returning 500 on error

    **PUT /:id route** (after GET /:id):
    - `ledgerRoutes.put('/:id', requireAuth, zValidator('json', UpdateTransactionSchema, ...), async (c) => { ... })`
    - zValidator callback returns 400 on validation failure (same pattern as POST route lines 17-23)
    - UUID validation for param `id`
    - Call `const tx = await updateTransaction(id, input);`
    - If error message includes 'not found', return 404
    - Return `{ data: tx, error: null, meta: null }` with 200

    **DELETE /:id route** (after PUT /:id):
    - `ledgerRoutes.delete('/:id', requireAuth, async (c) => { ... })`
    - UUID validation for param `id`
    - Call `await deleteTransaction(id);`
    - Return `{ data: { success: true }, error: null, meta: null }` with 200
    - Try/catch returning 500 on error

    **Response envelope** for all routes: `{ data: T | null, error: { message: string } | null, meta: null }`
  </action>
  <acceptance_criteria>
    - use-cases.ts exports `getTransaction`, `updateTransaction`, `deleteTransaction`
    - `updateTransaction` updates all 7 fields and does NOT enqueue to PGMQ
    - `deleteTransaction` runs 3 atomic steps: SET import_hash=NULL → array_remove from insights → hard DELETE
    - ledger.ts has `GET /:id`, `PUT /:id`, `DELETE /:id` on `ledgerRoutes`
    - All routes use UUID param validation, requireAuth middleware, and envelope response format
    - PUT route uses zValidator with UpdateTransactionSchema
    - ALL user decisions implemented: D-01 (all fields), D-04 (no re-enqueue), D-05 (hash clear), D-06 (insights cleanup), D-07 (hard delete)
  </acceptance_criteria>
  <verify>
    <automated>bun check 2>&1 | head -20</automated>
  </verify>
  <done>
    - Three new use-case functions exported
    - Three new API routes registered on ledgerRoutes
    - Code compiles without errors (`bun check` passes)
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests — rewrite immutability test, add CRUD tests for get/update/delete</name>
  <read_first>tests/ledger.test.ts, src/core/ledger/use-cases.ts</read_first>
  <files>tests/ledger.test.ts</files>
  <action>
    **Imports update** (line 3-10):
    Add `getTransaction, updateTransaction, deleteTransaction` to the import from `'../src/core/ledger/use-cases'`.

    **Rewrite immutability test** (lines 152-174 — rename and change behavior):
    - Rename from `'enforces transaction immutability (DB trigger validation)'` to `'allows transaction update and delete (immutability removed)'`
    - Fetch first transaction via `listTransactions`
    - Test UPDATE succeeds: `await sql\`UPDATE transactions SET description = 'Updated via test' WHERE id = ${tx.id} RETURNING *\`` — expect 1 row returned, expect description to match
    - Do NOT test DELETE here (delete has its own dedicated test below) — just test that a direct SQL UPDATE works

    **Add test: `getTransaction() returns single transaction by ID`:**
    - Create a transaction via `createTransaction`
    - Call `getTransaction(tx.id)`
    - Expect returned object to have matching `id` and `amount`
    - Call `getTransaction` with non-existent UUID — expect `undefined`

    **Add test: `updateTransaction() succeeds and returns updated entity`:**
    - Create a transaction via `createTransaction` with known values
    - Call `updateTransaction(tx.id, { account_id, category_id: ..., type: 'expense', amount: '200.0000', description: 'Updated desc', date: '2026-06-15' })`
    - Expect returned `id` matches original
    - Expect `amount` is '200.0000'
    - Expect `description` is 'Updated desc'
    - Expect `updated_at` is defined (not null)
    - Expect `created_at` unchanged (inserted in first call)

    **Add test: `updateTransaction() throws when transaction not found`:**
    - Call `updateTransaction(nonExistentId, ...)` — expect it throws with message containing 'not found'
    - Use try/catch pattern (same as existing uniqueness test at lines 122-135)

    **Add test: `deleteTransaction() clears hash, cleans insights, hard deletes`:**
    - Create a transaction via `createTransaction`
    - Verify it exists via `getTransaction`
    - Call `deleteTransaction(tx.id)`
    - Verify `getTransaction(tx.id)` returns `undefined`
    - Verify that direct SQL `SELECT import_hash FROM transactions WHERE id = ${tx.id}` returns empty (row gone — hash was cleared before delete but delete removed the row)
    - Note: insights cleanup is hard to test without an insight referencing this transaction ID. For test purposes, verify it doesn't throw (the array_remove is a no-op if no insight references the ID). Add a comment explaining this.

    **Cleanup note:** Ensure `beforeAll` still truncates properly. Existing setup at lines 15-30 is sufficient — it truncates transactions and cleans queue.

    **Important:** After tests pass, also update the existing enqueuing test at line 33. The createTransaction test should be unaffected since we didn't change createTransaction behavior.
  </action>
  <acceptance_criteria>
    - Existing immutability test rewritten to expect UPDATE to succeed
    - New test for `getTransaction` (found + not-found cases)
    - New test for `updateTransaction` (success + not-found cases)
    - New test for `deleteTransaction` (create → delete → verify gone)
    - Run: `bun test tests/ledger.test.ts` — ALL tests pass (expected: 7-8 tests)
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ledger.test.ts 2>&1</automated>
  </verify>
  <done>
    - All tests pass with `bun test tests/ledger.test.ts`
    - If first run fails due to DB state, run `docker compose restart` then retry
  </done>
</task>

</tasks>

<artifact_register>
## Artifacts This Phase Produces (Backend)

### New Types / Interfaces
| Symbol | Kind | File |
|--------|------|------|
| `Transaction.updated_at` | interface field (string \| null) | `src/core/ledger/entities.ts` |
| `UpdateTransactionSchema` | Zod schema object | `src/application/schemas/ledger.ts` |
| `UpdateTransactionInput` | TypeScript type | `src/application/schemas/ledger.ts` |

### New Functions
| Symbol | Kind | File |
|--------|------|------|
| `getTransaction(id)` | async function returning `Transaction \| undefined` | `src/core/ledger/use-cases.ts` |
| `updateTransaction(id, input)` | async function returning `Transaction` | `src/core/ledger/use-cases.ts` |
| `deleteTransaction(id)` | async function returning `void` | `src/core/ledger/use-cases.ts` |

### New API Routes
| Route | Method | File |
|-------|--------|------|
| `/transactions/:id` | GET | `src/interface-adapters/api/ledger.ts` |
| `/transactions/:id` | PUT | `src/interface-adapters/api/ledger.ts` |
| `/transactions/:id` | DELETE | `src/interface-adapters/api/ledger.ts` |

### Database Changes
| Change | Type | File |
|--------|------|------|
| `transactions.updated_at TIMESTAMPTZ` | new column (nullable) | `src/infrastructure/db/schema.sql` |
| `block_immutable_change()` | modified — now returns NEW unconditionally | `src/infrastructure/db/schema.sql` |
| `block_delete()` + `trg_transactions_no_delete` | removed | `src/infrastructure/db/schema.sql` |
| `update_transaction_timestamp()` | new trigger function (BEFORE UPDATE) | `src/infrastructure/db/schema.sql` |
| `trg_transactions_updated_at` | new trigger (executes on UPDATE) | `src/infrastructure/db/schema.sql` |

### Tests
| Test | File |
|------|------|
| `'allows transaction update and delete (immutability removed)'` | `tests/ledger.test.ts` |
| `'getTransaction() returns single transaction by ID'` | `tests/ledger.test.ts` |
| `'updateTransaction() succeeds and returns updated entity'` | `tests/ledger.test.ts` |
| `'updateTransaction() throws when transaction not found'` | `tests/ledger.test.ts` |
| `'deleteTransaction() clears hash, cleans insights, hard deletes'` | `tests/ledger.test.ts` |
</artifact_register>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Authenticated user sends HTTP requests to Hono routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-01 | Tampering | GET/PUT/DELETE /transactions/:id | mitigate | `requireAuth` middleware on all routes (existing pattern) |
| T-04.5-02 | Tampering | PUT /transactions/:id request body | mitigate | Zod `UpdateTransactionSchema` via `zValidator` at route entry (existing pattern) |
| T-04.5-03 | Tampering | `:id` path param (non-UUID injection) | mitigate | `uuidSchema.safeParse()` before any DB operation (existing pattern at ledger.ts:9) |
| T-04.5-04 | Tampering | SQL injection in dynamic queries | mitigate | postgres.js template literals (`${id}`) — no raw string concatenation (existing pattern) |
| T-04.5-05 | Information Disclosure | GET /transactions/:id returns full entity | accept | Transaction data is user's own data; auth gate prevents cross-user access |
| T-04.5-SC | Tampering | No new npm packages installed | accept | No package installs in this plan — slopcheck not applicable |
</threat_model>

<verification>
## Overall Phase Checks

1. Run `bun test` — all tests must pass
2. Run `bun check` — no type errors
3. Verify schema migration: `psql -d finance -c "\d transactions"` shows `updated_at` column
4. Verify trigger dropped: `psql -d finance -c "SELECT tgname FROM pg_trigger WHERE tgname = 'trg_transactions_no_delete'"` — empty result
5. Verify trigger created: `psql -d finance -c "SELECT tgname FROM pg_trigger WHERE tgname = 'trg_transactions_updated_at'"` — shows trigger

## Nyquist Validation
Every test must have an automated command. See Task 3 verify sections.
</verification>

<success_criteria>
1. ✅ All backend code compiles (`bun check` passes)
2. ✅ All tests pass (`bun test tests/ledger.test.ts`)
3. ✅ Schema migration applied (updated_at column, triggers modified/dropped/created)
4. ✅ Three new API endpoints functional (GET/PUT/DELETE /transactions/:id)
5. ✅ All user decisions implemented: D-01 through D-07
6. ✅ REQ-1.2 updated behavior (immutability removed for edit/delete)
</success_criteria>
</objective>
