---
phase: 06-schema-migration-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/db/migrations/008_add_user_id_columns.sql
  - src/infrastructure/db/migrations/009_update_uniques.sql
  - src/infrastructure/db/migrations/010_add_indexes.sql
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05, SCHEMA-06, SCHEMA-07, SCHEMA-08, SCHEMA-09, SCHEMA-10, SCHEMA-11]

must_haves:
  truths:
    - "accounts has user_id TEXT NOT NULL REFERENCES \"user\"(id) ON DELETE CASCADE"
    - "categories has user_id TEXT NOT NULL REFERENCES \"user\"(id) ON DELETE CASCADE"
    - "transactions has user_id TEXT NOT NULL REFERENCES \"user\"(id) ON DELETE CASCADE"
    - "monthly_opening_balances has user_id TEXT NOT NULL REFERENCES \"user\"(id) ON DELETE CASCADE"
    - "assets has user_id TEXT NOT NULL REFERENCES \"user\"(id) ON DELETE CASCADE"
    - "import_jobs has user_id TEXT NOT NULL REFERENCES \"user\"(id) ON DELETE CASCADE"
    - "categories has UNIQUE(user_id, name) replacing UNIQUE(name)"
    - "assets has UNIQUE(user_id, name) replacing UNIQUE(name)"
    - "transactions has UNIQUE(user_id, import_hash) replacing UNIQUE(import_hash)"
    - "monthly_opening_balances has UNIQUE(user_id, year, month) replacing UNIQUE(year, month)"
  artifacts:
    - path: "src/infrastructure/db/migrations/008_add_user_id_columns.sql"
      provides: "ADD COLUMN user_id on all 6 domain tables with FK to \"user\"(id)"
      min_lines: 25
    - path: "src/infrastructure/db/migrations/009_update_uniques.sql"
      provides: "DROP global UNIQUE → ADD per-user composite UNIQUE on 4 tables"
      min_lines: 25
    - path: "src/infrastructure/db/migrations/010_add_indexes.sql"
      provides: "Documentation: UNIQUE constraints create required indexes, no additional indexes needed per D-04"
      min_lines: 5
  key_links:
    - from: "008_add_user_id_columns.sql"
      to: "009_update_uniques.sql"
      via: "Sequential node-pg-migrate — 008 applies first, then 009"
      pattern: "008.*009"
    - from: "009_update_uniques.sql"
      to: "010_add_indexes.sql"
      via: "Sequential node-pg-migrate — 009 applies first, then 010"
      pattern: "009.*010"
---

<objective>
Create three SQL migration files that add user_id columns, update UNIQUE constraints, and document index strategy for Phase 6 schema migration.

Purpose: Provide the database DDL that enables per-user data isolation for all 6 domain tables. Since the DB will be wiped before onboarding (D-01), user_id is added as NOT NULL directly with no backfill needed.

Output:
- `src/infrastructure/db/migrations/008_add_user_id_columns.sql` — ADD COLUMN user_id to all 6 tables
- `src/infrastructure/db/migrations/009_update_uniques.sql` — Replace global UNIQUE with per-user composite UNIQUE on 4 tables
- `src/infrastructure/db/migrations/010_add_indexes.sql` — Document that UNIQUE constraints create required indexes; no-op migration
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/olafk/finance/.planning/phases/06-schema-migration-backfill/06-RESEARCH.md
@/home/olafk/finance/.planning/phases/06-schema-migration-backfill/06-PATTERNS.md
@/home/olafk/finance/.planning/phases/06-schema-migration-backfill/06-CONTEXT.md
@/home/olafk/finance/src/infrastructure/db/schema.sql

# Migration file pattern reference
@/home/olafk/finance/src/infrastructure/db/migrations/007_setup_pgmq.sql
@/home/olafk/finance/src/infrastructure/db/migrations/001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 008_add_user_id_columns.sql — add user_id column to all 6 domain tables</name>
  <read_first>
    src/infrastructure/db/migrations/007_setup_pgmq.sql (marker pattern)
    src/infrastructure/db/migrations/001_initial_schema.sql (FK reference pattern against `"user"(id)`)
    src/infrastructure/db/schema.sql (current table definitions to confirm column ordering)
    .planning/phases/06-schema-migration-backfill/06-RESEARCH.md § "Pattern 1: ALTER TABLE ADD COLUMN with FK" (exact SQL)
    .planning/phases/06-schema-migration-backfill/06-PATTERNS.md § "ALTER TABLE ADD COLUMN with FK to user"
  </read_first>
  <files>
    src/infrastructure/db/migrations/008_add_user_id_columns.sql
  </files>
  <action>
    Create `src/infrastructure/db/migrations/008_add_user_id_columns.sql` following the established node-pg-migrate SQL convention:

    - Header comment: `-- 008_add_user_id_columns: Add user_id FK to all domain tables`
    - Marker `-- Up Migration`
    - Six ALTER TABLE ADD COLUMN statements, one per table, in this order: accounts, categories, transactions, monthly_opening_balances, assets, import_jobs
    - For each: `ADD COLUMN user_id TEXT NOT NULL REFERENCES "user"(id) ON DELETE CASCADE`
    - Use `TEXT` type (NOT UUID) — the `"user"(id)` column is TEXT per Better Auth convention. Verified in schema.sql line 72.
    - Marker `-- Down Migration`
    - Six ALTER TABLE DROP COLUMN statements in reverse order: import_jobs, assets, monthly_opening_balances, transactions, categories, accounts
    - Down: `ALTER TABLE <table> DROP COLUMN user_id`
    - Retain the same blank-line style as 007_setup_pgmq.sql between markers and SQL blocks

    Decision reference: D-01 (DB wiped, NOT NULL directly), D-02 (NOT NULL), D-03 (split by concern), D-07 (down migration required)
  </action>
  <verify>
    <automated>
      # Verify file exists with correct structure
      test -f src/infrastructure/db/migrations/008_add_user_id_columns.sql
      # Verify Up Migration marker
      grep -c "Up Migration" src/infrastructure/db/migrations/008_add_user_id_columns.sql | grep -q 1
      # Verify Down Migration marker
      grep -c "Down Migration" src/infrastructure/db/migrations/008_add_user_id_columns.sql | grep -q 1
      # Verify all 6 tables have ADD COLUMN user_id
      for table in accounts categories transactions monthly_opening_balances assets import_jobs; do
        grep -q "table.*user_id" src/infrastructure/db/migrations/008_add_user_id_columns.sql || exit 1
      done
      # Verify TEXT type (not UUID)
      grep -c "TEXT" src/infrastructure/db/migrations/008_add_user_id_columns.sql | grep -q 6
      # Verify FK reference
      grep -c "REFERENCES" src/infrastructure/db/migrations/008_add_user_id_columns.sql | grep -q 6
      # Verify DROP COLUMN in down migration for all 6
      grep -c "DROP COLUMN user_id" src/infrastructure/db/migrations/008_add_user_id_columns.sql | grep -q 6
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/infrastructure/db/migrations/008_add_user_id_columns.sql` exists
    - Contains exactly one `-- Up Migration` and one `-- Down Migration` marker
    - Up section has 6 `ALTER TABLE ... ADD COLUMN user_id TEXT NOT NULL REFERENCES "user"(id) ON DELETE CASCADE` statements
    - Down section has 6 `ALTER TABLE ... DROP COLUMN user_id` statements in reverse table order
    - All 6 tables (accounts, categories, transactions, monthly_opening_balances, assets, import_jobs) are present
  </acceptance_criteria>
  <done>
    008_add_user_id_columns.sql created with both up and down migrations for 6 tables, using TEXT type and FK to "user"(id) with ON DELETE CASCADE.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 009_update_uniques.sql — replace global UNIQUE with per-user composite UNIQUE on 4 tables</name>
  <read_first>
    src/infrastructure/db/schema.sql (current constraint declarations: lines 13-15 for UNIQUE(name), line 28 for UNIQUE(import_hash), line 41 for UNIQUE(year, month))
    .planning/phases/06-schema-migration-backfill/06-RESEARCH.md § "Pattern 2: Drop & Recreate UNIQUE Constraints" (exact SQL examples)
    .planning/phases/06-schema-migration-backfill/06-PATTERNS.md § "DROP/ADD CONSTRAINT" (constraint naming convention)
  </read_first>
  <files>
    src/infrastructure/db/migrations/009_update_uniques.sql
  </files>
  <action>
    Create `src/infrastructure/db/migrations/009_update_uniques.sql` with these exact constraint operations:

    Up Migration order:
    1. categories — `DROP CONSTRAINT categories_name_key` → `ADD CONSTRAINT categories_user_id_name_key UNIQUE(user_id, name)`
    2. assets — `DROP CONSTRAINT assets_name_key` → `ADD CONSTRAINT assets_user_id_name_key UNIQUE(user_id, name)`
    3. transactions — `DROP CONSTRAINT transactions_import_hash_key` → `ADD CONSTRAINT transactions_user_id_import_hash_key UNIQUE(user_id, import_hash)`
    4. monthly_opening_balances — `DROP CONSTRAINT monthly_opening_balances_year_month_key` → `ADD CONSTRAINT monthly_opening_balances_user_id_year_month_key UNIQUE(user_id, year, month)`

    Use `DROP CONSTRAINT IF EXISTS` on all drop operations for safety (constraint names may vary — per Pitfall 1).

    Down Migration order (reverse of Up):
    1. monthly_opening_balances — `DROP CONSTRAINT monthly_opening_balances_user_id_year_month_key` → `ADD CONSTRAINT monthly_opening_balances_year_month_key UNIQUE(year, month)`
    2. transactions — `DROP CONSTRAINT transactions_user_id_import_hash_key` → `ADD CONSTRAINT transactions_import_hash_key UNIQUE(import_hash)`
    3. assets — `DROP CONSTRAINT assets_user_id_name_key` → `ADD CONSTRAINT assets_name_key UNIQUE(name)`
    4. categories — `DROP CONSTRAINT categories_user_id_name_key` → `ADD CONSTRAINT categories_name_key UNIQUE(name)`

    Use `IF NOT EXISTS` suffix on ADD CONSTRAINT in down migration for idempotency.

    Comment each operation block with a descriptive comment (e.g., `-- Categories: name uniqueness scoped to user`).

    Decision reference: D-03 (split by concern), D-05 (import_hash scoping), D-08 (per-user unique names), D-09 (per-user unique import hashes), D-10 (per-user unique opening balance months), D-07 (down migration required)
  </action>
  <verify>
    <automated>
      test -f src/infrastructure/db/migrations/009_update_uniques.sql
      # Verify Up and Down markers
      grep -c "Up Migration" src/infrastructure/db/migrations/009_update_uniques.sql | grep -q 1
      grep -c "Down Migration" src/infrastructure/db/migrations/009_update_uniques.sql | grep -q 1
      # Verify Up: 4 DROP CONSTRAINT statements
      DROPS_UP=$(grep -c "DROP CONSTRAINT" src/infrastructure/db/migrations/009_update_uniques.sql)
      [ "$DROPS_UP" -eq 4 ]
      # Verify Up: 4 ADD CONSTRAINT statements
      ADDS_UP=$(grep -c "ADD CONSTRAINT" src/infrastructure/db/migrations/009_update_uniques.sql)
      [ "$ADDS_UP" -eq 4 ]
      # Verify Down: 4 DROP CONSTRAINT on new names
      grep -c "user_id" src/infrastructure/db/migrations/009_update_uniques.sql | grep -q 4
      # Verify all 4 constraint names are present
      grep -q "categories_user_id_name_key" src/infrastructure/db/migrations/009_update_uniques.sql
      grep -q "assets_user_id_name_key" src/infrastructure/db/migrations/009_update_uniques.sql
      grep -q "transactions_user_id_import_hash_key" src/infrastructure/db/migrations/009_update_uniques.sql
      grep -q "monthly_opening_balances_user_id_year_month_key" src/infrastructure/db/migrations/009_update_uniques.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/infrastructure/db/migrations/009_update_uniques.sql` exists
    - Up section: 4 DROP CONSTRAINT + 4 ADD CONSTRAINT for categories, assets, transactions, monthly_opening_balances
    - Down section: 4 ADD CONSTRAINT (restoring old global UNIQUE) + 4 DROP CONSTRAINT (removing per-user UNIQUE) in reverse order
    - All constraint names match the convention `{table}_user_id_{column(s)}_key`
    - Uses `DROP CONSTRAINT IF EXISTS` for safety per Pitfall 1
  </acceptance_criteria>
  <done>
    009_update_uniques.sql created with up migration dropping 4 global UNIQUE constraints and adding 4 per-user composite UNIQUE constraints, and down migration restoring them in reverse order.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 010_add_indexes.sql — document index strategy (no-op migration)</name>
  <read_first>
    src/infrastructure/db/schema.sql (existing indexes: lines 45-48, 117-118, 133-134)
    .planning/phases/06-schema-migration-backfill/06-RESEARCH.md § "Example 3: Full Migration File"
    .planning/phases/06-schema-migration-backfill/06-PATTERNS.md § "CREATE INDEX IF NOT EXISTS"
  </read_first>
  <files>
    src/infrastructure/db/migrations/010_add_indexes.sql
  </files>
  <action>
    Create `src/infrastructure/db/migrations/010_add_indexes.sql` as a documentation/acknowledgment migration:

    Up Migration: A SQL comment block documenting that the UNIQUE constraints created in migration 009 automatically create btree indexes on:
    - categories(user_id, name)
    - assets(user_id, name)
    - transactions(user_id, import_hash)
    - monthly_opening_balances(user_id, year, month)

    State clearly per D-04 that no additional composite indexes are needed beyond these constraint-created indexes.

    Include a comment about `idx_mob_year_month`: the existing standalone index on `monthly_opening_balances(year, month)` is retained for now — it may become redundant after Phase 7 when all queries include user_id filtering, but removal is deferred.

    The Down Migration should be a simple comment explaining there are no indexes to drop.
  </action>
  <verify>
    <automated>
      test -f src/infrastructure/db/migrations/010_add_indexes.sql
      grep -c "Up Migration" src/infrastructure/db/migrations/010_add_indexes.sql | grep -q 1
      grep -c "Down Migration" src/infrastructure/db/migrations/010_add_indexes.sql | grep -q 1
      # Verify it documents the 4 UNIQUE constraint indexes
      grep -q "categories(user_id, name)" src/infrastructure/db/migrations/010_add_indexes.sql
      grep -q "assets(user_id, name)" src/infrastructure/db/migrations/010_add_indexes.sql
      grep -q "transactions(user_id, import_hash)" src/infrastructure/db/migrations/010_add_indexes.sql
      grep -q "monthly_opening_balances(user_id, year, month)" src/infrastructure/db/migrations/010_add_indexes.sql
      # Verify no ALTER TABLE or CREATE INDEX (no actual DDL)
      grep -c "ALTER TABLE\|CREATE INDEX" src/infrastructure/db/migrations/010_add_indexes.sql | grep -q 0
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/infrastructure/db/migrations/010_add_indexes.sql` exists
    - Contains `-- Up Migration` and `-- Down Migration` markers
    - Up section is a comment-only block documenting the 4 constraint-created indexes and the D-04 "minimal essential" policy
    - No ALTER TABLE, CREATE INDEX, or other DDL statements in the migration
    - Documents the idx_mob_year_month retention decision
  </acceptance_criteria>
  <done>
    010_add_indexes.sql created as a documentation migration acknowledging that UNIQUE constraints (from 009) provide all required indexes, with no additional DDL.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

### New Files
| Path | Type | Description |
|------|------|-------------|
| `src/infrastructure/db/migrations/008_add_user_id_columns.sql` | SQL migration | 6 ALTER TABLE ADD COLUMN user_id — up + down |
| `src/infrastructure/db/migrations/009_update_uniques.sql` | SQL migration | 4 DROP/ADD UNIQUE constraint pairs — up + down |
| `src/infrastructure/db/migrations/010_add_indexes.sql` | SQL migration | Documentation-only — confirms UNIQUE constraints cover index needs |

### Constraint Names Created
| Constraint Name | Table | Columns | Replaces |
|-----------------|-------|---------|----------|
| `categories_user_id_name_key` | categories | (user_id, name) | `categories_name_key` |
| `assets_user_id_name_key` | assets | (user_id, name) | `assets_name_key` |
| `transactions_user_id_import_hash_key` | transactions | (user_id, import_hash) | `transactions_import_hash_key` |
| `monthly_opening_balances_user_id_year_month_key` | monthly_opening_balances | (user_id, year, month) | `monthly_opening_balances_year_month_key` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration files → PostgreSQL | DDL statements executed by node-pg-migrate with direct database connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | FK constraint bypass via direct DB access | mitigate | `ON DELETE CASCADE` ensures referential integrity at schema level. Application-layer access scoping is Phase 7 responsibility. |
| T-06-02 | Tampering | Constraint naming mismatch during migration | mitigate | Use `DROP CONSTRAINT IF EXISTS` in 009 migration for safety. Verify constraint names via `pg_constraint` catalog if in doubt. |
| T-06-SC | Supply Chain | npm/pip/cargo installs | accept | No new packages — all dependencies verified in v1.0. No slopcheck needed. |
</threat_model>

<verification>
## Migration Verification

After all 3 migrations are created:

```bash
# Verify files exist
ls -la src/infrastructure/db/migrations/008_add_user_id_columns.sql
ls -la src/infrastructure/db/migrations/009_update_uniques.sql
ls -la src/infrastructure/db/migrations/010_add_indexes.sql

# Verify file numbering is consecutive
echo "Files: $(ls src/infrastructure/db/migrations/0*sql | wc -l) total, next available: 011"

# Verify down migration symmetry — run grep to check DROP and reverse ops match
grep -A2 "Down Migration" src/infrastructure/db/migrations/008_add_user_id_columns.sql
grep -A2 "Down Migration" src/infrastructure/db/migrations/009_update_uniques.sql
grep -A2 "Down Migration" src/infrastructure/db/migrations/010_add_indexes.sql
```
</verification>

<success_criteria>
- 3 migration files exist at correct paths with correct naming (008, 009, 010)
- Each file has both `-- Up Migration` and `-- Down Migration` markers
- 008: all 6 tables get user_id column (TEXT, NOT NULL, FK to "user"(id), ON DELETE CASCADE)
- 009: 4 global UNIQUE constraints replaced with per-user composite UNIQUE constraints
- 010: acknowledges no additional indexes needed per D-04
- All down migrations properly reverse their up counterparts
</success_criteria>

<output>
Create `.planning/phases/06-schema-migration-backfill/06-01-SUMMARY.md` when done
</output>
