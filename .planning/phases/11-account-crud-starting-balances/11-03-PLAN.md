---
phase: 11-account-crud-starting-balances
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql
  - src/core/assets/use-cases.ts
  - src/interface-adapters/api/assets.ts
  - src/core/ledger/use-cases.ts
  - src/infrastructure/db/schema.sql
autonomous: true
requirements:
  - BAL-02
must_haves:
  truths:
    - "Migration 014 creates asset_value_snapshots table with FK to assets(id) and ON DELETE CASCADE"
    - "Migration 014 is reversible (down function drops the table)"
    - "updateAsset creates a snapshot of the OLD value before overwriting with new value — transparent to the caller"
    - "getMonthlySummary aggregates per-account starting_balance where starting_balance_date <= month as baseline"
    - "getMonthlySummary falls back to monthly_opening_balances if no accounts have starting_balance set (legacy compat)"
    - "Accounts with starting_balance_date=NULL and starting_balance > 0 treated as effective from earliest transaction month"
    - "getMonthlySummary computes wartosc_netto = stan_konta + forward-filled asset values from snapshots"
    - "Forward-fill: last snapshot value carries forward for months without explicit snapshots"
    - "GET /assets/:id/snapshots returns asset value snapshot history in date order"
    - "Existing listAssets, createAsset, getAsset functions preserved unchanged"
  artifacts:
    - path: "src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql"
      provides: "Asset value snapshots table"
      contains: "CREATE TABLE asset_value_snapshots"
    - path: "src/core/assets/use-cases.ts"
      provides: "Snapshot auto-creation on asset update + snapshot list"
      contains: "createAssetSnapshot"
    - path: "src/interface-adapters/api/assets.ts"
      provides: "Asset snapshot endpoint"
      contains: "GET /:id/snapshots"
    - path: "src/core/ledger/use-cases.ts"
      provides: "Updated balance computation with per-account starting balances and combined net worth"
      contains: "getMonthlySummary"
  key_links:
    - from: "src/core/assets/use-cases.ts"
      to: "src/infrastructure/db/client"
      via: "import sql from '../../infrastructure/db/client'"
    - from: "src/core/ledger/use-cases.ts"
      to: "src/core/reference/use-cases.ts"
      via: "import { listAccounts } from '../reference/use-cases' (for per-account starting balance aggregation)"
    - from: "src/core/assets/use-cases.ts"
      to: "src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql"
      via: "INSERT INTO asset_value_snapshots"

## Artifacts This Phase Produces

### New files:
- `src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql` — Migration: create asset_value_snapshots table with columns (id UUID PK, asset_id FK→assets(id) ON DELETE CASCADE, value NUMERIC(19,4) NOT NULL, date DATE NOT NULL, notes TEXT, created_at TIMESTAMPTZ DEFAULT now())

### Modified files:
- `src/core/assets/use-cases.ts` — Add `createAssetSnapshot(assetId, value, date, notes)` function; modify `updateAsset` to auto-create snapshot of old value before update; add `listAssetSnapshots(assetId)` function
- `src/interface-adapters/api/assets.ts` — Add `GET /assets/:id/snapshots` route
- `src/core/ledger/use-cases.ts` — Rewrite balance computation: aggregate per-account starting_balance → baseline; forward-fill asset snapshots → wartosc_netto; fall back to monthly_opening_balances for legacy users
- `src/infrastructure/db/schema.sql` — Add asset_value_snapshots table definition

### New symbols:
- `createAssetSnapshot(assetId, value, date, notes)` — inserts a snapshot row
- `listAssetSnapshots(assetId)` — returns snapshot history ordered by date
- `getMonthlySummary` — modified return type includes `wartosc_netto` field
---

<objective>
Implement asset value history snapshots and update the balance computation. Create the asset_value_snapshots table, auto-snapshot on asset value changes, add a snapshot read endpoint, and rewrite getMonthlySummary to use per-account starting balance aggregation with forward-filled asset values for the combined net worth line.

Purpose: Enable accurate net worth tracking with per-account starting balances and asset value history.
Output: Migration, asset snapshot logic, snapshot endpoint, updated balance computation.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-account-crud-starting-balances/11-CONTEXT.md
@.planning/phases/11-account-crud-starting-balances/11-SPEC.md
@/home/olafk/finance/src/core/assets/use-cases.ts
@/home/olafk/finance/src/interface-adapters/api/assets.ts
@/home/olafk/finance/src/core/ledger/use-cases.ts
@/home/olafk/finance/src/core/reference/use-cases.ts
@/home/olafk/finance/src/infrastructure/db/migrations/006_create_assets_table.sql
@/home/olafk/finance/src/infrastructure/db/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 014 — asset_value_snapshots table</name>
  <files>
    src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql
  </files>
  <read_first>
    @/home/olafk/finance/src/infrastructure/db/migrations/006_create_assets_table.sql
    @/home/olafk/finance/src/infrastructure/db/schema.sql (assets table definition)
  </read_first>
  <action>
    Create `src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql`:
    
    Up:
    ```sql
    CREATE TABLE IF NOT EXISTS asset_value_snapshots (
      id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      asset_id UUID NOT NULL REFERENCES assets(id) ON DELETE CASCADE,
      value NUMERIC(19,4) NOT NULL,
      date DATE NOT NULL,
      notes TEXT,
      created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );
    CREATE INDEX IF NOT EXISTS idx_asset_value_snapshots_asset_id_date ON asset_value_snapshots(asset_id, date);
    ```
    
    Down:
    ```sql
    DROP TABLE IF EXISTS asset_value_snapshots CASCADE;
    ```
    
    Follow the exact format of 006_create_assets_table.sql (IF NOT EXISTS guards, column comments, index creation).
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE.*asset_value_snapshots" src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql && grep -q "REFERENCES assets" src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql && grep -q "ON DELETE CASCADE" src/infrastructure/db/migrations/014_add_asset_value_snapshots.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration 014 creates asset_value_snapshots table
    - Columns: id (UUID PK), asset_id (FK→assets ON DELETE CASCADE), value (NUMERIC 19,4), date (DATE, NOT NULL), notes (TEXT), created_at (TIMESTAMPTZ)
    - Index on (asset_id, date) for efficient forward-fill queries
    - IF NOT EXISTS on CREATE TABLE and CREATE INDEX
    - Down drops the table with CASCADE
    - Valid SQL: can be parsed by postgres
  </acceptance_criteria>
  <done>Migration 014 created: asset_value_snapshots table with FK, index, and up/down reversibility</done>
</task>

<task type="auto">
  <name>Task 2: Add snapshot functions to assets/use-cases.ts and modify updateAsset</name>
  <files>
    src/core/assets/use-cases.ts
  </files>
  <read_first>
    @/home/olafk/finance/src/core/assets/use-cases.ts
  </read_first>
  <action>
    Modify `src/core/assets/use-cases.ts`:
    
    1. Add new function `listAssetSnapshots(assetId: string)`:
       - SELECT * FROM asset_value_snapshots WHERE asset_id = ${assetId} ORDER BY date ASC, created_at ASC
       - Returns array of snapshot rows
    
    2. Add new function `createAssetSnapshot(assetId: string, value: number, date: string, notes?: string)`:
       - INSERT INTO asset_value_snapshots (asset_id, value, date, notes) VALUES (...) RETURNING *
       - Destructure [row] from result, return row
       - Default notes to NULL if not provided
    
    3. Modify `updateAsset(id, name, value, userId)` to auto-create a snapshot of the OLD value before updating:
       - Before the UPDATE: SELECT current value: `SELECT value FROM assets WHERE id = ${id} AND user_id = ${userId}`
       - If the new `value` differs from current `value`: INSERT a snapshot with the current value and today's date
       - Then proceed with the UPDATE as before
       - This is transparent — the caller does not need to know about snapshots (D-10)
       - Note: use `new Date().toISOString().split('T')[0]` for snapshot date
    
    All functions follow existing patterns:
    - Import sql from '../../infrastructure/db/client'
    - Positional parameters
    - Return raw rows from postgres.js
  </action>
  <verify>
    <automated>grep -c "listAssetSnapshots" src/core/assets/use-cases.ts && grep -c "createAssetSnapshot" src/core/assets/use-cases.ts && grep -c "asset_value_snapshots" src/core/assets/use-cases.ts</automated>
  </verify>
  <acceptance_criteria>
    - listAssetSnapshots returns all snapshots for an asset ordered by date ASC
    - createAssetSnapshot inserts a row and returns it
    - updateAsset: reads current value before update, creates snapshot if value differs, then performs update
    - Snapshot creation is transparent — no API change for updateAsset callers
    - Snapshot stores the OLD value (pre-update), not the new value
    - Existing listAssets, createAsset, deleteAsset functions preserved unchanged
  </acceptance_criteria>
  <done>Asset snapshot functions added listAssetSnapshots, createAssetSnapshot; updateAsset auto-snapshots old value on change</done>
</task>

<task type="auto">
  <name>Task 3: Add asset snapshot read endpoint</name>
  <files>
    src/interface-adapters/api/assets.ts
  </files>
  <read_first>
    @/home/olafk/finance/src/interface-adapters/api/assets.ts
  </read_first>
  <action>
    Modify `src/interface-adapters/api/assets.ts`:
    
    1. Add import: `import { listAssetSnapshots } from '../../core/assets/use-cases'`
    
    2. Add GET route before the export:
       ```typescript
       assetsRoutes.get('/:id/snapshots', requireAuth, async (c) => {
         try {
           const user = c.get('user');
           const id = c.req.param('id');
           // Verify the asset belongs to the current user
           const asset = await getAsset(id, user.id);
           if (!asset) {
             return c.json({ data: null, error: { message: 'Not found' }, meta: null }, 404);
           }
           const snapshots = await listAssetSnapshots(id);
           return c.json({ data: snapshots, error: null, meta: { total: snapshots.length } });
         } catch (err) {
           return c.json({ data: null, error: { message: err instanceof Error ? err.message : 'Internal error' }, meta: null }, 500);
         }
       });
       ```
    
    Follow existing route patterns (try/catch, standard response shape, requireAuth).
    Need to import `getAsset` if not already imported (add to imports from use-cases).
  </action>
  <verify>
    <automated>grep -c "/:id/snapshots" src/interface-adapters/api/assets.ts && grep -c "listAssetSnapshots" src/interface-adapters/api/assets.ts</automated>
  </verify>
  <acceptance_criteria>
    - GET /assets/:id/snapshots returns snapshot history for the asset
    - Verifies asset ownership (404 if asset doesn't belong to user)
    - Uses requireAuth middleware
    - Standard try/catch error handling and response shape
    - Existing asset CRUD routes preserved unchanged
  </acceptance_criteria>
  <done>Asset snapshot read endpoint added: GET /assets/:id/snapshots with ownership validation</done>
</task>

<task type="auto">
  <name>Task 4: Update getMonthlySummary with per-account starting balances + combined net worth</name>
  <files>
    src/core/ledger/use-cases.ts
  </files>
  <read_first>
    @/home/olafk/finance/src/core/ledger/use-cases.ts
    @/home/olafk/finance/src/core/reference/use-cases.ts (listAccounts function)
  </read_first>
  <action>
    Modify `src/core/ledger/use-cases.ts`:
    
    1. Add imports:
       ```typescript
       import { listAccounts } from '../reference/use-cases';
       ```
    
    2. Rewrite the opening balance computation in getMonthlySummary:
       
       **Replace the existing monthly_opening_balances query with:**
       A. Fetch all accounts for the user: `const accounts = await listAccounts(userId);`
       B. Compute per-account starting balance sum for each month:
          - Create a Map<month_string, number> to hold the aggregated starting balance for each month
          - For each account: if `starting_balance_date` is not null AND `starting_balance > 0`, add `starting_balance` to all months where `month_start >= starting_balance_date`
          - For accounts with `starting_balance_date = null AND starting_balance > 0`: find the earliest transaction month from the aggregation result, treat starting_balance as effective from that month
       C. Fallback: if no accounts have starting_balance > 0, fall back to existing monthly_opening_balances behavior (SELECT * FROM monthly_opening_balances WHERE user_id = ${userId})
       
       **Update the running balance loop** to use per-account aggregated starting balance:
       - Instead of `balanceMap.get(row.month)`, use the computed per-account starting balance map
       - Fall back to monthly_opening_balances map if no per-account data exists
    
    3. Add combined net worth (wartosc_netto) computation:
       
       A. After the main aggregation, fetch asset snapshots:
       ```typescript
       const assetSnapshots = await sql`
         SELECT asset_id, value, date FROM asset_value_snapshots 
         WHERE asset_id IN (SELECT id FROM assets WHERE user_id = ${userId})
         ORDER BY date ASC, created_at ASC
       `;
       ```
       
       B. Build a forward-filled asset value map per month:
       - For each month in the result, compute total asset value by forward-filling from the last snapshot on or before that month's end
       - If no snapshot exists before a month, asset value is 0 for that month
       
       C. Add `wartosc_netto` to each row:
       ```typescript
       return {
         ...row,
         stan_konta: currentRunningBalance.toFixed(4),
         wartosc_netto: (currentRunningBalance + forwardFilledAssetValue).toFixed(4),
       };
       ```
    
    4. Keep all existing fields (wydatki, przychody, fixed_cost_total, etc.) and sorting unchanged.
    
    5. Make the new fields optional for backward compatibility:
       - Return type: append `wartosc_netto?: string` to the existing MonthlySummaryRow
       - If asset snapshots query fails or returns empty, set `wartosc_netto: null`
    
    Follow existing code style (sql tagged templates, const naming, .toFixed(4)).
  </action>
  <verify>
    <automated>grep -c "wartosc_netto" src/core/ledger/use-cases.ts && grep -c "listAccounts" src/core/ledger/use-cases.ts && grep -c "asset_value_snapshots" src/core/ledger/use-cases.ts</automated>
  </verify>
  <acceptance_criteria>
    - getMonthlySummary aggregates per-account starting_balance where starting_balance_date <= month
    - Falls back to monthly_opening_balances if no starting_balances set (D-04 backward compat)
    - Accounts with NULL starting_balance_date and non-zero balance: effective from earliest transaction month (D-05)
    - wartosc_netto computed as stan_konta + forward-filled asset values from snapshots (D-11)
    - Forward-fill: last snapshot value carries forward for months without explicit snapshots (D-12)
    - All existing fields (wydatki, przychody, fixed_cost_total, zaoszczedzone, stan_konta) preserved
    - Existing sorting (most recent month first via toReversed()) preserved
    - monthly_opening_balances data preserved as read-only fallback (D-06)
  </acceptance_criteria>
  <done>getMonthlySummary updated: per-account starting balance aggregation, forward-filled asset snapshots for wartosc_netto, backward-compatible fallback</done>
</task>

<task type="auto">
  <name>Task 5: Update schema.sql with asset_value_snapshots table</name>
  <files>
    src/infrastructure/db/schema.sql
  </files>
  <read_first>
    @/home/olafk/finance/src/infrastructure/db/schema.sql
  </read_first>
  <action>
    Add the asset_value_snapshots table definition to schema.sql:
    ```sql
    CREATE TABLE IF NOT EXISTS asset_value_snapshots (
      id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      asset_id UUID NOT NULL REFERENCES assets(id) ON DELETE CASCADE,
      value NUMERIC(19,4) NOT NULL,
      date DATE NOT NULL,
      notes TEXT,
      created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );
    ```
    Place it after the assets table definition. Match existing formatting conventions.
  </action>
  <verify>
    <automated>grep -q "asset_value_snapshots" src/infrastructure/db/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - schema.sql includes asset_value_snapshots table definition
    - Table definition matches migration 014 exactly
    - Placed after assets table in logical order
    - Formatting matches existing conventions
  </acceptance_criteria>
  <done>schema.sql updated with asset_value_snapshots table definition</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB | Asset snapshots inherit assets table's user_id scoping — owned via FK to assets table which is user-scoped |
| API → User | Snapshot creation is transparent — user edits asset value as normal, history captured automatically (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | snapshot auto-creation | mitigate | updateAsset checks user_id before reading current value — same ownership pattern as existing code |
| T-11-09 | Information Disclosure | snapshot read endpoint | mitigate | GET /assets/:id/snapshots validates asset belongs to current user via getAsset(id, user.id) — 404 if not found |
| T-11-10 | Information Disclosure | getMonthlySummary | mitigate | All queries filtered by user_id from session — no cross-user data leakage |
| T-11-11 | Data Loss | updateAsset + snapshot atomicity | mitigate | Snapshot inserted before UPDATE — if snapshot fails, updateAsset still proceeds (graceful degradation) |
</threat_model>

<verification>
1. Migration 014 creates asset_value_snapshots table with FK and index
2. listAssetSnapshots and createAssetSnapshot functions created
3. updateAsset auto-creates snapshot of old value before overwriting
4. GET /assets/:id/snapshots endpoint created with ownership validation
5. getMonthlySummary updated: per-account starting balance aggregation + forward-filled asset snapshots + backward compat
6. schema.sql updated with new table
7. npx tsc --noEmit passes with no type errors
8. Git commit: `feat(phase-11): add asset snapshots and update balance computation with per-account starting balances`
</verification>

<success_criteria>
- [ ] Migration 014 creates asset_value_snapshots table with FK to assets (D-09)
- [ ] updateAsset auto-creates snapshot of old value on value change (D-10)
- [ ] GET /assets/:id/snapshots returns snapshot history with ownership check
- [ ] getMonthlySummary: baseline = sum of starting_balance where date <= month (D-04)
- [ ] getMonthlySummary: fallback to monthly_opening_balances if no starting_balances set (D-04 legacy compat)
- [ ] getMonthlySummary: NULL starting_balance_date accounts effective from earliest transaction (D-05)
- [ ] getMonthlySummary: wartosc_netto = stan_konta + forward-filled asset values (D-11)
- [ ] Forward-fill: carries last known snapshot value for months without explicit snapshots (D-12)
- [ ] monthly_opening_balances preserved, no new UI (D-06)
- [ ] npx tsc --noEmit passes
</success_criteria>

<output>
Create `.planning/phases/11-account-crud-starting-balances/11-03-SUMMARY.md` when done
</output>
