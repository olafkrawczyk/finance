---
phase: 03-views-categorization
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - frontend/src/components/ZbiorczyTable.tsx
  - frontend/src/components/TransactionTable.tsx
  - frontend/src/components/MonthSidebar.tsx
  - frontend/src/components/CategoryDropdown.tsx
autonomous: true
requirements:
  - REQ-2.1
  - REQ-2.2

must_haves:
  truths:
    - "ZbiorczyTable renders a table with 7 columns: Miesiąc, Wydatki, Przychody, Stan konta, Wydatki bez stałych, Zaoszczędzone, Zaoszcz. log"
    - "TransactionTable renders date, category, description, amount columns with income/expense color coding"
    - "MonthSidebar displays opening balance, income sources by category, and fixed costs breakdown"
    - "CategoryDropdown renders a select element populated with categories from props"
  artifacts:
    - path: "frontend/src/components/ZbiorczyTable.tsx"
      provides: "Monthly summary table rendering"
      min_lines: 40
    - path: "frontend/src/components/TransactionTable.tsx"
      provides: "Reusable transaction list table"
      min_lines: 30
    - path: "frontend/src/components/MonthSidebar.tsx"
      provides: "Monthly view sidebar computations"
      min_lines: 40
    - path: "frontend/src/components/CategoryDropdown.tsx"
      provides: "Shared category selector dropdown"
      min_lines: 25
  key_links:
    - from: "TransactionTable.tsx"
      to: "NormalizedTransaction type"
      via: "props interface"
      pattern: "interface NormalizedTransaction"
    - from: "CategoryDropdown.tsx"
      to: "Category type"
      via: "props interface"
      pattern: "interface Category"
</objective>

# Plan 03: UI Components

<objective>
Create the four shared UI components that all Phase 3 pages compose: ZbiorczyTable (monthly summary table with 7 columns matching budget.xlsx), TransactionTable (reusable transaction list), MonthSidebar (income sources + fixed costs + opening balance), and CategoryDropdown (shared category selector).

Purpose: These are the visual building blocks. Every view page (Dashboard, Zbiorczy, Monthly, Categorize, Add) composes these components. They must follow the dark theme Tailwind patterns, UI-SPEC color/border/typography standards, and data normalization contract.

Output: Four new React components in `frontend/src/components/` with consistent dark-theme Tailwind styling, Polish number formatting, and mobile-responsive layouts per D-08.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-views-categorization/03-RESEARCH.md  (Patterns 2-3, Pitfall 1 — string-to-number, Standard Stack)
@.planning/phases/03-views-categorization/03-PATTERNS.md  (Patterns 12-15 — component skeletons)
@.planning/phases/03-views-categorization/03-UI-SPEC.md   (Color, Typography, Spacing, Component Inventory, Data Normalization Contract, Responsive Behavior)

# Existing components to match styling from:
@frontend/src/components/ImportUpload.tsx    (Tailwind patterns: card layout, form elements, error display, dark theme)
@frontend/src/components/ImportStatus.tsx   (Loading spinner, data display states)
@frontend/src/App.tsx                        (layout pattern, color reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ZbiorczyTable and TransactionTable components</name>
  <files>frontend/src/components/ZbiorczyTable.tsx, frontend/src/components/TransactionTable.tsx</files>
  <read_first>
    - frontend/src/components/ImportUpload.tsx (lines 92-107 — card container styling, error display; lines 110-127 — form element Tailwind classes)
    - frontend/src/components/ImportStatus.tsx (lines 76-83 — loading spinner pattern)
    - .planning/phases/03-views-categorization/03-UI-SPEC.md (Color section — table-specific colors, Typography, Spacing; Data Normalization Contract — Polish number formatting; Copywriting — empty states)
    - .planning/phases/03-views-categorization/03-PATTERNS.md (Pattern 12 — ZbiorczyTable skeleton; Pattern 13 — TransactionTable skeleton)
    - .planning/phases/03-views-categorization/03-RESEARCH.md (Pattern 2 — string-to-number normalization, Pitfall 1)
  </read_first>
  <action>
    Create two new component files in `frontend/src/components/`.

    **ZbiorczyTable.tsx** — Monthly summary table with 7 columns matching budget.xlsx:
    - Props interface `ZbiorczyTableProps`: `{ rows: NormalizedSummaryRow[] }` where `NormalizedSummaryRow` has: `month: string`, `wydatki: number`, `przychody: number`, `stan_konta: number | null`, `wydatki_bez_stalych: number`, `zaoszczedzone: number`, `zaoszczedzone_log: number`
    - Polish number formatter: `new Intl.NumberFormat('pl-PL', { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(n)` — use as `fmt()` helper
    - Table columns (7): Miesiąc (left-aligned, `text-slate-200 font-medium`), Wydatki (right, `text-red-400`), Przychody (right, `text-green-400`), Stan konta (right, `text-blue-400`), Wydatki bez stałych (right), Zaoszczędzone (right), Zaoszcz. log (right, `text-slate-400 font-mono`, `.toFixed(2)`)
    - Container: `overflow-x-auto rounded-xl border border-slate-800` wrapper with `min-w-[700px]` table inside (mobile horizontal scroll per D-08)
    - Table header: `bg-slate-900 text-slate-400 uppercase text-xs`, header row with `<th>` elements
    - Table body: `divide-y divide-slate-800`, rows with `hover:bg-slate-900/50 transition-colors`
    - Null `stan_konta` → render `'—'` (mdash), not "null" or 0
    - Empty rows prop → render empty state: "No transaction data yet" heading + "Import a CSV bank statement to see your monthly summary." body (from UI-SPEC Copywriting > Empty States)
    - Row click handler: clicking a row navigates to `/month/${row.month}` — accept `onRowClick?: (month: string) => void` prop

    **TransactionTable.tsx** — Reusable transaction list table:
    - Props interface `TransactionTableProps`: `{ transactions: NormalizedTransaction[], showCategory?: boolean, showCheckbox?: boolean, selectedIds?: Set<string>, onToggleSelect?: (id: string) => void, onSelectAll?: () => void }` where `NormalizedTransaction` has: `id: string`, `date: string`, `description: string | null`, `category_name: string | null`, `type: 'income' | 'expense' | 'transfer'`, `amount: number`
    - Columns: Date (YYYY-MM-DD format), Category (only if `showCategory` is true), Description, Amount
    - Amount color coding: `text-green-400` for income, `text-red-400` for expense, `text-yellow-400` for transfer
    - When `showCheckbox` is true: add checkbox column with `<input type="checkbox">` per row + select-all in header
    - Polish number formatting on amount (same `fmt()` helper)
    - Same table styling as ZbiorczyTable (container, header, rows, hover)
    - Empty state: "No transactions found" message
    - Null description → render `'—'`

    Both components must:
    - Import React: `import React from 'react'`
    - Export as default function
    - Use the dark theme Tailwind classes from UI-SPEC Color section (NOT arbitrary colors)
    - Use `text-sm` body text, `font-semibold` for labels (per UI-SPEC Typography — NOT font-extrabold)
    - NOT include any data fetching — they are pure presentational components receiving data via props
  </action>
  <acceptance_criteria>
    **ZbiorczyTable:**
    - Renders 7-column table: Miesiąc, Wydatki, Przychody, Stan konta, Wydatki bez stałych, Zaoszczędzone, Zaoszcz. log
    - `overflow-x-auto` container with `min-w-[700px]` table for mobile
    - Polish number formatting with `Intl.NumberFormat('pl-PL', { minimumFractionDigits: 2, maximumFractionDigits: 2 })`
    - Amount colors: text-red-400 (expenses/wydatki), text-green-400 (income/przychody), text-blue-400 (balance/stan konta)
    - Null stan_konta displays '—'
    - Empty props array shows "No transaction data yet" empty state message
    - Hover state on rows: `hover:bg-slate-900/50 transition-colors`
    - NO font-extrabold anywhere in the component

    **TransactionTable:**
    - Shows Date, Description, Amount columns by default
    - Category column shown when `showCategory` prop is true
    - Checkbox column shown when `showCheckbox` prop is true (with select-all)
    - Amounts color-coded: green-400 (income), red-400 (expense), yellow-400 (transfer)
    - Null description displays '—'
    - Empty transactions array shows "No transactions found"
  </acceptance_criteria>
  <verify>
    <automated>test -f frontend/src/components/ZbiorczyTable.tsx && echo "EXISTS"</automated>
    <automated>test -f frontend/src/components/TransactionTable.tsx && echo "EXISTS"</automated>
    <automated>grep -c 'Intl.NumberFormat.*pl-PL' frontend/src/components/ZbiorczyTable.tsx</automated>
    <automated>grep -c 'text-red-400' frontend/src/components/ZbiorczyTable.tsx</automated>
    <automated>grep -c 'text-green-400' frontend/src/components/ZbiorczyTable.tsx</automated>
    <automated>grep -c 'overflow-x-auto' frontend/src/components/ZbiorczyTable.tsx</automated>
    <automated>grep -c "showCheckbox" frontend/src/components/TransactionTable.tsx</automated>
    <automated>grep -c "showCategory" frontend/src/components/TransactionTable.tsx</automated>
    <automated>grep -c 'export default function' frontend/src/components/ZbiorczyTable.tsx</automated>
    <automated>grep -c 'export default function' frontend/src/components/TransactionTable.tsx</automated>
  </verify>
  <done>ZbiorczyTable renders the exact 7-column summary table from budget.xlsx. TransactionTable is a reusable transaction list with optional category column, checkbox selection, and income/expense/transfer color coding.</done>
</task>

<task type="auto">
  <name>Task 2: Create MonthSidebar and CategoryDropdown components</name>
  <files>frontend/src/components/MonthSidebar.tsx, frontend/src/components/CategoryDropdown.tsx</files>
  <read_first>
    - frontend/src/components/ImportUpload.tsx (lines 92-93 — card container; lines 110-127 — select dropdown pattern with label, className, onChange)
    - .planning/phases/03-views-categorization/03-UI-SPEC.md (Color — border colors, text colors; Typography — label/body sizing; Component Inventory — both component specs)
    - .planning/phases/03-views-categorization/03-PATTERNS.md (Pattern 14 — MonthSidebar skeleton; Pattern 15 — CategoryDropdown skeleton)
  </read_first>
  <action>
    Create two new component files.

    **MonthSidebar.tsx** — Monthly view sidebar card:
    - Props interface `MonthSidebarProps`: `{ openingBalance: string | null, incomeByCategory: { category: string; amount: number }[], fixedCostTotal: number, nonFixedExpenses: number }`
    - Card container: `bg-slate-900/80 backdrop-blur-xl border border-slate-800 rounded-2xl p-6` (from ImportUpload card pattern)
    - Three sections, each with heading (`text-sm font-semibold text-slate-400 uppercase mb-3`) + content:
      1. **Opening Balance:** Display `openingBalance` as PLN formatted number, or '—' if null
      2. **Income Sources:** Map over `incomeByCategory`, render `category → amount` rows with `text-green-400` amounts
      3. **Fixed Costs:** Show `fixedCostTotal` as formatted PLN + `nonFixedExpenses` below as "Pozostałe wydatki"
    - Polish number formatting: same `Intl.NumberFormat('pl-PL', { minimumFractionDigits: 2, maximumFractionDigits: 2 })` helper
    - If all values are 0/null → show subtle "No data for this month" text

    **CategoryDropdown.tsx** — Shared category select dropdown:
    - Props interface `CategoryDropdownProps`: `{ categories: Category[], value: string, onChange: (categoryId: string) => void, label?: string, includeUncategorized?: boolean }` where `Category` is `{ id: string; name: string }`
    - Renders a `<label>` (if `label` prop provided, `text-slate-300 text-sm font-semibold mb-2 block`) + `<select>` element
    - Select styling: `w-full bg-slate-950 border border-slate-800 rounded-lg px-4 py-3 text-slate-200 outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500 transition-colors` (exact classes from ImportUpload.tsx select pattern, lines 117)
    - When `includeUncategorized` is true: add `<option value="">— (uncategorized)</option>` as first option
    - Map categories to `<option key={cat.id} value={cat.id}>{cat.name}</option>`
    - Support `id` prop to wire label htmlFor to select id
    - Handle loading: if `categories.length === 0`, show "Loading categories..." in a disabled option

    Both components:
    - Import React: `import React from 'react'`
    - Export as default function
    - Use `text-sm` body, `font-semibold` labels (NOT font-extrabold)
    - Import nothing from `api.ts` — receive all data via props
  </action>
  <acceptance_criteria>
    **MonthSidebar:**
    - Card container with `bg-slate-900/80 border border-slate-800 rounded-2xl p-6`
    - Three labeled sections: Opening Balance, Income Sources, Fixed Costs
    - Income amounts in `text-green-400`
    - Polish number formatting on all monetary values
    - Null opening balance displays '—'
    - Empty state when all values are zero/null

    **CategoryDropdown:**
    - Select element with `w-full bg-slate-950 border border-slate-800 rounded-lg px-4 py-3 text-slate-200`
    - Focus styles: `focus:border-blue-500 focus:ring-1 focus:ring-blue-500`
    - Optional label via `label` prop
    - Optional "— (uncategorized)" option via `includeUncategorized` prop
    - `id` prop for label `htmlFor` binding
    - Empty categories array shows "Loading categories..." disabled option
  </acceptance_criteria>
  <verify>
    <automated>test -f frontend/src/components/MonthSidebar.tsx && echo "EXISTS"</automated>
    <automated>test -f frontend/src/components/CategoryDropdown.tsx && echo "EXISTS"</automated>
    <automated>grep -c "openingBalance" frontend/src/components/MonthSidebar.tsx</automated>
    <automated>grep -c "incomeByCategory" frontend/src/components/MonthSidebar.tsx</automated>
    <automated>grep -c "fixedCostTotal" frontend/src/components/MonthSidebar.tsx</automated>
    <automated>grep -c "bg-slate-900/80" frontend/src/components/MonthSidebar.tsx</automated>
    <automated>grep -c "focus:border-blue-500" frontend/src/components/CategoryDropdown.tsx</automated>
    <automated>grep -c "includeUncategorized" frontend/src/components/CategoryDropdown.tsx</automated>
    <automated>grep -c 'export default function' frontend/src/components/MonthSidebar.tsx</automated>
    <automated>grep -c 'export default function' frontend/src/components/CategoryDropdown.tsx</automated>
  </verify>
  <done>MonthSidebar displays opening balance, income breakdown, and fixed costs for the monthly drill-down view. CategoryDropdown provides a consistent category selector used by CategorizePage and AddTransactionPage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | All four components are pure presentational — no network requests, no user input storage, no authentication. Data arrives via props from parent pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | ZbiorczyTable, TransactionTable | accept | Components render data from props only. React auto-escapes JSX content — no XSS risk from transaction descriptions. No `dangerouslySetInnerHTML` used. |
| T-03-09 | Spoofing | CategoryDropdown | accept | Select element renders option values from server-provided category IDs. No user input is trusted for navigation or auth decisions. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
## Verification Steps

1. Run `npx tsc --noEmit` from project root — all four components must compile without errors
2. Import ZbiorczyTable in a test page, pass `rows={[...]}` — table renders with 7 columns
3. Pass `rows={[]}`  — empty state message displays
4. Import TransactionTable, pass `transactions={[...]}` with different types — amounts color-coded
5. Set `showCategory={true}` — category column appears
6. Set `showCheckbox={true}` — checkboxes render, click toggles selection
7. Import MonthSidebar with sample data — three sections display
8. Import CategoryDropdown with categories array — dropdown renders, selecting fires onChange
</verification>

<success_criteria>
- [ ] All 4 component files exist in `frontend/src/components/`
- [ ] All components use consistent dark theme Tailwind classes from UI-SPEC
- [ ] All monetary values use Polish locale formatting (`Intl.NumberFormat('pl-PL')`)
- [ ] All components use `text-sm` / `font-semibold` (NOT font-extrabold)
- [ ] ZbiorczyTable matches the 7-column layout from budget.xlsx
- [ ] TransactionTable supports optional category column and checkbox selection
- [ ] CategoryDropdown uses exact select styling from ImportUpload.tsx
- [ ] `npx tsc --noEmit` passes with no errors
</success_criteria>

<output>
Create `.planning/phases/03-views-categorization/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `ZbiorczyTable` | React component (default export) | `frontend/src/components/ZbiorczyTable.tsx` | 7-column monthly summary table with Polish formatting |
| `NormalizedSummaryRow` | TypeScript interface | `frontend/src/components/ZbiorczyTable.tsx` | `{ month, wydatki, przychody, stan_konta, wydatki_bez_stalych, zaoszczedzone, zaoszczedzone_log }` |
| `TransactionTable` | React component (default export) | `frontend/src/components/TransactionTable.tsx` | Reusable transaction list with optional category/checkbox columns |
| `NormalizedTransaction` | TypeScript interface | `frontend/src/components/TransactionTable.tsx` | `{ id, date, description, category_name, type, amount }` |
| `TransactionTableProps` | TypeScript interface | `frontend/src/components/TransactionTable.tsx` | Props including showCategory, showCheckbox, selection handlers |
| `MonthSidebar` | React component (default export) | `frontend/src/components/MonthSidebar.tsx` | Monthly sidebar: opening balance + income + fixed costs |
| `MonthSidebarProps` | TypeScript interface | `frontend/src/components/MonthSidebar.tsx` | `{ openingBalance, incomeByCategory, fixedCostTotal, nonFixedExpenses }` |
| `CategoryDropdown` | React component (default export) | `frontend/src/components/CategoryDropdown.tsx` | Shared category select dropdown |
| `CategoryDropdownProps` | TypeScript interface | `frontend/src/components/CategoryDropdown.tsx` | `{ categories, value, onChange, label?, includeUncategorized?, id? }` |
