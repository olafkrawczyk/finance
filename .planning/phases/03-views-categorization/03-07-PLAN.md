---
phase: 03-views-categorization
plan: 07
type: execute
wave: 4
depends_on:
  - 03-05
  - 03-06
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/ImportStatus.tsx
  - frontend/src/components/ImportUpload.tsx
autonomous: true
requirements:
  - REQ-3.1
  - REQ-3.2
  - REQ-3.3
  - REQ-2.3
  - REQ-5.1

must_haves:
  truths:
    - "App.tsx header nav shows 5 nav buttons: Dashboard, Zbiorczy, Categorize, + Add, CSV Ingestion (existing)"
    - "App.tsx routes: / → Dashboard, /zbiorczy → ZbiorczyPage, /month/YYYY-MM → MonthlyPage, /categorize → CategorizePage, /add → AddTransactionPage"
    - "App.tsx layout supports full-width pages (not centered single-card)"
    - "ImportStatus.tsx shows 'Categorize Transactions' button on completed import jobs"
    - "All font-extrabold changed to font-semibold per UI-SPEC Typography contract"
  artifacts:
    - path: "frontend/src/App.tsx"
      contains: "DashboardPage"
    - path: "frontend/src/components/ImportStatus.tsx"
      contains: "Categorize Transactions"
  key_links:
    - from: "App.tsx"
      to: "all 5 page components"
      via: "import statements"
      pattern: "import.*from.*pages"
</objective>

# Plan 07: App Integration + Typography Fix

<objective>
Wire all Phase 3 pages into the client-side router, extend the header navigation bar, update layout for multi-view support, add the post-import "Categorize" button to ImportStatus, and fix typography to align with the UI-SPEC contract (font-extrabold → font-semibold across all components).

Purpose: Without this plan, Phase 3 pages exist as files but are unreachable. The app router must know about them. Navigation must link to them. The post-import flow (D-10) must connect import completion to categorization. Typography must match the design contract.

Output: Updated App.tsx (routes, nav, layout), ImportStatus.tsx (Categorize button + typography fix), ImportUpload.tsx (typography fix only).
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-views-categorization/03-CONTEXT.md   (D-05 header nav, D-06 routes, D-07 dashboard landing, D-10 post-import flow)
@.planning/phases/03-views-categorization/03-PATTERNS.md  (Patterns 6, 8 — App.tsx and ImportStatus exact modifications)
@.planning/phases/03-views-categorization/03-UI-SPEC.md   (Typography — font-extrabold override rule; Copywriting — navigation labels, CTA labels)

# Existing files to modify:
@frontend/src/App.tsx                         (full file — routes, nav, layout, font-extrabold on line 68)
@frontend/src/components/ImportStatus.tsx    (full file — Categorize button addition, font-extrabold on line 124)
@frontend/src/components/ImportUpload.tsx    (full file — font-extrabold on line 95)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend App.tsx with new routes, nav links, layout, and typography fix</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (full file — lines 1-98: imports, route matching, header nav, main layout, navigateTo helper)
    - .planning/phases/03-views-categorization/03-PATTERNS.md (Pattern 6 — exact imports, nav buttons, route matching, layout change)
    - .planning/phases/03-views-categorization/03-UI-SPEC.md (Typography — font-extrabold → font-semibold rule; Copywriting — navigation labels: Dashboard, Zbiorczy, Kategoryzuj, Dodaj, Import CSV)
    - .planning/phases/03-views-categorization/03-CONTEXT.md (D-05 header nav, D-06 routes, D-07 dashboard landing)
  </read_first>
  <action>
    Modify `frontend/src/App.tsx` in four areas:

    **1. Imports (lines 1-3):** Add page component imports after the existing imports:
    - `import DashboardPage from './pages/DashboardPage'`
    - `import ZbiorczyPage from './pages/ZbiorczyPage'`
    - `import MonthlyPage from './pages/MonthlyPage'`
    - `import CategorizePage from './pages/CategorizePage'`
    - `import AddTransactionPage from './pages/AddTransactionPage'`

    **2. Route matching (lines 25-57, `renderContent` function):**
    - Change: `if (currentPath === '/import' || currentPath === '/')` → `if (currentPath === '/import')` (remove `|| currentPath === '/'` — dashboard is now the landing page per D-07)
    - Add BEFORE the `/import/` check (around line 36):
      - Dashboard (D-07): `if (currentPath === '/dashboard' || currentPath === '/') { return <DashboardPage onMonthClick={(month: string) => navigateTo(\`/month/${month}\`)} />; }`
      - Zbiorczy: `if (currentPath === '/zbiorczy') { return <ZbiorczyPage />; }`
      - Monthly (D-06): `if (currentPath.startsWith('/month/')) { const yearMonth = currentPath.substring('/month/'.length); return <MonthlyPage yearMonth={yearMonth} />; }`
      - Categorize: `if (currentPath === '/categorize') { return <CategorizePage />; }`
      - Add (D-11): `if (currentPath === '/add') { return <AddTransactionPage onSuccess={() => navigateTo('/dashboard')} />; }`
    - Keep the existing `/import` and `/import/:jobId` routes
    - Keep the 404 fallback but update the "Go to Imports" button to "Go to Dashboard" with `navigateTo('/dashboard')`

    **3. Header navigation (lines 72-83, `<nav>` block):** D-05 — extend with new nav buttons.
    - Keep the existing "CSV Ingestion" button (update label to "Import CSV" per UI-SPEC Copywriting navigation labels)
    - Add 4 new buttons before the CSV Ingestion button (or after — order: Dashboard, Zbiorczy, Categorize, + Add, Import CSV):
      - **Dashboard:** `navigateTo('/dashboard')`, active when `currentPath === '/dashboard' || currentPath === '/'`, label "Dashboard"
      - **Zbiorczy:** `navigateTo('/zbiorczy')`, active when `currentPath.startsWith('/zbiorczy')`, label "Zbiorczy"
      - **Categorize:** `navigateTo('/categorize')`, active when `currentPath.startsWith('/categorize')`, label "Kategoryzuj" (Polish per UI-SPEC)
      - **+ Add:** `navigateTo('/add')`, active when `currentPath.startsWith('/add')`, label "Dodaj" (Polish per UI-SPEC)
    - Each button uses the EXACT same className pattern as the existing CSV Ingestion button:
      `px-4 py-2 rounded-lg text-sm font-semibold transition-colors` with active state `bg-slate-900 text-blue-400` and inactive state `text-slate-400 hover:text-slate-200`
    - Header nav might overflow on mobile — add `flex-wrap` to the `<nav>` to allow wrapping

    **4. Layout change (line 88):** 
    - Change `<main className="flex-grow flex items-center justify-center px-6 py-12">` to `<main className="flex-grow px-6 py-12 max-w-6xl mx-auto w-full">`
    - This removes centered single-card layout — pages now have full-width content with max-w-6xl constraint

    **5. Typography fix (line 68):**
    - Change `font-extrabold` → `font-semibold` in the FinanceFlow logo span:
      `<span className="text-lg font-semibold tracking-tight text-white">`

    **Route priority:** Place new routes BEFORE the existing `/import` routes. The `/import` check with `currentPath === '/import' || currentPath === '/'` must be split — remove the `'/'` condition.
  </action>
  <acceptance_criteria>
    - All 5 page components imported at top of file
    - Route `/` or `/dashboard` → renders DashboardPage with `onMonthClick` prop
    - Route `/zbiorczy` → renders ZbiorczyPage
    - Route `/month/YYYY-MM` → parses yearMonth from URL, renders MonthlyPage
    - Route `/categorize` → renders CategorizePage
    - Route `/add` → renders AddTransactionPage with `onSuccess` navigating to `/dashboard`
    - Existing `/import` and `/import/:jobId` routes still work
    - 404 fallback navigates to `/dashboard` (not `/import`)
    - Header nav has 5 buttons: Dashboard, Zbiorczy, Kategoryzuj, Dodaj, Import CSV
    - Active route gets `bg-slate-900 text-blue-400` styling
    - Nav uses `flex-wrap` for mobile overflow
    - Main layout changed to `max-w-6xl mx-auto w-full` (removed centering)
    - `font-extrabold` changed to `font-semibold` on line 68
    - `font-extrabold` count in file is 0 after edit
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'DashboardPage' frontend/src/App.tsx</automated>
    <automated>grep -c 'ZbiorczyPage' frontend/src/App.tsx</automated>
    <automated>grep -c 'MonthlyPage' frontend/src/App.tsx</automated>
    <automated>grep -c 'CategorizePage' frontend/src/App.tsx</automated>
    <automated>grep -c 'AddTransactionPage' frontend/src/App.tsx</automated>
    <automated>grep -c "startsWith('/month/')" frontend/src/App.tsx</automated>
    <automated>grep -c 'navigateTo.*dashboard' frontend/src/App.tsx</automated>
    <automated>grep -c 'navigateTo.*zbiorczy' frontend/src/App.tsx</automated>
    <automated>grep -c 'navigateTo.*categorize' frontend/src/App.tsx</automated>
    <automated>grep -c 'navigateTo.*add' frontend/src/App.tsx</automated>
    <automated>grep -c 'Kategoryzuj' frontend/src/App.tsx</automated>
    <automated>grep -c 'Dodaj' frontend/src/App.tsx</automated>
    <automated>grep -c 'max-w-6xl mx-auto w-full' frontend/src/App.tsx</automated>
    <automated>! grep -c 'font-extrabold' frontend/src/App.tsx</automated>
    <automated>! grep -c "items-center justify-center" frontend/src/App.tsx</automated>
  </verify>
  <done>App.tsx routes all 5 new pages, header nav shows all views, layout supports full-width pages, dashboard is landing page per D-07, typography aligned with UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Add Categorize button to ImportStatus + fix typography in ImportStatus and ImportUpload</name>
  <files>frontend/src/components/ImportStatus.tsx, frontend/src/components/ImportUpload.tsx</files>
  <read_first>
    - frontend/src/components/ImportStatus.tsx (full file — lines 1-209: props interface, completed state rendering at lines 121-208, action button at lines 200-205)
    - frontend/src/components/ImportUpload.tsx (full file — line 95 font-extrabold to fix)
    - .planning/phases/03-views-categorization/03-PATTERNS.md (Pattern 8 — ImportStatus exact modifications: prop interface extension, Categorize button JSX)
    - .planning/phases/03-views-categorization/03-UI-SPEC.md (Typography fix; Copywriting — "Categorize Transactions" button label)
    - .planning/phases/03-views-categorization/03-CONTEXT.md (D-10 post-import flow)
  </read_first>
  <action>
    **ImportStatus.tsx modifications:**

    **1. Typography fix (line 124):** Change `<h2 className="text-3xl font-extrabold text-slate-100">Import Status</h2>` to `<h2 className="text-3xl font-semibold text-slate-100">Import Status</h2>` — change `font-extrabold` → `font-semibold` per UI-SPEC Typography contract.

    **2. Props interface (lines 15-18):** Add optional `onCategorize` prop:
    - `interface ImportStatusProps { jobId: string; onBack: () => void; onCategorize?: () => void; }`

    **3. Categorize button (D-10):** Inside the completed state rendering (around line 199, between the error list section and the existing "Back to Imports" button), add:
    - When `job.status === 'completed'` AND `onCategorize` prop is provided:
      ```tsx
      {job.status === 'completed' && onCategorize && (
        <button
          onClick={onCategorize}
          className="w-full py-4 px-6 bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-500 hover:to-teal-500 text-white rounded-xl font-bold transition-all duration-300 active:scale-95 shadow-lg mb-3"
        >
          Categorize Transactions
        </button>
      )}
      ```
    - This button appears ABOVE the existing "Back to Imports" button
    - Uses emerald-to-teal gradient (positive/success action color, distinct from the blue CTA gradient)
    - Matches the button sizing pattern: `w-full py-4 px-6 ... rounded-xl font-bold`

    **ImportUpload.tsx modifications:**

    **4. Typography fix only (line 95):** Change `<h2 className="text-3xl font-extrabold ...">` to `<h2 className="text-3xl font-semibold ...">` — change `font-extrabold` → `font-semibold` per UI-SPEC Typography contract.
    - Do NOT change any other content in ImportUpload.tsx.

    **App.tsx bridge (update the import route to pass onCategorize):**
    - In App.tsx, where ImportStatus is rendered (`/import/:jobId` route), add the `onCategorize` prop:
      `<ImportStatus jobId={jobId} onBack={() => navigateTo('/import')} onCategorize={() => navigateTo('/categorize')} />`
    - This needs to be done in App.tsx — note it in the action so executor handles it. Since App.tsx is modified in Task 1, ensure Task 1 includes this prop or Task 2 adds it.
    - Actual: Add this to Task 1's App.tsx changes. The executor should update the ImportStatus render call in App.tsx to include `onCategorize`.
  </action>
  <acceptance_criteria>
    **ImportStatus.tsx:**
    - `font-extrabold` count is 0 after edit
    - `font-semibold` used on heading
    - `onCategorize` added to props interface (optional)
    - When `job.status === 'completed'` and `onCategorize` is provided, "Categorize Transactions" button renders
    - Button uses emerald-to-teal gradient: `from-emerald-600 to-teal-600`
    - Button appears above "Back to Imports"
    - When `onCategorize` is not provided, button does NOT render (backward compatible)

    **ImportUpload.tsx:**
    - `font-extrabold` count is 0 after edit
    - `font-semibold` used on heading (line 95)

    **App.tsx (Task 1):**
    - ImportStatus render call includes `onCategorize={() => navigateTo('/categorize')}`
  </acceptance_criteria>
  <verify>
    <automated>! grep -c 'font-extrabold' frontend/src/components/ImportStatus.tsx</automated>
    <automated>! grep -c 'font-extrabold' frontend/src/components/ImportUpload.tsx</automated>
    <automated>grep -c 'onCategorize' frontend/src/components/ImportStatus.tsx</automated>
    <automated>grep -c 'Categorize Transactions' frontend/src/components/ImportStatus.tsx</automated>
    <automated>grep -c 'from-emerald-600' frontend/src/components/ImportStatus.tsx</automated>
    <automated>grep -c "onCategorize.*navigateTo.*categorize" frontend/src/App.tsx</automated>
  </verify>
  <done>ImportStatus shows a "Categorize Transactions" button after successful import (D-10). All typography across ImportStatus, ImportUpload, and App.tsx matches UI-SPEC (font-semibold, not font-extrabold).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side router (App.tsx) | URL path changes via `window.history.pushState` + `popstate` listener. All routing is client-side — no server-side redirects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Spoofing | App.tsx client router | accept | Router matches known paths only. Unknown paths fall through to 404 page. `window.history.pushState` only modifies current-origin path — no open redirect risk. |
| T-03-20 | Information Disclosure | ImportStatus Categorize button | accept | Button only appears on completed imports. No sensitive data in button label. Click navigates to `/categorize` — same-origin navigation only. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
## Verification Steps

1. Start frontend dev server — navigate to `/`
2. DashboardPage renders with 4 charts (landing page per D-07) ✓
3. Click "Zbiorczy" in header nav — ZbiorczyPage renders with summary table ✓
4. Click a month row — navigates to `/month/2024-01` → MonthlyPage renders ✓
5. Click "Kategoryzuj" — CategorizePage renders ✓
6. Click "Dodaj" — AddTransactionPage renders ✓
7. Click "Import CSV" — ImportUpload renders ✓
8. Import a CSV, wait for completion → "Categorize Transactions" button appears ✓
9. Click "Categorize Transactions" → navigates to /categorize ✓
10. Verify header nav: active route has `bg-slate-900 text-blue-400` styling ✓
11. Verify typography: all headings use `font-semibold` (not `font-extrabold`) ✓
12. Resize to 375px width — nav wraps, all views adapt ✓
</verification>

<success_criteria>
- [ ] `/` and `/dashboard` render DashboardPage as landing page (D-07)
- [ ] All 5 nav routes work correctly (D-05, D-06)
- [ ] Active route highlighting works in nav bar
- [ ] `/month/YYYY-MM` parses URL params correctly
- [ ] Post-import "Categorize Transactions" button appears in ImportStatus (D-10)
- [ ] Button navigates to `/categorize`
- [ ] Zero `font-extrabold` in App.tsx, ImportStatus.tsx, ImportUpload.tsx
- [ ] Main layout supports full-width pages (no centering)
- [ ] 404 fallback still works
- [ ] Existing import flow still works (/import, /import/:jobId)
</success_criteria>

<output>
Create `.planning/phases/03-views-categorization/03-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `App.tsx` (updated) | React component | `frontend/src/App.tsx` | Extended router with 5 new routes, 5 nav buttons, dashboard landing, full-width layout |
| `ImportStatus` (updated) | React component | `frontend/src/components/ImportStatus.tsx` | Added `onCategorize` prop + "Categorize Transactions" button on completed state |
| `ImportUpload` (updated) | React component | `frontend/src/components/ImportUpload.tsx` | Typography fix only: `font-extrabold` → `font-semibold` |
