---
phase: 04.9-transaction-list-enhancements-reversal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ledger/use-cases.ts
  - tests/ledger.test.ts
autonomous: true
requirements:
  - D-04
must_haves:
  truths:
    - "getMonthlySummary computes running account balances chronologically first, then reverses the final array to return newest month first"
  artifacts:
    - path: "src/core/ledger/use-cases.ts"
      provides: "getMonthlySummary reversing the returned array"
    - path: "tests/ledger.test.ts"
      provides: "Unit test verifying reversed month order but correct chronological balances"
---

<objective>
**Backend Summary Reversal**

This plan reverses the monthly summaries returned by `getMonthlySummary` in the backend use-case so that the newest month is returned first. It validates that the chronological running balance calculations (`stan_konta`) remain mathematically correct and are unaffected by the presentation order.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.9-transaction-list-enhancements-reversal/04.9-CONTEXT.md
@.planning/phases/04.9-transaction-list-enhancements-reversal/04.9-RESEARCH.md
@src/core/ledger/use-cases.ts
@tests/ledger.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update getMonthlySummary Use Case</name>
  <read_first>src/core/ledger/use-cases.ts</read_first>
  <files>
    src/core/ledger/use-cases.ts
  </files>
  <action>
    Modify the return value of `getMonthlySummary` in `src/core/ledger/use-cases.ts`.
    
    1. Keep the chronological aggregation and balance calculations as they are (oldest to newest).
    2. Chain `.reverse()` to the resulting array mapping operation:
       ```typescript
       return agg.map((row: { month: string; wydatki: string; przychody: string; fixed_cost_total: string }) => {
         // ...
       }).reverse();
       ```
  </action>
  <acceptance_criteria>
    - `getMonthlySummary` returns the monthly summaries reversed (newest month first).
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ledger.test.ts</automated>
  </verify>
  <done>
    - Reversed return array in `getMonthlySummary` use-case.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update and Verify Ledger Tests</name>
  <read_first>tests/ledger.test.ts</read_first>
  <files>
    tests/ledger.test.ts
  </files>
  <action>
    Update `tests/ledger.test.ts` to accommodate the reversed order and verify correctness:
    
    1. In the test "calculates monthly summary correctly and excludes transfers", assert that the order is reversed if there are multiple months, or add a dedicated test case that:
       - Seeds transactions and opening balances across two distinct months (e.g. `2026-05` and `2026-06`).
       - Calls `getMonthlySummary()`.
       - Expects the first item in the result to be `2026-06` and the second to be `2026-05`.
       - Verifies that the running balances computed for both months are correct based on chronological propagation (i.e. `2026-06` balance matches `2026-05` balance + `2026-06` savings).
    2. Run all ledger tests to ensure they pass.
  </action>
  <acceptance_criteria>
    - `tests/ledger.test.ts` contains assertions verifying reversed list order with correct mathematical balances.
    - `bun test tests/ledger.test.ts` passes successfully.
  </acceptance_criteria>
  <verify>
    <automated>bun test tests/ledger.test.ts</automated>
  </verify>
  <done>
    - Verified backend summary reversal with unit tests.
  </done>
</task>

</tasks>
