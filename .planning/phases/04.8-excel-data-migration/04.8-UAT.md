---
status: diagnosed
phase: 04.8-excel-data-migration
source: [04.8-01-SUMMARY.md, 04.8-02-SUMMARY.md, 04.8-UI-SPEC.md]
started: 2026-06-07T00:00:00Z
updated: 2026-06-07T00:08:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running server. Start the application from scratch. The dev server boots without errors, the app loads at localhost, and the login page renders correctly.
result: pass

### 2. Access Hidden Migration Route
expected: Type `/migration` in the URL bar. The page navigates to a full-screen migration page with the title "Excel Data Migration" ("Migracja Danych z Excela"), a dashed-border dropzone labeled "Drag and drop budget.xlsx here, or click to browse", and a disabled "Start Ingestion" button.
result: pass

### 3. Invalid File Rejection
expected: Drop or select a non-.xlsx file (e.g. .csv, .pdf). The dropzone border turns red (`border-red-800`) and an inline error message appears: "Only .xlsx Excel workbooks are supported". The file is rejected and "Start Ingestion" remains disabled.
result: pass

### 4. Valid File Selection
expected: Drop or select a valid `.xlsx` file. The dropzone shows the filename, human-readable size (KB/MB), a "Remove File" trash icon, and the "Start Ingestion" button becomes active (not disabled).
result: pass

### 5. Destructive Confirmation Modal
expected: Click "Start Ingestion" with a valid file selected. A modal overlay appears with a red warning panel reading "⚠️ Permanent Data Reset" with a detailed warning about wiping all data. A text input challenges the user to type "MIGRATE" exactly. "Yes, Wipe Data and Migrate" button is disabled until the input matches "MIGRATE". "Cancel" closes the modal and resets the input.
result: pass

### 6. Migration Processing & Progress
expected: After confirming the modal (typing "MIGRATE" and clicking submit), the modal closes. The page transitions to a processing screen showing an animated spinner, "Ingesting Excel Data..." title, the subtext "Please keep this tab open and do not navigate away. This may take up to a minute.", and a gradient progress bar updating every 2 seconds with live row counts.
result: pass

### 7. Migration Success Screen
expected: When the migration completes successfully, polling stops. A success card appears with a green checkmark, "Migration Completed" title, and "Go to Dashboard" button. Clicking "Go to Dashboard" navigates to `/dashboard`.
result: issue
reported: "migration failed with: undefined is not an object (evaluating 'content.split')"
severity: blocker

### 8. Migration Failure + Retry
expected: If the migration fails (or API returns an error), a failure card appears with a red alert icon, "Migration Failed" title, an error message, and a "Try Again" button. Clicking "Try Again" resets the page to the initial dropzone state, clearing the file and errors.
result: pass

## Summary

total: 8
passed: 7
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Migration completes successfully — data is wiped and re-ingested from the Excel workbook"
  status: failed
  reason: "User reported: migration failed with error 'undefined is not an object (evaluating 'content.split')' — worker status shows failed with 0 processed rows"
  severity: blocker
  test: 7
  root_cause: "Running import-worker process has old code (pre-7944db2) without excel_migration routing. The old processJob destructures csv_content from every payload — migration payload has file_path instead, so csv_content is undefined → preprocessIpkoCsv(undefined) → content.split(/\r?\n/) crashes."
  artifacts:
    - path: "src/workers/import-worker.ts"
      issue: "Old processJob (pre-routing) destructures csv_content without type dispatch guard"
  missing:
    - "Restart import-worker after backend code update"
    - "Input validation on csv_content before calling preprocess* functions"
  debug_session: ".planning/debug/migration-content-split-error.md"

- truth: "Worker temp-file cleanup in finally block does not break PGMQ retry"
  status: failed
  reason: "CR-01: Worker deletes uploaded file in finally block, but PGMQ retries (MAX_RETRIES=3) re-read it → every retry fails with ENOENT, masking the real error"
  severity: blocker
  test: 7
  root_cause: "Temp file deletion in finally block (T-MIG-05) happens before PGMQ retries can re-read the file; retry always fails with ENOENT, never reaching the actual processing code"
  artifacts:
    - path: "src/workers/import-worker.ts"
      issue: "finally-block cleanup deletes upload before retry cycle exhausts"
  missing:
    - "Conditional cleanup — only delete when status is 'completed' or after max retries exhausted"
    - "Or preserve upload until all retries consumed"
  debug_session: ""

- truth: "Worker commits in transactional batches, not single transaction — contradicts error UI copy"
  status: failed
  reason: "CR-02: UI claims 'transaction has been rolled back' on failure, but the worker commits in batches of 100 rows (sql.begin per batch). Mid-migration crash can leave partial data committed in an already-truncated DB"
  severity: major
  test: 7
  root_cause: "processExcelMigrationJob wraps individual batch inserts in sql.begin (auto-commit), not a single encompassing transaction. After TRUNCATE in the API route, a mid-migration failure leaves the DB partially populated with no rollback"
  artifacts:
    - path: "src/workers/import-worker.ts"
      issue: "Batch inserts auto-commit independently; no outer transaction wrapping the entire migration"
  missing:
    - "Wrap the entire worker's sheet iteration in a single outer transaction"
    - "Or update UI copy to not claim rollback on failure"
  debug_session: ""

- truth: "Empty/invalid workbooks (0 parseable rows) report 'completed', not 'failed'"
  status: failed
  reason: "CR-03: When a workbook has no parseable rows (empty or all-invalid structure), the worker reports status 'completed' — UI shows 'Migration Completed... data wiped and replaced' even though nothing was imported and original data is gone"
  severity: major
  test: 7
  root_cause: "Worker sets status 'completed' after processing all sheets, even if total parsed transactions = 0. No guard checks whether any data was actually inserted vs. skipped"
  artifacts:
    - path: "src/workers/import-worker.ts"
      issue: "No check for zero inserted rows before marking completed"
  missing:
    - "After batch loop, assert total_inserted > 0 before setting status 'completed'"
    - "If zero rows inserted AND no error, set status 'failed' with 'No parseable data found in workbook'"
  debug_session: ""
