---
phase: 07-backend-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ledger/use-cases.ts
  - src/core/assets/use-cases.ts
  - src/core/import/use-cases.ts
  - src/core/reference/use-cases.ts
autonomous: true
requirements:
  - SCOPE-01
  - SCOPE-02
  - SCOPE-03
  - SCOPE-04
  - SCOPE-05
  - SCOPE-07
user_setup: []

must_haves:
  truths:
    - "All SELECT queries on scoped tables include `AND user_id = ${userId}` — no cross-user data leakage"
    - "All INSERT queries on scoped tables include `user_id` column — data tagged to creating user"
    - "All UPDATE queries on scoped tables include `AND user_id = ${userId}` — no cross-user modification"
    - "All DELETE queries on scoped tables include `AND user_id = ${userId}` — no cross-user deletion"
    - "Ownership validation is implicit via SQL WHERE — non-owned resources return empty result → 404"
    - "PATCH /:id/category inline SQL is extracted to assignCategory() use-case with user_id scoping"
    - "Reference data queries (listAccounts, listCategories) have user_id scoping in dedicated module"
  artifacts:
    - path: src/core/reference/use-cases.ts
      provides: "Scoped reference data queries (listAccounts, listCategories)"
      min_lines: 20
    - path: src/core/ledger/use-cases.ts
      provides: "All 8 ledger use-cases with userId param"
      contains: "userId"
    - path: src/core/assets/use-cases.ts
      provides: "All 4 assets use-cases with userId param"
      contains: "userId"
    - path: src/core/import/use-cases.ts
      provides: "Import use-cases with userId param"
      contains: "userId"
  key_links:
    - from: src/core/ledger/use-cases.ts
      to: "insights pattern"
      pattern: "params object with userId"
    - from: src/core/reference/use-cases.ts
      to: src/interface-adapters/api/reference.ts
      pattern: "export async function listAccounts|listCategories"
---

<objective>
Scope all use-case functions with `userId` parameter — every SELECT, INSERT, UPDATE, DELETE on accounts, categories, transactions, monthly_opening_balances, assets, and import_jobs includes `user_id` filtering/tagging. Create new `src/core/reference/use-cases.ts` with scoped reference queries. Extract `assignCategory` from inline PATCH route SQL into a scoped use-case.

Purpose: Core isolation layer — all route handlers and upstream consumers depend on use-cases being correctly scoped. This is the foundation plan.
Output: Four use-case files with per-user scoping, reference module, and extracted assignCategory.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/REQUIREMENTS.md
@.planning/phases/07-backend-scoping/07-CONTEXT.md
@.planning/phases/07-backend-scoping/07-RESEARCH.md

@src/core/insights/use-cases.ts
@src/core/ledger/use-cases.ts
@src/core/assets/use-cases.ts
@src/core/import/use-cases.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope ledger use-cases with userId param</name>
  <files>src/core/ledger/use-cases.ts</files>
  <read_first>
    @src/core/ledger/use-cases.ts
    @src/core/insights/use-cases.ts
    @.planning/phases/07-backend-scoping/07-RESEARCH.md
  </read_first>
  <action>
    Add `userId: string` to the params object of every function in `src/core/ledger/use-cases.ts`. Per D-01 (params object pattern) and D-02 (implicit WHERE → 404).

    Target 8 functions, following insights module pattern exactly:

    1. **createTransaction** — Add `userId: string` to params object. Insert includes `user_id` column set to `input.userId`. Per SCOPE-02.
       Signature: `createTransaction(input: CreateTransactionInput & { userId: string })`
       Change: The INSERT adds `user_id, ${input.userId}` after `transfer_to_account_id` in the column list.

    2. **listTransactions** — Add `userId: string` to params object. Add `AND user_id = ${userId}` to both SELECT and COUNT queries, after the `WHERE true` line and before the filter conditions. Per SCOPE-01.
       Signature: `listTransactions(params: { userId: string; page: number; per_page: number; account_id?: string; ... })`

    3. **getMonthlySummary** — Add `userId` as a positional param: `getMonthlySummary(userId: string)`. Add `AND t.user_id = ${userId}` to the aggregation query WHERE clause (after `WHERE t.type != 'transfer'`). Add `AND user_id = ${userId}` to the opening_balances subquery. Per SCOPE-01 — this is a critical gap (Pitfall 1 from research).

    4. **createOpeningBalance** — Add `userId: string` to params object. Insert includes `user_id` column. Per SCOPE-02.
       Signature: `createOpeningBalance(input: CreateOpeningBalanceInput & { userId: string })`

    5. **updateOpeningBalance** — Add `userId: string` as 3rd positional param after `id, input`. Add `AND user_id = ${userId}` to the UPDATE WHERE clause. Per SCOPE-03.
       Signature: `updateOpeningBalance(id: string, input: UpdateOpeningBalanceInput, userId: string): Promise<...>`

    6. **getTransaction** — Add `userId: string` as 2nd positional param. Add `AND user_id = ${userId}` to the SELECT WHERE clause. Per SCOPE-01.
       Signature: `getTransaction(id: string, userId: string): Promise<Transaction | undefined>`

    7. **updateTransaction** — Add `userId: string` as 3rd positional param after `id, input`. Add `AND user_id = ${userId}` to the UPDATE WHERE clause (after `WHERE id = ${id}`). Per SCOPE-03.
       Signature: `updateTransaction(id: string, input: UpdateTransactionInput, userId: string): Promise<Transaction>`

    8. **deleteTransaction** — Add `userId: string` as 2nd positional param. Add `AND user_id = ${userId}` to all three sub-operations (import_hash UPDATE, insights UPDATE, and hard DELETE). Per SCOPE-04.
       Signature: `deleteTransaction(id: string, userId: string): Promise<void>`
       Change: Each SQL operation in the `sql.begin` block must filter by `WHERE user_id = ${userId}`.

    9. **listOpeningBalances** — Add `userId: string` to the optional `params` object: `listOpeningBalances(params?: { userId?: string; year?: number; month?: number })`. Add `AND user_id = ${userId}` (or skip when not provided). Per SCOPE-01.
       Signature: `listOpeningBalances(params?: { userId?: string; year?: number; month?: number })`

    10. **NEW: assignCategory** — Extract from inline SQL in `src/interface-adapters/api/ledger.ts` lines 185-189. Create new exported function. Per SCOPE-07 and D-02.
        Signature: `export async function assignCategory(transactionId: string, categoryId: string, userId: string): Promise<Transaction | null>`
        SQL:
        ```sql
        UPDATE transactions SET category_id = ${categoryId}
        WHERE id = ${transactionId}
          AND category_id IS NULL
          AND user_id = ${userId}
        RETURNING *
        ```
        Return `(updated as Transaction) || null` — null triggers 404 in caller.

    Use `userId` property in params objects for functions that already use params (`listTransactions`, `createTransaction`). For simple functions, use positional `userId: string` arg consistent with `dismissInsight(id, userId)` pattern from insights module.

    **What to avoid:**
    - Do NOT add `user_id` to Zod schemas — it comes from session, not client input (SCOPE-06).
    - Do NOT add explicit ownership validation helpers — implicit WHERE per D-02.
    - Do NOT use a different naming convention — use `userId` consistently with insights module.
  </action>
  <verify>
    <automated>
      bun build src/core/ledger/use-cases.ts 2>&1 | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `createTransaction` inserts include `user_id` column
    - `listTransactions` SELECT has `AND user_id = ${userId}` in both rows and count queries
    - `getMonthlySummary` aggregation has `AND t.user_id = ${userId}` and opening_balances subquery has `AND user_id = ${userId}`
    - `getTransaction` has `AND user_id = ${userId}` in WHERE
    - `updateTransaction` has `AND user_id = ${userId}` in UPDATE WHERE
    - `deleteTransaction` has `AND user_id = ${userId}` in all three sub-operations
    - `updateOpeningBalance` has `AND user_id = ${userId}` in UPDATE WHERE
    - `listOpeningBalances` has `AND user_id = ${userId}` option
    - `assignCategory` is a new exported function with user_id scoping
    - All functions compile without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scope assets use-cases with userId param</name>
  <files>src/core/assets/use-cases.ts</files>
  <read_first>
    @src/core/assets/use-cases.ts
    @src/core/insights/use-cases.ts
  </read_first>
  <action>
    Add `userId: string` parameter to all 4 functions in `src/core/assets/use-cases.ts`. Per D-01 (params object pattern) and D-02 (implicit WHERE → 404).

    1. **listAssets** — Add `userId: string` param. Add `WHERE user_id = ${userId}` to SELECT. Per SCOPE-01.
       Signature: `export async function listAssets(userId: string): Promise<Asset[]>`
       SQL: `SELECT * FROM assets WHERE user_id = ${userId} ORDER BY name ASC`

    2. **createAsset** — Add `userId: string` as 3rd param after `name, value`. Add `user_id` column to INSERT. Per SCOPE-02.
       Signature: `export async function createAsset(name: string, value: number, userId: string): Promise<Asset>`
       SQL: `INSERT INTO assets (name, value, user_id) VALUES (${name}, ${value}, ${userId}) RETURNING *`

    3. **updateAsset** — Add `userId: string` as 4th param after `id, name, value`. Add `AND user_id = ${userId}` to UPDATE WHERE. Per SCOPE-03.
       Signature: `export async function updateAsset(id: string, name: string, value: number, userId: string): Promise<Asset | undefined>`
       SQL: `UPDATE assets SET name = ${name}, value = ${value} WHERE id = ${id} AND user_id = ${userId} RETURNING *`

    4. **deleteAsset** — Add `userId: string` as 2nd param after `id`. Add `AND user_id = ${userId}` to DELETE WHERE. Per SCOPE-04.
       Signature: `export async function deleteAsset(id: string, userId: string): Promise<void>`
       SQL: `DELETE FROM assets WHERE id = ${id} AND user_id = ${userId}`

    **What to avoid:**
    - Do NOT use params object for assets — these are simple positional params (different from ledger pattern where params objects already exist). Keep consistent with the existing positional style of the assets module.
    - Do NOT add user_id to Asset interface — it's already in the schema but doesn't need to be returned differently.
  </action>
  <verify>
    <automated>
      bun build src/core/assets/use-cases.ts 2>&1 | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `listAssets(userId)` adds `WHERE user_id = ${userId}`
    - `createAsset(name, value, userId)` adds `user_id` to INSERT
    - `updateAsset(id, name, value, userId)` adds `AND user_id = ${userId}` to UPDATE
    - `deleteAsset(id, userId)` adds `AND user_id = ${userId}` to DELETE
    - All functions compile without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Scope import use-cases + create reference/use-cases.ts</name>
  <files>src/core/import/use-cases.ts, src/core/reference/use-cases.ts</files>
  <read_first>
    @src/core/import/use-cases.ts
    @src/interface-adapters/api/reference.ts
    @.planning/phases/07-backend-scoping/07-RESEARCH.md
  </read_first>
  <action>
    **Part A: Scope import use-cases (src/core/import/use-cases.ts)**

    1. **enqueueImportJob** — Add `userId: string` to params object. Add `user_id` to INSERT INTO import_jobs column list set to `payload.userId`. Add `user_id: payload.userId` to the PGMQ JSON message payload. Per D-05 (Phase 7 scopes enqueue side, worker enforcement in Phase 8).
       Signature:
       ```
       export async function enqueueImportJob(payload: {
         account_id: string;
         csv_content: string;
         bank_format: 'ing' | 'ipko';
         userId: string;  // NEW
       }): Promise<{ job_id: string; msg_id: number }>
       ```
       INSERT change: `INSERT INTO import_jobs (account_id, user_id, status) VALUES (${payload.account_id}, ${payload.userId}, 'pending')`
       PGMQ payload change: Add `user_id: payload.userId` to the JSON object after `bank_format`.

    2. **getImportStatus** — Add `userId: string` as 2nd param after `jobId`. Add `AND user_id = ${userId}` to SELECT WHERE. Per SCOPE-01.
       Signature: `export async function getImportStatus(jobId: string, userId: string): Promise<ImportJob | undefined>`
       SQL: `SELECT * FROM import_jobs WHERE id = ${jobId} AND user_id = ${userId}`

    **Part B: Create reference/use-cases.ts (NEW — per D-04)**

    Create `src/core/reference/use-cases.ts` with two exported functions extracted from the inline SQL in `src/interface-adapters/api/reference.ts`. Import `sql` from `../../infrastructure/db/client`.

    1. **listAccounts(userId: string)**
       ```typescript
       import sql from '../../infrastructure/db/client';

       export async function listAccounts(userId: string) {
         const rows = await sql`
           SELECT * FROM accounts WHERE user_id = ${userId} ORDER BY name
         `;
         return rows;
       }
       ```

    2. **listCategories(userId: string)**
       ```typescript
       export async function listCategories(userId: string) {
         const rows = await sql`
           SELECT * FROM categories WHERE user_id = ${userId} ORDER BY name
         `;
         return rows;
       }
       ```

    **What to avoid:**
    - Do NOT add a separate type file for reference — types are simple enough to infer from the query
    - Do NOT make this file a class or object — keep as standalone async functions consistent with all other use-case modules
    - For import: do NOT modify the worker processing functions (processJob, processCsvImportJob) — they are Phase 8 scope per D-05
  </action>
  <verify>
    <automated>
      bun build src/core/import/use-cases.ts 2>&1 | head -5 && bun build src/core/reference/use-cases.ts 2>&1 | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `enqueueImportJob` adds `user_id` to import_jobs INSERT and PGMQ payload
    - `getImportStatus(jobId, userId)` has `AND user_id = ${userId}`
    - `src/core/reference/use-cases.ts` exists with `listAccounts(userId)` and `listCategories(userId)` both scoped by user_id
    - Both files compile without errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | All HTTP requests cross an untrusted network boundary. Session cookie is the trust anchor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | ledger/use-cases.ts SELECTs | mitigate | `AND user_id = ${userId}` on every SELECT — getMonthlySummary is the critical gap (Pitfall 1) |
| T-07-02 | Tampering | ledger/use-cases.ts UPDATEs/DELETEs | mitigate | `AND user_id = ${userId}` on every UPDATE and DELETE — non-owned resources return empty → 404 |
| T-07-03 | Spoofing | INSERT use-cases | mitigate | `user_id` sourced from params object (originating from session), never from Zod schema |
| T-07-04 | Elevation of Privilege | assets, import, reference use-cases | mitigate | Implicit WHERE-based ownership → 404 (consistent with D-02, TEST-02) |
| T-07-05 | Information Disclosure | reference/use-cases.ts | mitigate | `WHERE user_id = ${userId}` on listAccounts and listCategories |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan — all work uses existing dependencies |
</threat_model>

<verification>
After all 3 tasks complete:
1. Run `grep -c 'user_id' src/core/ledger/use-cases.ts` — expect at least 12 occurrences (each scoped table query adds one)
2. Run `grep -c 'user_id' src/core/assets/use-cases.ts` — expect at least 4 occurrences
3. Run `grep -c 'user_id' src/core/import/use-cases.ts` — expect at least 2 occurrences
4. Run `bun build src/core/ledger/use-cases.ts src/core/assets/use-cases.ts src/core/import/use-cases.ts src/core/reference/use-cases.ts` — exit code 0
</verification>

<success_criteria>
- All 10 use-case functions in ledger module have userId scoping
- All 4 asset functions have userId scoping
- Both import functions have userId scoping
- `assignCategory` function extracted with user_id scoping
- `src/core/reference/use-cases.ts` created with scoped listAccounts and listCategories
- All 4 files compile without TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/07-backend-scoping/07-01-SUMMARY.md` when done
</output>
