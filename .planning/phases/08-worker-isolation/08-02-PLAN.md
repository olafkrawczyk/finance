---
phase: 08-worker-isolation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/insights/use-cases.ts
  - src/workers/insights-worker.ts
  - src/core/ledger/use-cases.ts
  - src/core/import/entities.ts
autonomous: true
requirements: [WORKER-01, WORKER-04]

must_haves:
  truths:
    - "getLatestTransactionDate accepts userId and filters by user_id — no cross-user date skew"
    - "getInsightDataWindow WHERE clause includes AND t.user_id = ${userId} — data is scoped per user"
    - "getCategoryAggregates accepts userId and passes it to getLatestTransactionDate for scoped anchor"
    - "createTransaction auto-trigger includes user_id in PGMQ analysis_queue payload"
    - "ImportJob entity reflects the user_id column that exists in the database schema"
  artifacts:
    - path: "src/core/insights/use-cases.ts"
      provides: "Scoped insights queries"
      min_lines: 180
    - path: "src/core/ledger/use-cases.ts"
      provides: "Fixed auto-trigger payload"
      contains: "user_id: input.userId"
    - path: "src/core/import/entities.ts"
      provides: "ImportJob with user_id field"
      contains: "user_id: string"
  key_links:
    - from: "getInsightDataWindow"
      to: "getLatestTransactionDate"
      via: "userId parameter"
      pattern: "getLatestTransactionDate\\(userId\\)"
    - from: "getInsightDataWindow"
      to: "transactions WHERE"
      via: "user_id filter"
      pattern: "AND t\\.user_id = \\$\\{userId\\}"
    - from: "createTransaction"
      to: "analysis_queue PGMQ"
      via: "user_id in payload"
      pattern: "user_id: input\\.userId"
---

<objective>
**Worker-04: Insights Worker Explicit user_id Scoping + createTransaction Auto-Trigger Fix**

**Purpose:** The insights worker's `getInsightDataWindow` already accepts a `userId` parameter but its SQL WHERE clause ignores it — this is a regression bug. `getLatestTransactionDate` returns max date across ALL users, causing cross-user date-window skew. The `createTransaction` auto-trigger sends `{ transaction_id }` without `user_id`, which causes the insights worker to throw. All three issues are fixed in this plan.

**Output:** Modified `src/core/insights/use-cases.ts`, `src/workers/insights-worker.ts`, `src/core/ledger/use-cases.ts`, `src/core/import/entities.ts`
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-worker-isolation/08-CONTEXT.md
@.planning/phases/08-worker-isolation/08-RESEARCH.md
@.planning/phases/08-worker-isolation/08-PATTERNS.md
@src/core/insights/use-cases.ts
@src/workers/insights-worker.ts
@src/core/ledger/use-cases.ts
@src/core/import/entities.ts
@src/core/insights/entities.ts
</context>

<tasks>

<task type="auto" tdd="false">
<name>Task 1: Fix getLatestTransactionDate and getInsightDataWindow SQL WHERE clauses (D-09, D-10)</name>
<files>src/core/insights/use-cases.ts</files>

<read_first>
- @src/core/insights/use-cases.ts lines 5-25 (getLatestTransactionDate + getInsightDataWindow)
- @.planning/phases/08-worker-isolation/08-RESEARCH.md lines 410-457 (patterns for both fixes)
- @.planning/phases/08-worker-isolation/08-PATTERNS.md lines 269-301 (pattern for scoped queries)
</read_first>

<action>
Read `src/core/insights/use-cases.ts` before making changes.

**A. getLatestTransactionDate (lines 5-8, D-10):**
Add `userId: string` parameter and `WHERE user_id = ${userId}` to the SQL query:
```typescript
async function getLatestTransactionDate(userId: string): Promise<string> {
  const [row] = await sql`
    SELECT MAX(date)::text AS latest FROM transactions WHERE user_id = ${userId}
  `;
  return row?.latest ?? new Date().toISOString().slice(0, 10);
}
```

**B. getInsightDataWindow (lines 12-25, D-09):**
The `userId` parameter already exists on line 12. Two changes needed:
1. Line 13: Change `const anchor = await getLatestTransactionDate()` to `const anchor = await getLatestTransactionDate(userId)` — pass userId to scope the anchor.
2. Add `AND t.user_id = ${userId}` to the SQL WHERE clause. The current WHERE has two conditions OR'd together. Wrap them in parentheses and add the user_id filter as a third AND condition:
   ```typescript
   WHERE (t.date >= (${anchor}::date - interval '3 months')
      OR (EXTRACT(MONTH FROM t.date) = EXTRACT(MONTH FROM ${anchor}::date - interval '12 months')
          AND t.date >= ${anchor}::date - interval '15 months'
          AND t.date < ${anchor}::date - interval '11 months'))
     AND t.user_id = ${userId}
   ```
</action>

<verify>
<automated>bun build src/core/insights/use-cases.ts 2>&1; echo "---"; grep -c 'AND t.user_id = ' src/core/insights/use-cases.ts</automated>
</verify>

<acceptance_criteria>
- `getLatestTransactionDate` signature: `async function getLatestTransactionDate(userId: string): Promise<string>`
- SQL in `getLatestTransactionDate` includes `WHERE user_id = ${userId}`
- `getInsightDataWindow` calls `getLatestTransactionDate(userId)` (passing userId)
- SQL in `getInsightDataWindow` includes `AND t.user_id = ${userId}` as an additional WHERE condition
- Both parentheses around the date conditions are preserved correctly
- All other callers of `getLatestTransactionDate` have been updated (if any exist outside of `getInsightDataWindow` and `getCategoryAggregates`, they must be fixed — see Task 2)
</acceptance_criteria>
</task>

<task type="auto" tdd="false">
<name>Task 2: Fix getCategoryAggregates anchor + createTransaction auto-trigger (D-04, D-09 propagation)</name>
<files>src/core/insights/use-cases.ts, src/workers/insights-worker.ts, src/core/ledger/use-cases.ts</files>

<read_first>
- @src/core/insights/use-cases.ts lines 28-79 (getCategoryAggregates — calls getLatestTransactionDate on line 31)
- @src/workers/insights-worker.ts line 402 (getCategoryAggregates call site)
- @src/core/ledger/use-cases.ts lines 11-26 (createTransaction with auto-trigger on line 22)
- @.planning/phases/08-worker-isolation/08-RESEARCH.md lines 428-457 (getInsightDataWindow scoping pattern)
- @.planning/phases/08-worker-isolation/08-PATTERNS.md lines 376-391 (createTransaction auto-trigger fix)
</read_first>

<action>
Read all three source files before making changes.

**A. getCategoryAggregates (src/core/insights/use-cases.ts, line 28):**
Add `userId: string` as a 2nd parameter after `transactionIds`:
```typescript
export async function getCategoryAggregates(transactionIds: string[], userId: string): Promise<CategoryAggregate[]> {
```
Change the `getLatestTransactionDate()` call on line 31 to pass userId:
```typescript
const anchor = await getLatestTransactionDate(userId);
```

**B. insights-worker.ts call site (line 402):**
Change the call from `getCategoryAggregates(txIds)` to `getCategoryAggregates(txIds, userId)`.
The `userId` variable is already in scope at line 402 (extracted from payload at line 362). No imports needed.

**C. createTransaction auto-trigger (src/core/ledger/use-cases.ts, line 22):**
Change the PGMQ payload to include `user_id: input.userId`. The `transaction_id` must be preserved:
```typescript
await sql`SELECT pgmq.send('analysis_queue', ${JSON.stringify({
  transaction_id: tx.id,
  user_id: input.userId,
})}::jsonb)`;
```
This fixes the bug where auto-triggered analysis from manual transaction creation sends a payload without `user_id`, which causes `processAnalysisMessage` to throw `Error('Message payload missing user_id')`.
</action>

<verify>
<automated>bun build src/core/insights/use-cases.ts src/workers/insights-worker.ts src/core/ledger/use-cases.ts 2>&1</automated>
</verify>

<acceptance_criteria>
- `getCategoryAggregates` signature: `(transactionIds: string[], userId: string): Promise<CategoryAggregate[]>`
- `getCategoryAggregates` calls `getLatestTransactionDate(userId)` (not the no-arg version)
- `insights-worker.ts` line 402 passes `userId` to `getCategoryAggregates`
- `createTransaction` PGMQ payload includes `user_id: input.userId` alongside `transaction_id: tx.id`
- Compilation passes for all three files
</acceptance_criteria>
</task>

<task type="auto" tdd="false">
<name>Task 3: Add user_id field to ImportJob entity interface</name>
<files>src/core/import/entities.ts</files>

<read_first>
- @src/core/import/entities.ts lines 4-13 (current ImportJob interface)
- @src/core/insights/entities.ts lines 7-19 (analog — Insight entity already has user_id field)
- @src/infrastructure/db/migrations/009_update_uniques.sql (confirm table has user_id)
</read_first>

<action>
Read `src/core/import/entities.ts` before making changes.

Add `user_id: string;` field to the `ImportJob` interface (line 4), placed after `account_id`:
```typescript
export interface ImportJob {
  id: string;                          // UUID
  account_id: string;
  user_id: string;                     // NEW — reflects DB schema (user_id TEXT NOT NULL)
  status: 'pending' | 'processing' | 'completed' | 'failed';
  total_rows: number | null;
  processed: number;
  errors: string[] | null;
  created_at: string;                  // TIMESTAMPTZ as ISO string
  updated_at: string;                  // TIMESTAMPTZ as ISO string
}
```

This aligns the TypeScript type with the database schema — the `import_jobs` table already has `user_id TEXT NOT NULL` from migration 008, and queries like `SELECT * FROM import_jobs` return it. The existing `getImportStatus(jobId, userId)` already casts `row as ImportJob` and works with the extra field due to structural typing. Adding the field explicitly prevents confusion and type drift.
</action>

<verify>
<automated>bun build src/core/import/entities.ts 2>&1</automated>
</verify>

<acceptance_criteria>
- `ImportJob` interface has `user_id: string;` field after `account_id`
- No TypeScript compilation errors in `src/core/import/entities.ts`
- Existing consumers of `ImportJob` (like `getImportStatus`) do not break
</acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

### Modified Functions
- `getLatestTransactionDate(userId: string)` — new `userId` parameter, scoped SQL WHERE
- `getInsightDataWindow(userId)` — existing param now actually used in SQL WHERE and passed to `getLatestTransactionDate`
- `getCategoryAggregates(transactionIds, userId)` — new `userId` parameter (2nd param), scoped anchor date
- `createTransaction(input)` — PGMQ payload now includes `user_id: input.userId`

### Modified Types
- `ImportJob.user_id: string` — new field on the entity interface

### Modified Behaviors
- `insights-worker.ts` `getCategoryAggregates(txIds, userId)` call now passes userId for scoped anchor
- `createTransaction` auto-trigger no longer throws in `processAnalysisMessage` — payload now carries `user_id`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| createTransaction → analysis_queue | PGMQ message enqueued from ledger use-case — must carry user_id for insights worker to scope correctly |
| insights use-cases → DB | SQL queries returning transaction/aggregate data must be scoped per user_id |
| insights-worker → getCategoryAggregates | Worker calls getCategoryAggregates with transaction data — must pass userId for scoped anchor date |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | `getInsightDataWindow` — no user_id WHERE filter | mitigate | D-09: add `AND t.user_id = ${userId}` to WHERE clause |
| T-08-07 | Information Disclosure | `getLatestTransactionDate` — global max date across all users | mitigate | D-10: add `WHERE user_id = ${userId}` to the query |
| T-08-08 | Tampering | `createTransaction` auto-trigger payload missing user_id | mitigate | Add `user_id: input.userId` to PGMQ payload — worker will have user context to scope properly |
| T-08-09 | Information Disclosure | `getCategoryAggregates` anchor date from unscoped getLatestTransactionDate | mitigate | Add `userId` param, pass to `getLatestTransactionDate(userId)` for per-user anchor |
</threat_model>

<verification>
- `grep -n 'function getLatestTransactionDate' src/core/insights/use-cases.ts` returns line with `userId: string` param
- `grep -n 'AND t\\.user_id = ' src/core/insights/use-cases.ts` returns at least 1 occurrence
- `grep -n 'user_id: input\\.userId' src/core/ledger/use-cases.ts` returns 1 occurrence
- `grep -n 'user_id: string' src/core/import/entities.ts` returns 1 occurrence
- `bun build src/core/insights/use-cases.ts src/workers/insights-worker.ts src/core/ledger/use-cases.ts src/core/import/entities.ts` exits 0
</verification>

<success_criteria>
All tasks completed:
- [ ] `getLatestTransactionDate` takes `userId` and scopes by it
- [ ] `getInsightDataWindow` AND-condition filters by `t.user_id = ${userId}`
- [ ] `getCategoryAggregates` takes `userId` and passes it for scoped anchor
- [ ] `insights-worker.ts` passes `userId` to `getCategoryAggregates`
- [ ] `createTransaction` PGMQ payload includes `user_id: input.userId`
- [ ] `ImportJob` interface has `user_id` field
</success_criteria>

<output>
Create `.planning/phases/08-worker-isolation/08-02-SUMMARY.md` after execution documenting insights scoping, createTransaction fix, and entity update.
</output>
