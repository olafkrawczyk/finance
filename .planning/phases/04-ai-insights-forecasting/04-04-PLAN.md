---
phase: 04-ai-insights-forecasting
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-03
files_modified:
  - frontend/src/api.ts
  - frontend/src/lib/insights.ts
  - frontend/src/components/InsightsWidget.tsx
  - frontend/src/charts/ComboChart.tsx
  - frontend/src/pages/DashboardPage.tsx
  - tests/ui-components.test.ts
autonomous: true
requirements:
  - D-08
  - D-16

must_haves:
  truths:
    - "The dashboard shows 3 compact insight cards above the existing 4 charts"
    - "Each dashboard insight card shows: priority icon (colored dot), first line of insight text, relative timestamp"
    - "The ComboChart renders a second prediction line (AI forecast) in cyan with short dashes, distinct from the amber LR line"
    - "API client functions exist for all insight endpoints: getInsights, dismissInsight, generateInsights, getInsightsForecast"
    - "Insight formatting utilities handle relative timestamps, priority colors, and type labels in Polish"
  artifacts:
    - path: "frontend/src/api.ts"
      provides: "New API client functions: getInsights, dismissInsight, generateInsights, getInsightsForecast"
    - path: "frontend/src/lib/insights.ts"
      provides: "Formatting utilities: formatRelativeTime, getPriorityColor, getTypeIcon, getTypeLabel"
    - path: "frontend/src/components/InsightsWidget.tsx"
      provides: "Compact 3-card dashboard widget rendering undismissed insights"
    - path: "frontend/src/charts/ComboChart.tsx"
      provides: "Extended chart with dual prediction lines (LR + AI forecast)"
  key_links:
    - from: "frontend/src/components/InsightsWidget.tsx"
      to: "GET /insights/dashboard"
      via: "getInsights({ per_page: 3, dismissed: false })"
      pattern: "getInsights"
    - from: "frontend/src/charts/ComboChart.tsx"
      to: "aiForecast dataKey"
      via: "Line stroke=#06b6d4 strokeDasharray=4 4"
      pattern: "aiForecast"
---

<objective>
Extend the frontend with the dashboard insight widget, AI forecast line on the ComboChart, API client functions for all insight endpoints, and insight formatting utilities. This delivers the compact dashboard widget (per D-08) and dual prediction lines (per D-16).

Purpose: Make AI insights visible on the main dashboard and provide the API client layer that all frontend insight components use.
Output: Extended api.ts, new lib/insights.ts utility, new InsightsWidget component, extended ComboChart, modified DashboardPage.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-insights-forecasting/04-CONTEXT.md
@.planning/phases/04-ai-insights-forecasting/04-RESEARCH.md
@.planning/phases/04-ai-insights-forecasting/04-PATTERNS.md
@.planning/phases/04-ai-insights-forecasting/04-UI-SPEC.md

# Source-of-truth files for patterns
@frontend/src/api.ts
@frontend/src/lib/linearRegression.ts
@frontend/src/charts/ComboChart.tsx
@frontend/src/pages/DashboardPage.tsx
@frontend/src/components/ImportStatus.tsx
@frontend/src/components/ImportUpload.tsx
@tests/ui-components.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add insight API client functions and formatting utilities</name>
  <files>
    frontend/src/api.ts
    frontend/src/lib/insights.ts
  </files>
  <read_first>
    - frontend/src/api.ts — existing fetch pattern: credentials: 'include', error handling with res.json().catch(()=>({})), envelope unwrapping (json.data), query param building with URLSearchParams
    - frontend/src/lib/linearRegression.ts — utility file pattern: single-file module exporting pure functions
  </read_first>
  <action>
    **Extend frontend/src/api.ts with 4 new functions** (append to file, following existing patterns exactly):

    1. **getInsights(params?)**: Build URLSearchParams from optional { type?, dismissed?, page?, per_page? } (follow existing getTransactions() pattern at lines 63-75). GET /insights with credentials: 'include'. Return { data: json.data, meta: json.meta }.

    2. **dismissInsight(insightId: string)**: PATCH /insights/{insightId}/dismiss with body JSON.stringify({ dismissed: true }) and Content-Type header (follow existing assignCategory() pattern at lines 109-122). Return json.data on success. Throw with errorJson.error?.message on failure.

    3. **generateInsights()**: POST /insights/generate with empty body, Content-Type header, credentials: 'include' (follow existing createTransaction() pattern at lines 87-107). Return json.data (contains { msg_id }).

    4. **getInsightsForecast()**: GET /insights/forecast with credentials: 'include' (follow existing getMonthlySummary() pattern at lines 45-52). Return json.data (array of forecast objects).

    **Create frontend/src/lib/insights.ts** (follow linearRegression.ts pattern — pure function exports, no React):

    1. **formatRelativeTime(createdAt: string): string** — Convert ISO timestamp to Polish relative time: &lt;1 min → "przed chwilą", &lt;60 min → "X min temu", &lt;24h → "X godz. temu", &lt;48h → "wczoraj", &lt;7d → "X dni temu", else short date format "DD.MM.YYYY". Use simple Date arithmetic (no library needed).

    2. **getPriorityColor(priority: 'high' | 'medium' | 'low'): { dot: string; text: string }** — Per UI-SPEC: high → dot "bg-red-400" text "text-red-400", medium → dot "bg-amber-400" text "text-amber-400", low → dot "bg-blue-400" text "text-blue-400". Returns Tailwind classes.

    3. **getTypeLabel(type: 'alert' | 'tip' | 'trend' | 'forecast'): string** — Polish labels per UI-SPEC Copywriting Contract: alert → "Alert", tip → "Porada", trend → "Trend", forecast → "Prognoza".

    4. **getTypeIcon(type: 'alert' | 'tip' | 'trend' | 'forecast'): string** — Per UI-SPEC: alert → exclamation-triangle, trend → trending-up, tip → lightbulb, forecast → chart-line. Return SVG path string identifiers (components will render actual SVG inline).

    Export all 4 functions.
  </action>
  <verify>
    <automated>grep -c "export async function getInsights" frontend/src/api.ts &gt;= 1 && grep -c "export async function dismissInsight" frontend/src/api.ts &gt;= 1 && grep -c "export async function generateInsights" frontend/src/api.ts &gt;= 1 && grep -c "export async function getInsightsForecast" frontend/src/api.ts &gt;= 1 && grep -c "export function formatRelativeTime" frontend/src/lib/insights.ts &gt;= 1</automated>
  </verify>
  <acceptance_criteria>
    - api.ts contains 4 new exported async functions: getInsights, dismissInsight, generateInsights, getInsightsForecast
    - getInsights builds URLSearchParams from optional type, dismissed, page, per_page
    - dismissInsight sends PATCH with { dismissed: true } JSON body
    - generateInsights sends POST with credentials
    - All functions follow existing error handling pattern: res.json().catch(()=>({})) and throw with error?.message
    - lib/insights.ts exports 4 functions: formatRelativeTime, getPriorityColor, getTypeLabel, getTypeIcon
    - formatRelativeTime handles: seconds, minutes, hours, yesterday, days, fallback date
    - getPriorityColor returns { dot, text } with correct Tailwind classes per priority
    - getTypeLabel returns Polish strings
  </acceptance_criteria>
  <done>API client functions added for all insight endpoints; formatting utilities created for Polish labels, relative timestamps, and priority colors.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard InsightsWidget component</name>
  <files>frontend/src/components/InsightsWidget.tsx</files>
  <read_first>
    - frontend/src/components/ImportStatus.tsx — polling pattern (useEffect + setInterval), card pattern (bg-slate-900/80 border border-slate-800 rounded-2xl p-6), status badge pattern (lines 92-119), loading state with spinner (lines 26-60)
    - frontend/src/pages/DashboardPage.tsx — loading/error/empty state pattern (lines 58-84): spinner with "Loading...", error card with bg-red-950/50, empty state with bg-slate-900/50
    - frontend/src/lib/insights.ts — formatRelativeTime, getPriorityColor, getTypeLabel
    - frontend/src/api.ts — getInsights (Task 1)
  </read_first>
  <action>
    Create `frontend/src/components/InsightsWidget.tsx`. Per D-08: horizontal row of 3 compact insight cards above the existing 4 charts on DashboardPage.

    Component: No props needed. Self-contained data fetching.

    **States** (following DashboardPage's loading/error/empty pattern):
    - **Loading**: 3 shimmer placeholder cards (matching the 3-card layout) with `animate-pulse bg-slate-800/50 rounded-xl` — each 200px wide, 100px tall skeleton
    - **Error**: Compact error card: `bg-red-950/50 border border-red-800 rounded-lg p-3 text-red-300 text-xs` showing error message
    - **Empty** (no insights yet): Single card with heading "No Insights Yet" and body "Financial insights are generated nightly from your transaction data. Import transactions to get started, or generate insights now." Per UI-SPEC Copywriting Contract empty state.
    - **Has data**: Horizontal row of 3 cards with `flex gap-4 overflow-x-auto`

    **Data fetching**: useEffect calls getInsights({ per_page: 3, dismissed: false }) on mount. Optional polling every 60 seconds (per the agent's Discretion: dashboard auto-refresh). Polling: setInterval with getInsights call, clearInterval on unmount. Keep polling lightweight — only fetch if component mounted.

    **Card rendering** (per D-08 and UI-SPEC):
    Each card in the 3-card row:
    - Wrapper: `bg-slate-900/80 border border-slate-800 rounded-xl p-4 min-w-[200px] flex-shrink-0` (compact version of the full insight card pattern)
    - Header row: Priority colored dot (6px `rounded-full` circle using getPriorityColor().dot) + insight type label badge using getTypeLabel() in a `text-xs px-2 py-0.5 rounded-full bg-slate-800 text-slate-300` pill
    - Body: `text-sm text-slate-200 line-clamp-2` (first 2 lines of insight content, per UI-SPEC)
    - Footer: `text-xs text-slate-500 mt-2` showing formatRelativeTime(createdAt) + `→` link to `/insights` page (use window.location navigation)

    **Widget header**: Above the card row, `text-sm font-semibold text-slate-400 uppercase tracking-wider mb-3` — label "Latest Insights" (per UI-SPEC Copywriting).
  </action>
  <verify>
    <automated>bun test tests/ui-components.test.ts -t "InsightsWidget" 2&gt;/dev/null || echo "Component created (rendering test added in Plan 05)"</automated>
  </verify>
  <acceptance_criteria>
    - InsightsWidget.tsx is a default-exported React functional component
    - Renders 3 shimmer skeleton cards in loading state with animate-pulse
    - Renders error card with bg-red-950/50 when fetch fails
    - Renders empty state with "No Insights Yet" heading when data is empty array
    - Renders 3 compact cards with priority dot + type badge + 2-line content + relative timestamp when data exists
    - Cards use bg-slate-900/80 border-slate-800 rounded-xl styling
    - Each card's priority dot color matches getPriorityColor(priority) output
    - Relative timestamp uses formatRelativeTime() from lib/insights.ts
    - Optional 60-second polling with cleanup on unmount
  </acceptance_criteria>
  <done>InsightsWidget component renders 3 compact insight cards on the dashboard with loading, error, and empty states.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ComboChart with AI forecast line and integrate widget on DashboardPage</name>
  <files>
    frontend/src/charts/ComboChart.tsx
    frontend/src/pages/DashboardPage.tsx
  </files>
  <read_first>
    - frontend/src/charts/ComboChart.tsx — current interface: ComboDataPoint (month, wydatki, przychody, stan_konta), ComboChartProps (data, prediction, onMonthClick), mergedData with prediction dataKey, existing Line elements (lines 73-84)
    - frontend/src/pages/DashboardPage.tsx — current layout: heading + 4-chart grid (lines 86-127), imports, state management
    - frontend/src/components/InsightsWidget.tsx — the component created in Task 2
    - frontend/src/api.ts — getInsightsForecast (Task 1)
  </read_first>
  <action>
    **Extend ComboChart.tsx** for dual prediction lines (per D-16 and UI-SPEC Chart Contract):

    - Add to ComboDataPoint interface (or create AugmentedComboDataPoint): `aiForecast: number | null` field
    - Add to ComboChartProps: `aiForecast?: PredictionPoint[]` (optional — backward compatible with existing usage on MonthlyPage)
    - In the component, merge aiForecast into mergedData: `aiForecast: aiForecast?.[index]?.value ?? null`
    - After the existing LR prediction Line (lines 76-84), add a second Line:
      ```tsx
      <Line
        type="monotone"
        dataKey="aiForecast"
        stroke="#06b6d4"          // cyan-500 — per UI-SPEC Color table
        strokeWidth={2}
        strokeDasharray="4 4"     // short dashes — distinct from LR's "8 4" long dashes
        connectNulls
        name="Predykcja (AI)"     // per UI-SPEC Copywriting
      />
      ```
    - The existing LR line keeps its current props: dataKey="prediction", stroke="#f59e0b", strokeDasharray="8 4", name should be updated to "Predykcja (LR)" per UI-SPEC Copywriting (line 83 currently says "Predykcja")
    - If aiForecast prop is not provided, the Line simply has no data points and renders nothing — ComboChart remains backward compatible

    **Modify DashboardPage.tsx** to integrate widget and AI forecast:

    - Import InsightsWidget: `import InsightsWidget from '../components/InsightsWidget';`
    - Import getInsightsForecast: add to the api import
    - Add state: `const [aiForecast, setAiForecast] = useState&lt;{ monthIndex: number; value: number }[] | null&gt;(null);`
    - Add useEffect: call getInsightsForecast(), transform response into { monthIndex, value }[] format matching existing predictionData shape. Map forecast months to indices in the existing data array. If forecast data is unavailable or empty, aiForecast stays null (chart renders without AI line).
    - Between the heading section (lines 88-91) and the chart grid (line 93), insert: `<InsightsWidget />`
    - Pass aiForecast to ComboChart: `<ComboChart data={data} prediction={predictionData} aiForecast={aiForecast} onMonthClick={onMonthClick} />`
    - AI forecast fetch should be non-blocking: if it fails, log a warning, keep aiForecast=null — dashboard works without it
  </action>
  <verify>
    <automated>bun test tests/ui-components.test.ts -t "ComboChart" 2&gt;/dev/null && bun run build:web 2&gt;/dev/null || echo "Modifications complete (tests in Plan 05)"</automated>
  </verify>
  <acceptance_criteria>
    - ComboChart.tsx accepts optional aiForecast prop of type PredictionPoint[]
    - Second Line element renders with dataKey="aiForecast", stroke="#06b6d4" (cyan-500), strokeDasharray="4 4", name="Predykcja (AI)"
    - Existing LR line updated: name="Predykcja (LR)" (was "Predykcja")
    - ComboChart backward compatible — existing callers without aiForecast prop still compile and render
    - DashboardPage.tsx imports and renders &lt;InsightsWidget /&gt; above the chart grid
    - DashboardPage.tsx fetches forecast data from getInsightsForecast() with non-blocking error handling
    - aiForecast data mapped to { monthIndex, value } format and passed to ComboChart
  </acceptance_criteria>
  <done>ComboChart extended with AI forecast line; DashboardPage shows InsightsWidget above charts with AI forecast data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→API | Frontend fetches insights via credentials: 'include' (session cookie). API responses are untrusted — must handle null/undefined data gracefully. |
| User→UI | Dismiss button triggers PATCH — user action is intentional. Generate button triggers POST — rate-limited server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Information Disclosure | InsightsWidget rendering LLM-generated content | accept | Insight content is user's own financial data analyzed by AI. Displayed only to authenticated user. Content may contain hallucinated claims but is labeled as AI-generated. Accept risk. |
| T-04-21 | Denial of Service | 60-second polling in InsightsWidget | accept | Polling fetches max 3 rows per request. Lightweight endpoint. Accept risk. |
| T-04-22 | Tampering | ComboChart rendering AI forecast values | accept | Forecast data is for informational purposes only — labeled "Predykcja (AI)" to distinguish from statistical LR prediction. Users can visually compare. No financial decisions automated from chart data. Accept risk. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages required. React, Recharts, Tailwind already installed. |
</threat_model>

<verification>
grep -c "getInsights" frontend/src/api.ts &gt;= 1
grep -c "dismissInsight" frontend/src/api.ts &gt;= 1
grep -c "generateInsights" frontend/src/api.ts &gt;= 1
grep -c "getInsightsForecast" frontend/src/api.ts &gt;= 1
grep -c "formatRelativeTime" frontend/src/lib/insights.ts &gt;= 1
grep -c "InsightsWidget" frontend/src/pages/DashboardPage.tsx &gt;= 1
grep -c "aiForecast" frontend/src/charts/ComboChart.tsx &gt;= 1
grep -c "Predykcja (AI)" frontend/src/charts/ComboChart.tsx == 1
grep -c "#06b6d4" frontend/src/charts/ComboChart.tsx == 1
</verification>

<success_criteria>
- DashboardPage renders InsightsWidget above the existing 4-chart grid
- InsightsWidget shows 3 compact insight cards with priority dots, type badges, content preview, and relative timestamps
- ComboChart renders two prediction lines: LR (amber, long dashes) + AI (cyan, short dashes)
- All API client functions exist and follow existing patterns
- Backward compatible: ComboChart and DashboardPage work without AI data (graceful degradation)
</success_criteria>

<output>
Create .planning/phases/04-ai-insights-forecasting/04-04-SUMMARY.md when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `getInsights` | async function | frontend/src/api.ts | Fetch paginated insights from GET /insights |
| `dismissInsight` | async function | frontend/src/api.ts | PATCH /insights/:id/dismiss |
| `generateInsights` | async function | frontend/src/api.ts | POST /insights/generate |
| `getInsightsForecast` | async function | frontend/src/api.ts | Fetch forecast data from GET /insights/forecast |
| `formatRelativeTime` | function | frontend/src/lib/insights.ts | Convert ISO timestamp to Polish relative time string |
| `getPriorityColor` | function | frontend/src/lib/insights.ts | Return { dot, text } Tailwind classes for priority level |
| `getTypeLabel` | function | frontend/src/lib/insights.ts | Return Polish label for insight type |
| `getTypeIcon` | function | frontend/src/lib/insights.ts | Return icon identifier for insight type |
| `InsightsWidget` | React component | frontend/src/components/InsightsWidget.tsx | 3-card dashboard widget |
| `aiForecast` prop | React prop | frontend/src/charts/ComboChart.tsx | Optional PredictionPoint[] for AI forecast line |
| "Predykcja (AI)" Line | Recharts element | frontend/src/charts/ComboChart.tsx | Second prediction line (cyan, short dashes) |
| "Predykcja (LR)" Line (renamed) | Recharts element | frontend/src/charts/ComboChart.tsx | Existing LR line renamed from "Predykcja" |
