---
phase: 09-testing-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/api-scoping.test.ts]
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "User A can paginate through their own transactions — page 2 contains no User B data"
    - "User A's filtered queries (by type, date range, uncategorized, account_id) return only User A's data"
    - "Sequential bulk-created resources by User A are all tagged with User A's user_id"
    - "All 27 existing isolation tests still pass after extension"
  artifacts:
    - path: "tests/api-scoping.test.ts"
      provides: "Extended multi-user isolation matrix (pagination, filtered queries, bulk create)"
      min_lines: 550
  key_links:
    - from: "tests/api-scoping.test.ts Group 6"
      to: "GET /transactions?page=...&per_page=..."
      via: "app.request() with Cookie header"
      pattern: "page.*per_page|offset"
    - from: "tests/api-scoping.test.ts Group 7"
      to: "GET /transactions?type=...&date_from=...&date_to=...&uncategorized=..."
      via: "app.request() with query parameters"
      pattern: "type=|date_from=|uncategorized"
    - from: "tests/api-scoping.test.ts Group 8"
      to: "POST /transactions (sequential)"
      via: "app.request() in loop, SQL SELECT verify user_id"
      pattern: "for|forEach.*app.request"
---

<objective>
Extend the existing 27-test multi-user isolation matrix in `tests/api-scoping.test.ts` with pagination/offset edge cases, filtered query isolation tests, and bulk-create verification.

**Purpose:** Prove that LIST operations (paginated, filtered) respect per-user data boundaries even at page boundaries and with query parameters. Verify sequential bulk-created resources are all correctly tagged.

**Output:** Extended `tests/api-scoping.test.ts` with 3 new describe groups (Groups 6-8), ~200 additional lines.
</objective>

<execution_context>
@/home/olafk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/olafk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-verification/09-CONTEXT.md
@.planning/phases/09-testing-verification/09-RESEARCH.md
@.planning/phases/09-testing-verification/09-PATTERNS.md
@tests/api-scoping.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Group 6 — Pagination and Offset Tests</name>
  <files>tests/api-scoping.test.ts</files>
  <read_first>tests/api-scoping.test.ts</read_first>
  <action>
    Add a new `describe('Group 6: Pagination and Offset — D-02', ...)` block after the existing Group 5 block (line 411).

    Inside this describe block:
    1. Use a `beforeAll` hook to create 11+ transactions for User A via direct SQL INSERT (not API calls, to keep test fast). User A's `userAAccountId` is already available as a module-scoped variable. Use `USER_A_EMAIL` to look up User A's ID:
       ```typescript
       const userAId = (await sql`SELECT id FROM "user" WHERE email = ${USER_A_EMAIL}`)[0].id;
       ```
       Insert 11+ transactions with incrementing dates and descriptions like `'Page test A ${i}'`.

    2. Test: `GET /transactions?page=1&per_page=5 returns 5 User A transactions` — verify status 200, json.data.length is 5 (or less if fewer), and each item's description matches the 'Page test A' pattern (they belong to User A, not User B). Use module-scoped variable to store the 11th ID for cross-user checks.

    3. Test: `GET /transactions?page=2&per_page=5 returns remaining User A transactions` — verify items belong to User A, no User B data appears.

    4. Test: `GET /transactions?page=999&per_page=50 returns empty array` — beyond-last-page test (D-02 mentions empty array for offset beyond data).

    5. Test: `User B cannot see any User A paginated transactions` — User B fetches page 1, verify User A's transaction IDs are not present.

    Use an `afterAll` hook to clean up the test transactions: `DELETE FROM transactions WHERE description LIKE 'Page test A %'`.

    Per D-02: Add pagination + offset tests — LIST with skip/limit parameters, verify page-2 results don't include cross-user data.
  </action>
  <verify>
    <automated>bun test tests/api-scoping.test.ts --timeout 30000 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Group 6 exists after Group 5 with 4-5 it() blocks
    - Pagination tests verify per-page count and cross-user data absence
    - Beyond-last-page returns empty array
    - All 27 existing tests still pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Group 7 — Filtered Query Tests</name>
  <files>tests/api-scoping.test.ts</files>
  <read_first>tests/api-scoping.test.ts</read_first>
  <action>
    Add a new `describe('Group 7: Filtered Query Isolation — D-03', ...)` block after Group 6.

    Inside this describe block:
    1. Use `beforeAll` to insert mix of transactions for User A via SQL: one expense, one income, one with a future date, one uncategorized (category_id = NULL). Use direct SQL INSERT with `userAAccountId`. Also insert a User B transaction with description `'User B filtered data'` via SQL.

    2. Test: `?type=expense returns only User A's expense transactions` — verify returned items are all type 'expense' AND description does not match 'User B filtered data'.

    3. Test: `?date_from=...&date_to=... respects user boundary` — use a date range that captures User A's transactions but also User B's. Verify the returned items don't include User B's transaction.

    4. Test: `?uncategorized=true returns only User A's uncategorized transactions` — verify returned items have null category_id AND belong to User A.

    5. Test: `?account_id=... returns only that account's User A transactions` — use User A's account_id. Verify no cross-user data.

    Use `afterAll` to clean up test transactions: `DELETE FROM transactions WHERE description LIKE 'Filtered test %'` AND `WHERE description = 'User B filtered data'`.

    Per D-03: Add filtered query tests — search by amount/date/description/category-filtered LIST — verify filters respect user_id boundary.
  </action>
  <verify>
    <automated>bun test tests/api-scoping.test.ts --timeout 30000 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Group 7 exists after Group 6 with 4-5 it() blocks covering type, date range, uncategorized, account_id filters
    - Each test verifies cross-user data does NOT appear in filtered results
    - All previous groups (1-6) still pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Group 8 — Bulk/Multi-Create User Tagging Tests</name>
  <files>tests/api-scoping.test.ts</files>
  <read_first>tests/api-scoping.test.ts</read_first>
  <action>
    Add a new `describe('Group 8: Bulk/Multi-Create User Tagging — D-04', ...)` block after Group 7.

    Inside this describe block:
    1. Test: `sequentially create 3 transactions for User A, all tagged with user_id`:
       - Use a for loop calling `app.request('POST /transactions', ...)` sequentially (not parallel).
       - Store the created IDs in an array.
       - After all creates, SQL SELECT each created transaction's `user_id` and verify it matches User A's user ID.

    2. Test: `User B cannot access User A's bulk-created transactions`:
       - Use the IDs from the previous test. App.request GET /transactions/:id with User B's cookie. Expect 404 for each.

    3. Test: `User B's own bulk-created transactions are tagged to User B`:
       - Create 2 transactions for User B sequentially.
       - Verify via SQL SELECT that all are tagged to User B's user_id.

    Use `afterAll` to clean up: `DELETE FROM transactions WHERE description LIKE 'Bulk test %'`.

    Per D-04: Add bulk/multi-create tests — verify all created resources are correctly tagged to the creating user.
  </action>
  <verify>
    <automated>bun test tests/api-scoping.test.ts --timeout 30000 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Group 8 exists after Group 7 with 3 it() blocks
    - All created transactions are verified via SQL SELECT to have correct user_id
    - Cross-user 404 verified for bulk-created resources
    - All previous groups (1-7) still pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User A → API → DB | Authenticated user creates/reads resources through API layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | GET /transactions with pagination/filter params | mitigate | Pagination tests (D-02) verify page-2 contains no cross-user data; filtered query tests (D-03) verify user boundary |
| T-09-02 | Tampering | POST /transactions bulk create | mitigate | SQL SELECT verification after each bulk create ensures user_id is correctly tagged (D-04) |
| T-09-SC | Tampering | npm install (no new packages in this plan) | accept | No new dependencies introduced — only extending existing test file |
</threat_model>

<verification>
- `bun test tests/api-scoping.test.ts --timeout 30000` — all tests pass (existing 27 + new groups)
- No existing test groups modified (Groups 1-5 remain identical)
- Clean-up hooks properly remove test data
</verification>

<success_criteria>
- [ ] `bun test tests/api-scoping.test.ts --timeout 30000` passes with all 27+ existing tests + ~12 new tests
- [ ] Pagination tests verify cross-user data exclusion at page boundaries
- [ ] Filtered query tests verify each filter parameter respects user boundary
- [ ] Bulk create tests verify user_id tagging via SQL SELECT
- [ ] All test data cleaned up in afterAll hooks
</success_criteria>

<output>
Create `.planning/phases/09-testing-verification/09-01-SUMMARY.md` when done
</output>
